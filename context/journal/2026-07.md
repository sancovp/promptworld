2026-07-10T01:38:00  DECISION: -m DECISION: ISAAC DIRECTIVE 2026-07-10 (verbatim): ok so we should update promptworld now i think right? since PW is supposed to have this whole system in it, and be our app whatever you call this at the top -- the Wizard lmao and now we know how we should theme it and give it roles etc -- not Wrights they are Wizards. INTERPRETATION GROUNDED IN THE RECORD: the geometry doc SkillWizard row already says PromptWorld = the first SkillWizard (its CEO builds the tome, train 52.1), and the task-14 journal parked the coupled nomicon rename unit until promptworld is next touched — this directive touches it, so the rename unit FIRES NOW. THE UPDATE SCOPE: (1) THEME: the seven specialist Wrights (Skillwright/Toolwright/Promptwright/Harnesswright/Teamwright/Flowwright/Systemwright) become WIZARDS; the app ITSELF is THE SkillWizard (order-1 = the tome as app, an agent designed to wield the SkillTome); the tome metaphor is native — SkillTome = the spellbook, frameworks = chapters (already canon), folding = inscribing. (2) THE SYSTEM IN IT: wire agent-skilltree 0.3.0 tome ops + the framework package + the skill2framework chain + the sancovp tome into PW (deps in the image, the tome surfaced to the agents). (3) THE COUPLED RENAME UNIT: fold-into-nomicon stage dir -> fold-into-tome, nomicon-atomize succession, promptworld nomicon references -> tome vocabulary (history/journal files stay unrewritten). Reading the full PW boundary before editing: README (read), world-context, engineer-ceo, the seven specialist CLAUDE.mds, global-context, promptworld_agent.py, agent_profiles.py, the two PW skills, the wrightmaster + nomicon vision files, deploy wiring. No backticks.
2026-07-10T01:47:17  DECISION: -m DECISION: THE WRIGHTMASTER RENAME IS RESOLVED — AS THE ARCHWIZARD (2026-07-10). The 2026-06-08 vision entry queued: rename CEO -> WrightMaster or whatever, calling the individual *Wrights or whatever. Isaacs 2026-07-10 directive settles the or-whatever: the roles are WIZARDS, not Wrights, because PromptWorld IS the first SkillWizard (the tome-as-app, SKILLTOME-GEOMETRY section 0.C). LANDED: the CEO role is now the ARCHWIZARD (display name in agent_personas.json; persona rewritten in agents/engineer-ceo.md which keeps its legacy filename; wields the SkillTome via the new wield-the-tome skill); the eight craft masters are Skillwizard, Toolwizard, Promptwizard, Harnesswizard, Teamwizard, Cave Teamwizard, Flowwizard, Systemwizard (world-context.md rewritten as the Wizards Tower, now with all EIGHT crafts including cave_team which the old seven-craft table omitted — drift fixed on sight); the SkillTome system is wired into the image (Dockerfile: agent-skilltree>=0.3.0 + the framework package + /opt/skill2framework + /opt/tome; build.sh dual-mode staging — chaincompiler bits from the public repo when the monorepo is absent, the tome itself dev-mode-only). The still-deferred piece of the old queue: CEO-runs-team hookup (unchanged, separate work). No backticks.
2026-07-10T02:11:23  FINDING: -m FINDING: PW IMAGE CRASH ROOT-CAUSED + FIXED (2026-07-10, monorepo 5d93315a). The first wizard-update image built green but the container DIED at server start. CHAIN TO THE EXACT SYMPTOM: current monorepo sdna chain_ontology.py imports uco (the Universal Chain Ontology Link/Chain primitives — another sessions in-flight integration) -> the dep was NEVER DECLARED in sdna pyproject -> the promptworld Dockerfile installs sdna with no-deps -> import sdna raises ModuleNotFoundError uco -> cave core/mixins/anatomy.py catches it, prints SDNA not available anatomy features limited, then its DEGRADED PATH ITSELF crashes at class-definition time (Heart dataclass annotates beats with Heartbeat which only exists when sdna imported) -> NameError kills the server process. TWO FIXES LANDED: sdna pyproject now declares universal-chain-ontology>=0.1.0, and the PW Dockerfile installs it before the cave/sdna layer. LOAD-BEARING TRAP RECORDED IN BOTH FILES: the import name is uco but the PyPI name uco is SQUATTED by an unrelated reserved placeholder (author Wenyu Zhao) — the real distribution is universal-chain-ontology (github sancovp/universal-chain-ontology, PyPI 0.1.0, repo public). NEVER pip install uco. FLAGGED NOT FIXED (separate owner): caves anatomy degraded path is incoherent — it announces limited-features fallback then dies anyway; per no-silent-fallbacks it should either hard-fail with the real cause or actually degrade. ALSO BLOCKED: MINIMAX_API_KEY is not present anywhere on this host right now (env, tmux global env, heaven_data, bash history all empty) — the container boots and serves but agent CHAT turns cannot authenticate, so the e2e chat leg + the first Archwizard live turn need Isaac to export the key. Rebuild with the fix in flight. No backticks.
2026-07-10T02:15:40  FINDING: -m FINDING: TASK 17 COMPLETE — THE FIRST SKILLWIZARD IS RUNNING (2026-07-10). The rebuilt image (with the uco fix) boots and I verified IN THE LIVE CONTAINER: /api/health ok; the SPA serves; the personas API returns display_name Archwizard for ceo and Skillwizard for skill (the hot-reloading agent_personas.json working through the real surface); /opt/tome holds the full author tome (root sancovp-tome, all five volumes pattern/factory/practice/runtime/pipe + skilltree.json + README); /opt/skill2framework holds all six stage skills + the compiled rollup; the app skills dir carries wield-the-tome and ingest-into-tome-app; and python imports of sdna + uco + framework + skilltree (0.3.0) ALL succeed in-container. DOCUMENTED LIMITATION (by design, stated in wield-the-tome): the tomes Frameworks row targets point into the dev monorepo, so in-container skilltree validate would fail on row targets — the tables serve as the INDEX in-container (names + what-for), and the resolvable form arrives when v1 chapters publish. REMAINING (blocked on Isaac): the live Archwizard CHAT e2e (a real agent turn + the first tome consultation) needs MINIMAX_API_KEY exported on this host — not present anywhere I could find. Task 17 closed; the chat leg is the first thing to run when the key lands. No backticks.
