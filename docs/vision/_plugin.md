# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T16:02:26  DECISION: DEPLOYMENT MODEL confirmed (Isaac) = complexity-ladder rungs 1->6->7 made concrete. RUNG 1 (plugin): the shipped claude-code plugin installs SKILLS into the user's Claude Code (legible, normal install). RUNG 6 (containerize): the plugin ALSO carries an INSTALL/IMAGE-WIZARD SKILL that runs a docker-compose/.sh — IF the reading agent can run it — which SPAWNS THE WHOLE PROMPTWORLD SYSTEM (its own agents, its own CC login, its own creds) in a CONTAINER. Pattern = the JOBWORLD IMAGE (jobworld-cave:latest already does this; ink-ceo served from inside it). RUNG 7 (fold-in via skill-over-API): the user's MAIN Claude Code talks to the spawned system via the skill; the GROUP CHAT works because CAVE CALLS/WAKES the CEO agent whenever a message lands in its INBOX, and the frontend shows the inbox messages — so BOTH the user (via frontend) and their main agent (via skill) feed the SAME inbox = 'talk to it WITH them'. CONSEQUENCE for the frontend: the assistant-ui app LAUNCHES INSIDE THE IMAGE, served by the FastAPI server (like ink-ceo). BUILD ORDER (do NOT conflate): slice 2 = build the assistant-ui app served by the server (image-READY), verified locally first; the IMAGE/compose/wizard-skill packaging + the CAVE-inbox group-chat (input/wake half) are LATER layers after the app works locally. Isaac will verify the vite/react visual once it runs as the separate system/image; builder verifies headlessly (builds + server serves + adapter maps events + optional playwright smoke).  tags:[Promptworld, Plugin, Docker, Jobworld_Image, Cave_Inbox, Group_Chat, Complexity_Ladder]
- [v2]  2026-06-08T17:13:11  INTENT: Baking /home/ceo/.claude/settings.json into the image (deploy/claude-settings.json) registering doc-mirror as a Claude Code plugin via directory-source marketplace -> /opt/doc-mirror-plugin + enabledPlugins. This is the canonical container-CC plugin registration Isaac demanded ('load the plugin into the Claude Code application directories in the container'). Additive: SDK agents keep getting it via plugins=/--plugin-dir; this gives the INTERACTIVE terminal claude the plugin too. Mirrors the host registration. Rebuild+reboot+verify via 'claude plugin list' (no LLM call, no OAuth risk).  tags:[doc-mirror, plugin, container, settings]
