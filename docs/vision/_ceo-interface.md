# vision: promptworld-system (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-01T12:47:59  DECISION: CEO INTERFACE — tmux vs claude -p, answered from CAVE code. What tmux DOES (cave/core/agent.py send_keys/capture_pane/send_and_wait + anatomy.py heartbeat + EventBridge): (1) hold a PERSISTENT interactive claude TUI alive across turns; (2) send_keys = inject a prompt BUT ALSO drive the TUI with control keys (Down/Enter/C-c/menu-nav/'/resume' selection, with sleeps) — puppeting the interactive UI; (3) capture_pane = read the scrollback; (4) send_and_wait = poll the pane until output 'stabilizes' (flaky response-detection, no clean return); (5) ASYNC-INJECT into a LIVE/mid-turn session (heartbeat tick + worker DONE-signals send_keys into the running agent anytime). -p PARITY: persistent->--resume <session_id>; inject prompt->claude -p --resume sid 'msg' (native); response->-p returns stdout / --output-format stream-json (replaces the capture-pane-poll); live mirror->stream-json rendered as chat; TUI-puppeting (menus/permission-prompts/'/resume'/C-c) DISAPPEARS -> becomes FLAGS: --permission-mode bypassPermissions + --resume + kill-subprocess (this is exactly the brittle tmux class Isaac hates, and it vanishes); full Claude Code -> -p is full claude headless. THE ONE GENUINE GAP: tmux can inject/interrupt a turn ALREADY mid-run; -p is request->response so async events (heartbeat, DONE) are delivered at the NEXT TURN BOUNDARY via a message QUEUE — benign for this use (turns are short; you don't need to interrupt). CAVE COST = ~NOTHING (Isaac right): /input->main_agent.send_keys, /output->main_agent.capture_pane, heartbeat->send_keys ALL go through the abstract main_agent; swap to a -p-backed main_agent (send_keys->enqueue-next-turn, capture_pane->return stream-json transcript) + a turn-loop; server/dashboard/data-model/dept-agent/SOP-harvest UNCHANGED. THE -p-PARITY UTIL = (1) per-agent session_id persistence (--resume); (2) message-queue + turn-loop (async events at turn boundaries); (3) --output-format stream-json (chat output); (4) --permission-mode bypassPermissions (kills the interactive-prompt puppeting). cave-teams TeamLeader + promptworld run_agent/server already prototype this. RECOMMENDATION: CEO = chat-UI over claude -p --resume (full Claude Code, no tmux) + that small util layer.  tags:[promptworld-system, ceo-interface]
- [v2]  2026-06-01T12:51:26  FINDING: PAIA HOOKS transfer to claude -p cleanly — verified from code. The paia_*.py hooks (pretooluse/stop/userpromptsubmit/...) are PURE HTTP transport: each reads hook stdin JSON and POSTs to CAVE at http://localhost:8080/hook/<type>, acting on the JSON response {result: continue|block, reason, additionalContext} via exit codes/stderr; CAVE-offline -> fail-open 'continue'. ZERO tmux/send_keys/capture_pane in them. Since headless 'claude -p' fires the SAME hook events (PreToolUse/PostToolUse/Stop/UserPromptSubmit/SessionStart) as interactive claude, the paia hooks fire identically on -p -> the CAVE connection works headless. IMPORTANT NUANCE: CAVE actually STEERS the agent through the hook RESPONSE (block + additionalContext on Stop/UserPromptSubmit — e.g. paia_stop has timeout=600 so CAVE can hold the Stop and return block to keep the agent looping, like the brainhook). That steering is HOOK-based, not tmux — so it transfers to -p too. The ONLY tmux-specific injection is the ASYNC send_keys path (heartbeat tick + EventBridge DONE-signals injected mid-session) -> on -p that becomes the next-turn message queue (already planned). REQUIREMENT this adds: the -p agent must run with the paia hooks in its SCOPED/LEAN .claude settings (paia hooks + that agent's skills only — NOT the full host bloat that stalls init, per the promptworld lean-options finding). NET: confirms CEO = chat-UI over claude -p; CAVE-connection (paia hooks) + CAVE-steering (hook responses) both transfer; only the async send_keys becomes a turn-queue.  tags:[promptworld-system, ceo-interface]
- [v3]  2026-06-01T12:59:28  DECISION: CEO INTERFACE — SETTLED (Isaac). The CEO (and every *Gym agent) is a CHAT-UI over 'claude -p --resume <session_id>' — NO tmux. THE KEY ADDITION = CONVERSATION MANAGEMENT via an ALIAS<->session_id REGISTRY: the user names conversations (human aliases) mapped to their claude -p resume IDs, giving NAMED, SAVE/RESUMABLE conversations. This is the convo management we currently LACK — tmux gives a live session but no named/managed/resumable conversations; the alias->resume-id map IS that layer. Applies to the CEO chat AND the *Gym 1-off agent chats (save/resume by alias = the 'talk to any agent, save and resume' requirement). FULL -p-PARITY UTIL (the build): (1) alias<->session_id registry = convo persistence + management (rename, list, resume by alias); (2) turn-loop + message-queue for async events (heartbeat / worker DONE-signals delivered at the NEXT turn boundary — the one thing tmux did that -p can't is mid-turn inject, made benign); (3) --output-format stream-json rendered as chat (replaces capture-pane); (4) --permission-mode bypassPermissions (kills the interactive-prompt puppeting); (5) paia hooks in the agent's SCOPED/LEAN .claude settings (CAVE connection + CAVE-steering-via-hook-responses both transfer to -p; paia hooks are pure HTTP-to-localhost:8080, tmux-agnostic, verified). CAVE COST ~NOTHING: swap CAVE's tmux main_agent for a -p-backed main_agent behind the SAME interface (/input->enqueue-next-turn, /output->return the stream-json transcript); server/dashboard/data-model/dept-agent/SOP-harvest UNCHANGED. SUBSTRATE (recap, settled) = CAVE fork: PromptWorldAgent(CAVEAgent) + PromptWorldHTTPServer. BOTH big decisions now settled. REMAINING GATE before build = verify CAVE boots/runs cleanly today (its .claude/rules flag a mid-refactor). cave-teams TeamLeader + promptworld run_agent.py/server.py already prototype the -p + SDK + session pieces to harvest.  tags:[promptworld-system, ceo-interface]
- [v4]  2026-06-01T14:27:25  FINDING: STEP1 EMPIRICAL (p_main_agent build): claude -p resume mechanism confirmed. Turn1 'claude -p --session-id <uuid> --model sonnet --output-format json --permission-mode bypassPermissions "<prompt>"' presets the id; subsequent turns 'claude -p --resume <uuid> --model sonnet --output-format json --permission-mode bypassPermissions "<prompt>"'. PROVEN 2-turn: turn1 'Remember 42'->result 'OK'; turn2 --resume->result '42'. FINDINGS: (1) --output-format json emits a JSON ARRAY of events; final type==result event holds .result(text) + .session_id(live id). (2) session_id does NOT rotate across --resume (stays same unless --fork-session). (3) --session-id <uuid> presets a chosen id so the registry owns it from turn1, no scraping. (4) host default model claude-3-5-sonnet-latest is 404 from -p here, so explicit --model 'sonnet' is REQUIRED. (5) sessions persist at ~/.claude/projects/<cwd-slug>/<sid>.jsonl, slug=abs cwd with each non-alnum char->'-'; this on-disk transcript is the restart-surviving resume-vs-preset signal. tags:[promptworld-system, ceo-interface, p-main-agent]  tags:[ceo-interface]
- [v5]  2026-06-01T14:27:36  BUILT p_main_agent.py (ClaudePMainAgent) + convo_registry.py (ConvoRegistry) + test_p_main_agent.py — standalone claude -p --resume-backed drop-in for CAVE's tmux main_agent. Public surface mirrors CodeAgent: session_exists(), create_session()/spawn_agent(), send_keys(*seq) [str=prompt chunk, 'Enter'=flush one turn, float=ignored sleep], capture_pane(history_limit), send_and_wait(prompt, timeout). ConvoRegistry = alias<->session_id JSON map (default ~/.promptworld/convos.json) giving named save/resumable convos: new/resume/get_or_create/set/rename/delete/list. Pure subprocess+claude -p; NO tmux, NO SDK, NO heaven/sdna. ALL deterministic parts VERIFIED offline (registry CRUD+cross-instance persistence, send_keys/flush state machine incl double-Enter no-op, preset-vs-resume command construction incl on-disk restart detection, JSON-array result parsing). The live 2-turn E2E acceptance test is correct and ran end-to-end (turn1 created session, turn2 resumed it with no 'no conversation found' error) but is currently BLOCKED by account state 'Credit balance is too low' (a bare 'claude -p' one-liner returns the same) — same auth that passed STEP1 earlier this session. Monitor armed to re-run the acceptance test the moment credit returns. paia-hook lean-settings wiring DEFERRED for the offline test (test must not depend on CAVE being up); class accepts an explicit settings_dir for it. tags:[promptworld-system, ceo-interface, p-main-agent]  tags:[ceo-interface]
- [v6]  2026-06-01T14:35:00  FINDING: FIXED + E2E VERIFIED (p_main_agent): the 'Credit balance is too low' block was environmental, NOT a code bug — ANTHROPIC_API_KEY in this container's env forced the claude CLI onto depleted API-key billing instead of the OAuth subscription. Proven: 'claude -p ...'->'Credit balance is too low'; 'env -u ANTHROPIC_API_KEY claude -p ...'->'OK'. FIX in _run_turn: pass env={k:v for k,v in os.environ.items() if k!='ANTHROPIC_API_KEY'} to subprocess.run so the headless turn uses the subscription. Acceptance test now PASSES on the real surface: (a) new('t1')+send_and_wait('Remember 42')->'OK'; (b) FRESH ClaudePMainAgent(alias='t1') send_and_wait('What number?')->'42' (persistence proven via registry+--resume across a fresh instance); (c) registry.list()={'t1':'3ef13771-c021-4489-a511-abacc2eea654'}. tags:[promptworld-system, ceo-interface, p-main-agent]  tags:[ceo-interface]
- [v7]  2026-06-02T01:38:33  FINDING: PERSONA-NOT-WIRED gap (agent-flagged, I confirmed via behavior): the engineer-CEO's /api/chat convo does NOT inject agents/engineer-ceo.md — the CEO replies from generic knowledge (it listed 'skills, agents, commands, hooks, rules' not its persona's 'MCPs/Skills/Harnesses/Operating-Systems/Prompts/Teams/Workflows'). It WORKS for the prompt-engineer ONLY because compile_prompt PREPENDS the persona into the instruction each turn. So: personas attach to claude -p convos by being sent as content; the CEO convo currently has no persona injected (bare claude). FIX (v2.1): inject agents/engineer-ceo.md as the CEO convo's first/system message (and confirm the persona's component-kind list matches the canonical 7). Also OPEN: re-prepending the persona every compile turn is wasteful — a convo should get its persona ONCE at session creation (a ClaudePMainAgent 'system/seed' on create_session). Deferred.  tags:[promptworld-departments, ceo-interface, persona-wiring]
