# vision: promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T12:09:40  DECISION: V2 (a/b/c) per-agent avatar+display-name+profile page (#45). Backend server/agent_profiles.py: JSON store keyed by alias under $PROMPTWORLD_DATA/~/.promptworld; avatar uploaded as base64 data URL in PUT body -> decoded+saved as avatars/<alias>.<ext> -> served by GET .../avatar (NO python-multipart dep). Routes GET/PUT /api/agents/{alias}/profile + GET .../avatar, alias jailed to {ceo}+gym types. Frontend: AgentAvatar circle (img or colored initial), ProfileEditor modal (name+upload+Remove+Edit-prompt), agentProfiles store (useSyncExternalStore re-renders every header on save), editorBus (openFileInEditor->FileExplorer opens path). AgentWindow header = avatar+display-name, click opens profile; SpecialistPage selector shows avatar+name. (c) NO prompt-blocks: Edit-prompt opens agent CLAUDE.md in Monaco (ceo->agents/engineer-ceo.md, type->promptgym/<type>/CLAUDE.md). VERIFIED by rendering real profile routes + dist via playwright 1920x1080: set name+avatar -> header img+name -> RELOAD persists -> Edit-prompt opened engineer-ceo.md -> selector avatar+name. Source-only; commander rebuilds :3858.  tags:[promptworld, avatar, profile, v2]
- [v2]  2026-06-08T12:15:47  FINDING: #46 pre-read (READ-ONLY). PromptWorldAgent(CAVEAgent) calls super().__init__ (promptworld_agent.py:109) -> CAVEAgent.__init__ (cave_agent.py) already: _init_automations() (AutomationRegistry loads /tmp/heaven_data/automations/*.json), start_heart(), and adds a 60s 'cron_scheduler' Tick -> fire_due_automations(). So PromptWorld ALREADY HAS the cron engine running. Reuse surface: AutomationMixin (add_automation/remove_automation/fire_automation/fire_all_due/list_automations/get_automation_status) on pw, + Calendar(Compiler) (list_scheduled/schedule_sync/cancel/view/check_deliverables) over pw.automation_registry. AutomationSchema={name,schedule,prompt_template,code_pointer,code_args,delivery,session_target,enabled,...}. CAVEHTTPServer exposes ONLY /reload_automations + /webhook/{path} — NO list/add/fire CRUD, so crons page = thin NEW routes over the reused engine. CEO heartbeat = a CronAutomation; conductor pattern (heartbeat_cron.conductor_heartbeat_fire) is a code_pointer that delivers to a file inbox — PromptWorld's CEO is ClaudePMainAgent (not a file-inbox poller), so bridge = a tiny code_pointer that POSTs to loopback /api/chat (mirrors the conductor fire fn, incl. enabled/interval/skip-if-user-active). CAVEAT: croniter NOT installed in env -> CronJob.is_due() returns False for 5-field cron exprs (silently never fire); INTERVAL schedules ('every:N') work with zero new dep -> default heartbeat+crons to interval; cron-expr support needs adding croniter to image (team-lead call). sanctum_automations = sanctum-ritual-specific, skip for PromptWorld. VERIFY-IN-CONTAINER for #46: that the Heart thread actually ticks fire_due_automations live on :3858 (no exception killed it). No edits made.  tags:[promptworld, cron, heartbeat, cave, v2]
- [v3]  2026-06-08T12:29:20  DECISION: V2 (f) crons + CEO/agent heartbeat page (#46), REUSING CAVE's automation engine (no rebuild). PromptWorldAgent(CAVEAgent) already runs the AutomationRegistry + a 60s Heart cron tick (fire_due_automations). NEW = thin layer only: server/promptworld_automations.py (helpers over the REAL registry via Automation.create() factory: schema_dict/register/list/set_enabled[rebuilds live CronJob for is_due]/remove/fire_now + a fire_agent_turn code_pointer that fire-and-forgets a daemon-thread POST to loopback /api/chat[ceo] or /api/gym/{alias}[specialist], sending BOTH message+request keys, so the Heart tick never blocks on a model turn). Routes in promptworld_server: GET/POST /api/automations, POST .../{name}/enable|disable|fire, DELETE .../{name}, GET/PUT /api/heartbeat/{alias} (heartbeat = a CronAutomation named heartbeat_{alias}). Default schedule = INTERVAL every:N (no croniter needed; croniter is ABSENT so cron-exprs silently never fire — flagged). Frontend: 4th left-nav 'Crons' page (CronsPage.tsx + automationsApi.ts): heartbeat card (agent select+avatar, interval, enable/disable, prompt, Save), new-automation form (name, target agent, interval, prompt), automations table (avatar+name/target/schedule/runs/last-run/ON-off pill/Fire-Play/Power/Trash). FOLD-IN #45 nit: AgentWindow idle state now renders AgentAvatar (size 56) + '{name} — idle' overlay; assistant-ui's default welcome 'A' avatar hidden via CSS (.aui-thread-welcome-center .aui-avatar-root display:none) + emptyText''. VERIFIED: (a) ENGINE-FIRES PROOF standalone via CAVE's real Automation.create+AutomationRegistry — register->due-after-interval->fire writes sentinel->disable(not due)->enable(due)->remove (PASS, interval works w/o croniter). (b) RENDER loop playwright 1920x1080 vs real profile routes + in-memory automation mock (cave too heavy to import in harness): Main idle shows AVATAR not 'A' (aui avatar hidden); Crons page renders pro + fills frame; create->3 rows; Fire-now->run_count++ + 'dispatched' status; enable/disable toggle both ways (row + heartbeat off/ON pills). Source-only; commander rebuilds + live-verifies the Heart tick + a real CEO-heartbeat turn on :3858.  tags:[promptworld, cron, heartbeat, cave, v2]
- [v4]  2026-06-08T12:37:19  DECISION: #46 follow-up per team-lead rulings: (1) ADDED croniter==2.0.7 to deploy/Dockerfile (after the mcp/sdk line) so 5-field CRON EXPRESSIONS actually fire (without it CAVE SDNA CronJob.is_due returns False for cron exprs). VERIFIED in a throwaway jobworld-cave:latest container: pydantic STAYS 2.10.6 (before+after), import croniter OK + cron parse OK, import cave.server.cave_http_server OK -> NO cascade. (2) cron-expr support in the crons UI: NEW AUTOMATION form now has a Schedule mode toggle (Interval | Cron expr); cron mode sends schedule='0 3 * * *', interval mode sends interval_seconds (backend schema_dict already passed schedule through). (3) view helper promptworld_automations.view(registry,days) -> upcoming fires handling BOTH interval (last_run/now + n*secs) AND cron-expr (croniter) + GET /api/automations/view?days route + an 'Upcoming fires — next 7 days' section on the page. VERIFIED: view() unit test (interval 200-cap + cron-only 3 nightly fires correct); render loop (cron-expr toggle shows the cron input; upcoming-fires section renders 30 rows/175 events; automations table shows '0 3 * * *' + 'every 3600s' w/ avatars; zero page errors). Source-only; commander rebuilds (the croniter layer) + live-verifies.  tags:[promptworld, cron, croniter, v2]
- [v5]  2026-06-08T14:04:09  DECISION: V2 #48: specialized the 7 PromptGym agents from hollow gnosys-seed clones into genuine domain-expert 'guys' via the doc-mirror-prompts method (how-to-make-a-persona-prompt). Each promptgym/<type>/CLAUDE.md rewritten as a tight <GUY> (name meta-first -> description -> <world> @-ref -> core_loop -> expertise[BUILD+CRITIQUE] -> cor -> reinforcement), replacing the ~175-line gnosys bloat (also fixes the 125k seed bloat). The 7 masters: Skillwright(skill), Toolwright(mcp), Promptwright(prompt), Harnesswright(harness), Teamwright(team), Flowwright(workflow), Systemwright(operating_system). Shared promptgym/world-context.md (the Builders' Guild: CEO orchestrator + 7 craft masters + the complexity-ladder defer-relations); every persona @-references ../world-context.md so each knows itself AND the others. NEUTRAL '-wright' theme = trivially re-skinnable (Isaac re-themes Smiths/Djinni later by editing one file + persona voice). NAMES: frontend roster labels (agents.ts) set to persona names = zero-seed default everywhere; backed by agent_profiles.seed_defaults (idempotent, non-destructive, never clobbers a user profile) seeding names+avatars at server start. AVATARS: 8 distinct craft-emblem PNGs (promptgym/avatars/<alias>.png, white shape on accent color) seeded into the profile store. SKILLS: the shippable doc-mirror plugin skills (doc-mirror-prompts=the persona method, make-ai-operating-system, distill-to-essential-transform, ship-a-plugin) are ALREADY symlinked into every specialist's .claude/skills by _provision_aios; the per-craft CATALOG skills (make-skill/make-mcp/make-subagent/fastmcp-setup) do NOT ship in the image -> FLAGGED for vendoring-into-plugin (needs portability review); each persona carries its domain method INLINE + names the catalog skill. VERIFIED: @-ref resolves from all 7 dirs; seed_defaults unit test (seeds once, idempotent, preserves user override); frontend build; RENDER (group page shows all 8 persona names + distinct emblems in header+idle; specialist selector shows persona name+emblem; zero page errors). NOT yet done = the live MiniMax identity-proof (ask each its identity+the others) = commander's container re-verify (cave OOMs the local harness). Source-only.  tags:[promptworld, persona, doc-mirror-prompts, v2, specialization]
- [v6]  2026-06-08T14:19:59  DECISION: #48 follow-up — fixed the 3 issues team-lead flagged on 9b2ca42 (all stated before the un-halt): (1) LITERAL <GUY> tags FORBIDDEN -> each persona now wrapped in its OWN NAME tag (<Skillwright>...</Skillwright>, <Toolwright>, <Promptwright>, <Harnesswright>, <Teamwright>, <Flowwright>, <Systemwright>); the Promptwright (which teaches the persona method) reworded all literal <GUY> body refs to 'the persona's OWN-NAME tag (e.g. <PersonaName>), never a placeholder' — verified 0 literal <GUY> remain. (2) NON-CLAUDE-CODE CONTAMINATION purged: the workflow/Flowwright persona was full of STARSYSTEM terms (flight/waypoint/next_flight/ratchet) -> fully REFRAMED around the actual Claude Code Workflow tool (phase/agent/parallel/pipeline + schema + synthesis); also removed the gnosys skill-category taxonomy (understand/single_turn_process/preflight + 'points to a flight') from the Skillwright -> grounded in real Claude Code skill structure (SKILL.md frontmatter name+description, body, resources/); world-context workflow row de-contaminated; verified 0 flight/waypoint/ratchet/starsystem/mission remain. (3) HARDCODED names/avatars -> HOT-RELOADING JSON config: new promptgym/agent_personas.json (alias->{name,avatar}); agent_profiles.seed_defaults (which BAKED into the store) REPLACED with load_personas (reads the JSON LIVE on each request); server _public + avatar route now fall back to the live config name+emblem when the user hasn't set their own (user override always wins) = true hot-reload, no restart, nothing baked in code; reverted agents.ts roster labels to generic component-type fallbacks (Skill/MCP/...) so names are NOT hardcoded in frontend code (the live config drives display via the profile API); GroupPage window-label default = '' so windows fall back to the persona name. VERIFIED: grep clean (no <GUY>, no STARSYSTEM); load_personas unit (live read, ignores _comment); curl (no-profile -> JSON name+emblem served 200; user override wins); frontend build; render (group windows show persona names from live config + distinct emblems; selector too; zero page errors). Source-only follow-up to 9b2ca42.  tags:[promptworld, persona, bugfix, v2, claude-code-only]
