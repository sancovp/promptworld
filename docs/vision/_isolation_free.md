# vision: Sdna_Heaven_Minimax (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T02:19:58  DECISION: PIVOT (Isaac, grounded by reading understand-sdna-agent-coding skill + sdna/__init__ + quickstart): PromptWorld agents = SDNA agents on the HEAVEN backend with MiniMax — NOT Claude Code (claude dash p), NOT claude_agent_sdk, NOT the dot-claude AIOS mechanism. Proven pattern: HermesConfig(name, system_prompt=<agent identity>, model='MiniMax-M2.5-highspeed', goal, max_turns>=5, mcp_servers={keys match heaven_mcp_config.json}, heaven_inputs=HeavenInputs(agent=HeavenAgentArgs(provider='ANTHROPIC', max_tokens=8000, use_uni_api=False))) -> heaven_agent_step(config) -> StepResult. provider='ANTHROPIC' + use_uni_api=False = ChatAnthropic path with anthropic_api_url OVERRIDE = how MiniMax is reached (Isaac: anything anthropic works with minimax, just redirected; MiniMax detected by MODEL NAME; NO uni-api). KEY: a Heaven agent context IS its HermesConfig ONLY (system_prompt + goal + MCP) — there is NO dot-claude auto-injection and NO host user config to leak, so ISOLATION IS FREE BY CONSTRUCTION. => the ENTIRE session struggle (dot-claude restructure #40, setting_sources, the relocated-config-dir env var, the claude_agent_sdk migration #41) was solving a NON-PROBLEM on this substrate; DROP all of it. The reified layer = HermesConfig + chain_ontology (Link/Chain/Compiler); skills via skillmanager. Imports: from sdna.config import HermesConfig,HeavenInputs,HeavenAgentArgs,HeavenHermesArgs; from sdna.heaven_runner import heaven_agent_step. SDNA also has sdna/claude_code_commands.py (tmux Claude Code driving) + sdna_flow (multi-turn/streaming) — TBD which for chat. STILL TO READ before re-spec: (1) skillmanager attach to HermesConfig, (2) heaven_agent_step/sdna_flow streaming for the gallery /ws, (3) multi-turn persistent chat sessions. Builders HELD (#41 hard-hold). Prereq: install sdna + heaven-framework from their SOURCE repos; MCP must be registered in /tmp/heaven_data/heaven_mcp_config.json.  tags:[Sdna_Heaven_Minimax, Isolation_Free, Drop_Claude_Sdk]
