# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T18:35:55  FINDING: BUILT + E2E-VERIFIED option (C): CEO dispatches PromptGym specialists + gallery (no SDNA, no cave_teams). server/gym_mcp.py = thin stdio FastMCP, tool run_gym_agent(component_type,request) HTTP-POSTs /api/gym/{type}. promptworld_agent: self.gym=PromptGym(); _write_gym_mcp_config(port) writes .promptgym_mcp.json (PROMPTWORLD_PORT baked) giving CEO the tool via mcp_config + dispatch instruction appended to system prompt; run_gym(type,req)=gym.run(type,req,on_event=broadcast_event(type)) IN-PROCESS so the specialist stream hits /ws tagged by type. promptworld_server: POST /api/gym/{ctype}. p_main_agent: ClaudePMainAgent mcp_config→--mcp-config. CRITICAL FIX: --mcp-config is VARIADIC (<configs...>) and SWALLOWED the trailing positional prompt (error: 'MCP config file not found: .../Call your run_gym_agent...'); FIX = feed prompt via STDIN not argv (verified echo|claude -p stream-json returns reply; affects all _run_turn agents, verified via both CEO+gym paths). E2E /tmp/test_ceo_controls_gym.py PASS: /ws shows BOTH 'ceo' AND 'skill' streams (skill init/assistant/result tagged skill = gallery, distinct alias) + CEO reply relays skill specialist verbatim ('I build Skills: a SKILL.md ... and resources/'). Nested claude -p works: server→claude(CEO)→gym_mcp→HTTP→/api/gym→claude(skill). Commit d6451ee. CEO is NOT config_dir-scoped (inherits user ~/.claude, omniscient per D2) — only gym specialists are AIOS-scoped. NEXT (per team-lead): cave_teams adapter (B), gallery frontend (per-window UI). compact()-via-stdin not re-tested this slice.  tags:[Promptworld, Ceo_Control, Promptgym, Gallery, Mcp]
- [v2]  2026-06-07T18:38:49  FINDING: OPTION C VERIFIED BY ME (re-ran /tmp/test_ceo_controls_gym.py + the ws regression + read the stdin fix). commit d6451ee (+156; new server/gym_mcp.py + 4 edits). THE CEO NOW CONTROLS PROMPTGYM WITH THE GALLERY, no SDNA, no cave_teams. Mechanism: server/gym_mcp.py = a thin stdio FastMCP server with ONE tool run_gym_agent(component_type,request) that HTTP-POSTs to /api/gym/{type}; promptworld_agent gives the CEO that tool via _write_gym_mcp_config -> .promptgym_mcp.json -> ClaudePMainAgent(mcp_config=...) -> --mcp-config + a dispatch instruction appended to the CEO system prompt; run_gym(type,req) runs the scoped gym agent IN-PROCESS with on_event=broadcast_event(type,ev) -> /ws tagged by type (gallery); POST /api/gym/{ctype} route. MY re-run: /ws aliases ['ceo','skill'] (the skill specialist on its OWN gallery tag), CEO reply relays the skill answer -> PASS. NESTED claude -p in claude -p proven E2E (server->CEO claude->gym_mcp stdio->HTTP->/api/gym/skill->skill claude scoped to AIOS dir). CRITICAL CORE FIX (affects ALL agents): --mcp-config <configs...> is VARIADIC and SWALLOWED the trailing positional prompt; FIX = feed the prompt via STDIN (p_main_agent.py:285-300 proc.stdin.write) instead of argv. VERIFIED NO REGRESSION: re-ran the ws test, still PASS (live tool_use+tool_result+WSTEST). NOTES: CEO is NOT config_dir-scoped (inherits user ~/.claude = the omniscient CEO per D2); only gym specialists are AIOS-scoped. compact()-via-stdin not re-tested (low risk, same path). REMAINING for promptworld: gallery FRONTEND (per-agent windows in React consuming type-tagged /ws — events flow, UI rendering next); (B) cave_teams adapter for shared-task-list+simultaneous; packaging (#1 code: Dockerfile/compose/install-skill, I write, Isaac runs mount); plugin skills (port-over/install-wizard/talk-to-CEO); nomicon conversion equipped to CEO; minimax-via-env test (deferred). Promptworld core surface now: chat to CEO (live stream+tools) + Monaco file explorer (path-jailed) + CEO dispatches AIOS-dir PromptGym specialists into a per-type gallery.  tags:[Promptworld, Ceo, Promptgym, Gallery, Mcp, Claude_Code_Sdk, Verified, Stdin]
