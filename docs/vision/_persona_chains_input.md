# vision: Persona_Chains_Input (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T14:14:17  DECISION: THE PROMPTING METHOD (Isaac, definitive — the core operating-model correction; I had been violating it all session). (1) MY JOB = read the code + merge/reject PRs + gate to ALL PROFESSIONAL STANDARDS; let ZERO unprofessional/off-domain line through, ever. I do nothing else but review + request changes. (2) I ONLY request SPECIFIC changes in this exact shape: 'You must READ [these lines] in [these modules] and UNDERSTAND [these features]. You must make changes that accomplish [these features] with [these requirements].' (3) EVERY prompt has 3 SECTIONS: PERSONA + CHAINS + INPUT REQUEST. The persona + chains LOCK the agent's thinking into REMEMBERING to do it professionally (a chain = a reusable process-priming block, e.g. HOWTOCODE:[DRY,YAGNI,read-whole-file], CLAUDE-CODE-ONLY:[no flight/waypoint/starsystem], PERSONA-TAG:[tag=persona's own name, never literal <GUY>]); you ALWAYS include the relevant chain when you need that thing. (4) prompts are COMPOSED BY REFERENCING PATHS of stored prompts: 'READ [these files] to assemble background context, THEN [the input request].' NOT bespoke prose; only combinations of blocks/prompts made before. WHY MY BUILDS FAILED: I sent ad-hoc prose + did not gate the code, so the builder was never primed (no CLAUDE-CODE-ONLY chain -> flight/waypoint leaked; no PERSONA-TAG chain + I echoed <GUY> -> literal <GUY> x7). FIXED THIS TURN: (a) corrected how-to-make-a-persona-prompt.md (<GUY> is a PLACEHOLDER; tag = persona's own name) + deployed to the plugin-cache runtime copy; (b) gated 9b2ca42 = REJECTED; (c) re-dispatched #48 in the 3-section form (persona + chains-by-path + exact gated input request) requiring zero <GUY> + zero non-CC vocab, verified by grep I re-run before merge. GOING FORWARD: maintain a library of chain/persona prompt files; every dispatch = persona + chains(by path) + exact input request; I read+gate every artifact.  tags:[Persona_Chains_Input, Gate_Code_Professional, Compose_By_Path]
