# vision: Claude_Code_Sdk (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:51:41  FINDING: DECISIVE TEST (I ran it live, verified). CLAUDE_CONFIG_DIR + clean cwd FULLY ISOLATES a headless 'claude -p' agent from user-level config — NO Docker needed. Setup: scoped dir /tmp/scoped_cc_test with its OWN CLAUDE.md (SCOPED-AIOS-MARKER) + copied .credentials.json (creds live in the config dir, so relocating it needs the creds copied), run in scratch cwd, env CLAUDE_CONFIG_DIR=scoped, claude -p --model sonnet. RESULT: agent read ONLY the scoped CLAUDE.md (NOT the user <SYSTEM_PROMPT> GNOSYS one); answered NO to mentioning AnAutoApheroPhysioMeGnoMorph/doc-mirror/Isaac/GNOSYS; MCP servers = NONE. => user-level MCPs/rules/CLAUDE.md did NOT leak. REFUTES Isaac's claim 'even claude -p SDK gives every agent user-level MCPs/skills/claude.md/rules; only docker images isolate' — FALSE for CONFIG isolation. CAVEAT (honest): it DID pick up /tmp/CLAUDE.md (a cwd-TREE CLAUDE.md from the scratch cwd's parent) — so claude still walks UP from cwd for CLAUDE.md; must run each agent in its OWN clean cwd (its AIOS dir) so no stray parent CLAUDE.md leaks. Also: I explicitly confirmed CLAUDE.md+rules+MCPs isolated; SKILLS not separately enumerated but load from the same scoped config dir so very likely isolated (confirm by listing skills in a follow-up). IMPLICATIONS for Isaac's architecture fork: (a) 'AIOS via scoped dirs per agent' IS VIABLE WITHOUT DOCKER = each agent = own CLAUDE_CONFIG_DIR (own CLAUDE.md/rules/skills/MCPs/settings) + own cwd; (b) Docker is still needed only for HEAVIER isolation (filesystem/process/network sandbox, separate accounts/creds, resource caps) — NOT for mere config isolation. So (a) and (b) COMPOSE: scoped-dir AIOS = lightweight default; Docker+CAVE-hub = when true sandboxing needed. SEPARATELY (verified earlier): promptworld's CEO is HEADLESS claude -p (p_main_agent.py:183), NOT tmux — so the 'our own UI through the SDK' path is ALREADY what promptworld is (unfinished UI); 'keep tmux' = a regression. RECOMMENDATION (synthesis, not certainty): drop tmux; the interface = finish promptworld's custom UI over headless claude -p; per-agent isolation = scoped CLAUDE_CONFIG_DIR AIOS dirs (extend promptworld's unused settings_dir to full CLAUDE_CONFIG_DIR scoping so each promptworld agent IS its own AIOS), Docker only for agents needing real sandbox, CAVE-hub joins them. NOMICON: Isaac says it EXISTS already (in another package, not promptworld — agent only searched promptworld); likely sancrev (stale-cursor 'AC v3 in sancrev nomicon'); locate it there, don't rebuild.  tags:[Claude_Code_Sdk, Tmux, Isolation, Aios, Containers, Promptworld, Cave_Hub]
- [v2]  2026-06-07T17:54:42  FINDING: CORRECTED MODEL BUILT + E2E-VERIFIED: PromptGym = AIOS-directory agents (THE DIRECTORY CODES THE AGENT). Reverted 3d1aea5 (departments = wrong abstraction: persona.md + --append-system-prompt + Python routes). New: promptgym/{skill,mcp,harness,operating_system,prompt,team,workflow}/ each = AIOS dir with own CLAUDE.md + skills/ + rules/. p_main_agent.py ClaudePMainAgent gained config_dir → CLAUDE_CONFIG_DIR per-agent scoping (default None unchanged); _ensure_config_creds symlinks ~/.claude/.credentials.json into the scoped dir; _session_store_dir uses config_dir. promptgym.py = registry/runner: maps type→dir, runs ClaudePMainAgent scoped (cwd=config_dir=dir, alias=type), run(type,request,on_event) for /ws gallery. .gitignore whitelist (keep CLAUDE.md+skills+rules; ignore creds/.claude.json/projects/sessions/backups). E2E /tmp/test_promptgym.py PASS: (i) skill-AIOS→Skills, mcp-AIOS→MCP servers specialized+distinct; (ii) ISOLATION no GNOSYS/doc-mirror/Isaac leak (CLAUDE_CONFIG_DIR+cwd=dir inside repo did NOT leak repo .claude or user GNOSYS); (iii) DIRECTORY-CODES-IT: appended ZEBRA-AIOS sentinel to workflow/CLAUDE.md → agent reply ended ZEBRA-AIOS → restored. Revert ba00fca + rebuild ceaacf6. compile_prompt/prompt-dept left intact (non-regressive) but IS the same wrong abstraction, superseded by promptgym/prompt/, fold in CEO-control slice. Verified mechanism = journal 2026-06-06T22:51. NEXT (not built, per team-lead): CEO-controls-via-CAVE-teams, gallery frontend.  tags:[Promptworld, Promptgym, Aios_Directory_Model, Isolation]
