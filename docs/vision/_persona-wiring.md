# vision: promptworld-departments (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-02T01:38:33  FINDING: PERSONA-NOT-WIRED gap (agent-flagged, I confirmed via behavior): the engineer-CEO's /api/chat convo does NOT inject agents/engineer-ceo.md — the CEO replies from generic knowledge (it listed 'skills, agents, commands, hooks, rules' not its persona's 'MCPs/Skills/Harnesses/Operating-Systems/Prompts/Teams/Workflows'). It WORKS for the prompt-engineer ONLY because compile_prompt PREPENDS the persona into the instruction each turn. So: personas attach to claude -p convos by being sent as content; the CEO convo currently has no persona injected (bare claude). FIX (v2.1): inject agents/engineer-ceo.md as the CEO convo's first/system message (and confirm the persona's component-kind list matches the canonical 7). Also OPEN: re-prepending the persona every compile turn is wasteful — a convo should get its persona ONCE at session creation (a ClaudePMainAgent 'system/seed' on create_session). Deferred.  tags:[promptworld-departments, ceo-interface, persona-wiring]
