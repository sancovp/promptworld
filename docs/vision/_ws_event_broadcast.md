# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T15:36:10  FINDING: BUILT + E2E-VERIFIED frontend slice 1: live agent event-stream over /ws. The /ws route already registered clients + cleaned up, and broadcast() existed but was BROKEN for the live path (asyncio.create_task from the sync /api/chat worker thread → no running loop). FIX: capture the server event loop in the /ws endpoint (self._loop); broadcast()/new broadcast_event(alias,ev)/_broadcast_threadsafe schedule sends via run_coroutine_threadsafe (guarded per client, drop dead sockets); chat() runs the turn via send_and_wait(on_event=broadcast_event), wrapping each event as {alias,event} and returning the final text (POST /api/chat contract intact). E2E (/tmp/test_promptworld_ws.py): booted real server (python -m server --dir <pwdir> --port 3996), ws client got 14 live events tagged alias=ceo incl assistant/tool_use + user/tool_result + result; POST reply 'Done — output: WSTEST'. Commit 167daf4. This is SLICE 1 (server broadcast); SLICE 2 = the assistant-ui React app (NOT built yet, per team-lead).  tags:[Promptworld, Server, Ws_Event_Broadcast, Frontend]
- [v2]  2026-06-07T16:15:15  FINDING: BUILT + E2E-VERIFIED frontend slice 2: assistant-ui React app over /ws, served by the server. New Vite+React+TS app at application/promptworld/frontend/. Grounded the assistant-ui API from the installed package .d.ts (NOT docs, which 404'd): useExternalStoreRuntime from @assistant-ui/react@0.11.58; ThreadMessageLike's tool-call part carries result INLINE → mapper merges user/tool_result onto the matching tool-call by toolCallId. Files: eventMapping.ts (PURE mapper, unit-tested), runtime.tsx (useExternalStoreRuntime + ws to /ws filter alias=ceo + onNew→POST /api/chat), components/Thread.tsx (headless primitives Thread/Message/Composer + tool fallback renderer; no tailwind). server/promptworld_server.py serves dist at / + mounts /assets, falls back to legacy index.html. dist force-committed (repo ignores dist/) for image-readiness; rebuild: cd frontend && npm ci && npm run build. E2E (env -u ANTHROPIC_API_KEY): (a) vite build→dist OK; (b) curl / = built SPA refs bundle, /assets/*.js 200; (c) vitest 5/5; (d) headless chromium loaded /, sent echo WSTEST, DOM showed streamed reply + tool-call card (🛠 Bash) + WSTEST. Screenshot /tmp/promptworld_frontend.png. Commit adc7586. VISUAL polish = UNKNOWN pending Isaac eyeball. GOTCHA: ComposerPrimitive.Input renders a TEXTAREA not input (bit the first browser test).  tags:[Promptworld, Frontend, Assistant_Ui_App, Ws_Event_Broadcast]
