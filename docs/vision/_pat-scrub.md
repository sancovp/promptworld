# vision: goal (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T19:19:30  INTENT: GOAL ACCEPTED (Isaac /goal, GO): scrub the leaked PAT -> heaven-bml-v2 publish core (parameterized rsync + gh repo create) -> per-repo README/CHANGELOG auto-regenerate workflow (heaven-bml generator pattern, extended) -> package as the scalable-publishing plugin -> apply to PromptWorld + the tier-3 index repo. Grounded in: scalable-publishing-system rule (3-tier topology, ~/.claude/rules/), /tmp/heaven_bml_map.md (the rsync clone-and-push publish + the ecosystem.json README generator), the drop-metastack decision (2026-06-08T19:17:32, lightweight: requests-only in shipped CI). STARTING step 1 = scrub the inline PAT in /tmp/heaven-bml-public/.git/config (set-url to the clean https URL) + tell Isaac to ROTATE the token on GitHub (I cannot rotate his PAT). Commander discipline: I author steering (plugin skills/rules/workflow templates/prompts) + verify; dispatch target code; the outward-irreversible steps (gh repo create public sancovp/promptworld + the tier-3 index repo) are authorized by the goal but I'll do them deliberately + verify.  tags:[goal, scalable-publishing, heaven-bml-v2, pat-scrub, publish-workflow, readme-changelog, plugin, promptworld, tier3-index]
- [v2]  2026-06-08T19:21:19  FINDING: PUB-1 DONE + VERIFIED BY ME (step 1 of the goal). Scrubbed the inline fine-grained PAT (github_pat_1...) from /tmp/heaven-bml-public/.git/config: git remote set-url origin + --push origin -> clean https://github.com/sancovp/heaven-bml.git. VERIFIED via raw config read: url + pushurl both clean, grep -c github_pat_ = 0, grep -cE inline-cred = 0. (An intermediate masked origin	https://@github.com/sancovp/sanctuary-revolution-alpha.git (fetch)
origin	https://@github.com/sancovp/sanctuary-revolution-alpha.git (push) showed a stale '@' = display artifact; the raw .git/config is the ground truth = clean.) REMAINING SECURITY ACTION = ISAAC's (I cannot): the token is still VALID on GitHub (only removed from this on-disk config) -> ROTATE/REVOKE the github_pat_1... fine-grained PAT in GitHub settings. NEXT: PUB-2 heaven-bml-v2 publish core — first READ heaven-bml's real public-release.yml + generate_ecosystem_readme.py IN FULL (only saw structure so far) to ground the parameterized v2.  tags:[pat-scrub, security, heaven-bml, rotate-token, PUB-1]
