# vision: promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T18:36:12  FINDING: MILESTONE 6 VERIFIED BY ME E2E (the meta-compiler skill). BUILT: skills/compile-a-world/{SKILL.md,resources/patterns.md} = the *World meta-compiler skill (unified model: a *World = a sim{team-leader,specialists,domain,driver}; the 4 named patterns w/ verbatim defs + anatomy; procedure reuse-or-combine -> fold up the nomicon ladder; honest IS-vs-VISION incl 'no general generator yet'). CARRIAGE: extended server/promptworld_agent.py _provision_ceo_aios with a (2) block symlinking <promptworld_dir>/skills/* into the CEO's project .claude/skills/ (also carries ingest-into-nomicon-app, a prior latent gap). PERSONA: engineer-ceo.md — corrected the stale 'you do NOT compile other Worlds / not a compiler' line to what IS (resemblance/way-of-building; you CAN compile via patterns) + added a compile-a-world rule (use the skill; you are the meta that treats all *Worlds as patterns; mark IS vs VISION). VERIFIED THROUGH THE LIVE USER SURFACE (docker cp the 3 paths -> restart -> health 200): (1) STRUCTURAL carriage — _provision created .claude/skills/compile-a-world -> /app/promptworld/skills/compile-a-world, resolves (SKILL.md present); (2) USAGE — live CEO turn 'compile a NEW World: RecipeWorld' -> the CEO (plan mode) produced a plan that EXPLICITLY cites 'the four patterns documented in the compile-a-world skill', chose JobWorld as base + GameWorld/HealthWorld OVERLAYS (=COMBINED patterns, the skill's objective), laid the on-disk World-module shape (chef-ceo + promptgym/<role>/ AIOS dirs), folded up the nomicon ladder (make-ai-operating-system->skill2framework->fold-into-nomicon->ship-a-plugin/ingest-into-nomicon-app), carried the 'resemblance not a compiler / not push-button' caveat VERBATIM, marked runnable-container+Gym as VISION. = the skill is carried AND used correctly. DEV-FLOW (coherence set, noted not separate-skill: a single knowledge skill): edit skills/compile-a-world/{SKILL.md,resources/}; carriage is automatic via _provision_ceo_aios skill-symlink loop; if RENAMED, update the engineer-ceo.md rule that names it; verify via a live CEO 'compile a World' turn. NEXT: commit+push, then compact.  tags:[promptworld, compile-a-world, meta-compiler, world-patterns, engineer-ceo, _provision_ceo_aios]
