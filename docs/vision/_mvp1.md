# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T19:32:20  DECISION: REHYDRATION ANCHOR (corrected current state — supersedes the wrong-MCP detour; a fresh builder should act on THIS). === ARCHITECTURE (locked) === promptworld = CAVE + claude -p, NO SDNA (CAVE Agent.run duck-types its runtime agent.py:374; SDNA is goal-level-only). Each PromptGym agent = an AIOS DIRECTORY (the directory codes the agent: its own CLAUDE.md + .claude/skills + .claude/rules), run as claude -p SCOPED to its dir via CLAUDE_CONFIG_DIR + cwd=the dir (VERIFIED isolates from user/global config; creds symlinked in). === OPTION B LOCKED (this turn) === each specialist agent has its OWN specialized prompt (its dir's CLAUDE.md), does NOT inherit the CEO prompt (A rejected — would make specialists omniscient, breaking D2/D3: CEO knows ALL components, specialists know ONLY their one). Each agent has its OWN doc-mirror (provisioned INTO its scoped dir) + access to a shared UNIVERSAL WORKER subagent (a CEO-mirror, callable by every agent, the uniform capability without prompt-inheritance). CEO = its own omniscient AIOS (doc-mirror scoped to be ABOUT all components — not a 'global' doc-mirror, just the CEO's). Agents don't know each other except via the CAVE hub when teamed. === MVP SEQUENCE (Isaac, verbatim intent) === MVP1 = (a) ALL 7 PromptGym agents WORKING INDIVIDUALLY = fill out the 7 AIOS dirs for real (real CLAUDE.md + skills/rules per component type + doc-mirror provisioned + the universal-worker subagent), each chattable standalone; (b) the NOMICON SYSTEM done; (c) the CEO dispatches them via CLAUDE-CODE-NATIVE teams (Task/TeamCreate — NO custom mechanism, NO MCP, NO event visibility — invisible is ACCEPTED in MVP1). MVP2 = a promptworld-team (or skill that starts the team) -> CAVE AUTO-INJECTS to all agents as they have messages (cave_teams: CEO=TeamLeader, agents=Conversations whose .send runs OUR promptgym.run for AIOS-scoping+gallery streaming; dispatch = leader writes JSONL to agent msg file, Harness delivers — NOT an MCP) + the interface (gallery) shows them all receiving/responding. Isaac: 'I dont know if MVP2 even works right' = uncertain. === WHAT IS BUILT (verified by me) === branch promptworld-event-stream: ClaudePMainAgent streaming+compact (278811f); CEO persona (93c3dd7); /ws broadcast (167daf46); assistant-ui chat (adc7586); Monaco file-explorer path-jailed (32c7b088); PromptGym 7 AIOS-dir STUBS (basic CLAUDE.md) + config_dir scoping (ceaacf6); CAVE Heart/automation/cron inherited (PromptWorldAgent(CAVEAgent) super().__init__). === WRONG / TO REVERT === the GYM MCP (d6451ee: server/gym_mcp.py + /api/gym route + CEO --mcp-config) = WRONG mechanism (CEO must control via CAVE injection / native teams, NOT an MCP) AND wrong phase (MVP2-premature). REVERT it. === PREMATURE MVP2 (leave on branch, do NOT build on yet) === the GALLERY (ece99b2) — it's MVP2 visibility, built early; fine but depends on the CAVE-auto-inject which is MVP2. === NEXT (MVP1) === FIRST fill out the 7 agent AIOS dirs for real (B: own specialized prompt + doc-mirror + universal worker) so each WORKS INDIVIDUALLY; AND build the NOMICON system; CEO uses claude-code-native teams (invisible). === DEPLOYMENT (later, #1 packaging) === container with one doc-mirror provisioned per agent dir + the CEO's; neo4j (CartON) via docker-compose if absent; --strict-mcp-config to make any MCP deterministic; clean container ~/.claude (fixes the CEO-MCP startup-race = 'problem 2'). === MINIMAX === claude -p + ANTHROPIC_BASE_URL env reaches minimax (no SDNA); IS-by-doc, not tested this session.  tags:[Promptworld, Promptgym, Mvp1, Mvp2, Aios, Cave, Claude_Code_Sdk, Nomicon, Doc_Mirror, Universal_Worker, Rehydration, Revert_Mcp]
- [v2]  2026-06-07T19:35:12  OPEN: REHYDRATION VERIFIED: fresh builder pw-builder reconstructed the FULL corrected model (CAVE+claude-p no SDNA, AIOS-dir option-B agents, MVP1-3parts vs MVP2, revert-d6451ee-gym-MCP, all 6 built commits, next step) from the promptworld vision docs ALONE — zero drift vs the anchor. The durable layer is sufficient = green light to compact. BUT it honestly surfaced 3 DESIGN GAPS (spec does not exist yet — NOT rehydration failures) that BLOCK full MVP1 and must be resolved before/early in the build: (1) NOMICON SCOPE INSIDE PROMPTWORLD = UNDEFINED — needs Isaac. WHAT nomicon is = grounded (doc-mirror-system framework-of-frameworks: nomicon-atomize + skill2framework/fold-into-nomicon, dogfooded E2E); the LADDER reading (promptworld=bottom rungs/components -> fold up -> author nomicon=top) is my proposed interpretation Isaac has NOT formally confirmed; and 'the nomicon system done' as MVP1 part (b) has NO concrete promptworld-internal spec. Building it = guessing. (2) UNIVERSAL-WORKER subagent mechanism = named+locked but HOW it's built is unspecified (a claude-code subagent definition in each scoped dir? a shared callable? a CEO-mirror dir?). Needs a spec. (3) DOC-MIRROR-PROVISIONING per agent dir = mechanism unspecified (cp the doc-mirror skill into each scoped .claude/skills? run the doc-mirror install-wizard per dir? symlink?). Needs a spec. ALSO: minimax-via-ANTHROPIC_BASE_URL = IS-by-doc, untested. Minor: vision topic-headers _aios.md='Claude_Code_Sdk' / _cave.md='Jobworld' (journal auto-naming artifact; content correct). POST-COMPACT FIRST MOVE on the build thread = resolve these 3 gaps (nomicon first = Isaac's call) before filling the 7 AIOS dirs.  tags:[Promptworld, Mvp1, Nomicon, Universal_Worker, Doc_Mirror, Open, Rehydration_Verified]
- [v3]  2026-06-07T19:43:09  DECISION: COMPACT ANCHOR (2026-06-07 PM). READ FIRST POST-COMPACT: the MVP1 REHYDRATION ANCHOR (journal 2026-06-07T19:32:20, in vision _mvp1.md) = the authoritative model (CAVE+claude-p no SDNA; PromptGym = AIOS dirs, option B; MVP1 3-parts vs MVP2; revert d6451ee gym-MCP; 6 built commits on branch promptworld-event-stream). REHYDRATION PROVEN: fresh builder pw-builder reconstructed it ALL from the vision docs alone, zero drift. === NOMICON RESOLVED (Isaac confirmed 'yes') — NOT undefined, my mislabel: nomicon = doc-mirror-system's framework-of-frameworks (nomicon-atomize + skill2framework/fold-into-nomicon, ALREADY EXISTS, dogfooded E2E). promptworld's agents FOLD their made components UP into the author nomicon via that pipeline (comes WITH doc-mirror-provisioning). The APP RUNG (Isaac's geometry): ingest a claude-code dir -> fold into a {name}-nomicon -> agentify into a SIDECAR APP (the hot/self-forking/GOD-FROM-HEAVEN pattern) = a plugin skill (port-over/ingest) + packaging (#1). MVP1 nomicon slice = agents-can-fold-up (rides on doc-mirror-provisioning); the full ingest->app rung = plugin skill + packaging (MVP1/deployment boundary). So NO Isaac-blocker remains — it's a DECOMPOSITION job. === IN-FLIGHT: pw-builder (teammate, survives this compact) is doing the UNBLOCKED slice: surgically revert the gym-MCP mechanism (server/gym_mcp.py + CEO --mcp-config + the dispatch instruction; KEEP /api/gym route + run_gym if the gallery/standalone-chat needs them) + write the 7 REAL specialized CLAUDE.mds (option B) + the directory-codes-it E2E. It reports to my inbox; its artifacts must stand alone. === POST-COMPACT FIRST MOVES (build thread): (1) VERIFY pw-builder's slice myself (re-run directory-codes-it E2E + read the diff + confirm the revert didn't regress chat/files/ws/gallery/SPA); (2) SPEC the 2 mechanisms: universal-worker subagent (likely a claude-code subagent def dropped into each scoped .claude/agents/ = a CEO-mirror callable by every agent) + doc-mirror-provisioning per dir (cp/symlink the doc-mirror skill+CLIs into each scoped .claude, or run the install-wizard per dir); (3) DECOMPOSE the nomicon geometry into MVP1 tasks; (4) finish MVP1(a) = provision doc-mirror + universal-worker into the 7 dirs; then MVP1(b) nomicon-fold + (c) CEO-via-native-teams. === DEFERRED: MVP2 (cave_teams CAVE-injection: CEO=TeamLeader, agents=Conversations whose .send=promptgym.run, dispatch=leader writes JSONL->Harness delivers, NOT an MCP; + the gallery viz ece99b2 already built-but-premature); packaging #1 (Dockerfile/compose/install-wizard + plugin skills, clean container ~/.claude + --strict-mcp-config fixes the CEO-MCP race=problem2, neo4j via docker-compose); minimax-via-env test. === ME: commander; pw-builder = the promptworld builder (owns task #27). All verified-by-me. Continuable.  tags:[Promptworld, Compact, Nomicon, Mvp1, Rehydration, Pw_Builder]
- [v4]  2026-06-07T20:06:31  FINDING: VERIFIED BY ME (re-ran /tmp/test_pw_mvp1_slice.py, env -u ANTHROPIC_API_KEY) pw-builder commit e99379e = OVERALL PASS. PART A directory-codes-it: skill/mcp/team/workflow agents each answered AS their specialized type, distinct + isolated (no gnosys/doc-mirror/isaac leak); sentinel ZEBRA-AIOS-7731 appended to workflow CLAUDE.md made the agent emit it then restored => the DIRECTORY codes the agent (option B confirmed: each its OWN prompt). PART B no-regression after the surgical gym-MCP revert: server boots, health type=promptworld main_agent=ClaudePMainAgent, GET slash served, files-tree 200, POST chat -> CEOPING (CEO persona-only, no mcp_config), ws streamed ceo events, POST gym skill 200 (standalone gallery path intact). gym_mcp.py deleted. MVP1a PARTIAL: 7 real specialized prompts exist but doc-mirror-provisioning + universal-worker per dir NOT yet built. PROCESS NOTE: I wrongly rehydrated by file-reading+E2E before querying DMN; corrected — DMN locate+thread is the ONLY rehydration path.  tags:[Promptworld, Mvp1, Pw_Builder, Aios, Revert_Mcp]
- [v5]  2026-06-07T20:24:46  FINDING: VERIFIED BY ME (re-ran /tmp/test_pw_provision.py, env -u ANTHROPIC_API_KEY) pw-builder commit 64302c3 = OVERALL PASS. Provisioning lands 12 doc-mirror skills into each scoped config skills/ + the universal-worker into agents/; a real scoped claude -p agent resolved its doc-mirror CLI (command -v journal docmirror-cursor -> /home/GOD/.local/bin + DMRESOLVED) AND invoked subagent_type universal-worker -> WORKER-PONG-42. Read the diff: PromptGym._provision_aios symlinks plugin skills + universal-worker at agent() init, idempotent best-effort; _doc_mirror_plugin_root resolves ~/.docmirror_plugin_root else monorepo path RELATIVE to file (portable); symlinks gitignored (env-specific runtime), canonical universal-worker.md versioned. So MVP1(a) = COMPLETE + verified: 7 specialized AIOS dirs (e99379e) + per-dir doc-mirror + universal-worker (64302c3), each chattable standalone. OPEN (builder flagged, Isaac's call): the CEO is NOT scoped (inherits user ~/.claude = omniscient per D2, has host-level doc-mirror); the anchor wanted CEO with its OWN doc-mirror scoped to be ABOUT all components. Builder left CEO as-is (scoping it would pollute user config + contradict D2 omniscience). In a real promptworld container the CEO gets the container doc-mirror = fine; in THIS dev env the CEO shares the dev doc-mirror = a leak, but packaging-deferred. NOT a blocker for MVP1(a).  tags:[Promptworld, Mvp1, Pw_Builder, Doc_Mirror, Universal_Worker]
- [v6]  2026-06-07T20:30:15  FINDING: LOAD-BEARING FINDING (pw-builder investigated, I confirmed from the Agent-tool schema myself): claude-code-NATIVE Task/TeamCreate CANNOT run a teammate scoped to a different CLAUDE_CONFIG_DIR. The Agent tool params = subagent_type/name/team_name/model/isolation(worktree)/run_in_background — NO config_dir/cwd; native subagent .md frontmatter = name/description/tools/model — NO cwd. A Task subagent runs AS the parent CEO's claude (CEO config/tools), not a separate claude -p. The ONLY way to run an AIOS-dir specialist = PromptGym.run(type) = a claude -p with config_dir->CLAUDE_CONFIG_DIR (a separate scoped process). So the LOCKED AIOS-dir model (directory-codes-the-agent) and pure-native-subagents (persona-.md-in-parent) are MUTUALLY EXCLUSIVE: the CEO can only reach scoped specialists through a BRIDGE. 3 options: A native Task + 7 thin proxy subagents in promptworld_dir/.claude/agents/{type}.md that shell to the already-kept POST /api/gym/{type} (-> run_gym -> scoped gym.run) = keeps AIOS-dir scoping + per-agent doc-mirror/universal-worker + free gallery; the only non-pure-native bit = the proxy's one shell line (RECOMMENDED). B pure-native persona subagents (copy CLAUDE.md into CEO config) = ABANDONS the AIOS-dir model = exactly the departments-as-personas Isaac REVERTED (ba00fca) = NO. C native TeamCreate = same scoping limit + overkill; real scoped version = cave_teams Conversation.send->promptgym.run = MVP2. FORK FOR ISAAC: A's thin Task->/api/gym bridge vs his 'no custom mechanism' intent — A is the only option preserving the locked model; B/C only look more-native by discarding scoping. Builder HOLDING pending Isaac's pick. Cites: promptgym.py agent/run, p_main_agent.py:275-279, promptworld_server.py:206-217, Agent-tool schema.  tags:[Promptworld, Mvp1, Ceo_Control, Native_Teams, Aios, Open]
- [v7]  2026-06-07T20:43:05  DECISION: ISAAC DEFINITIVE V1 (verbatim intent) — SUPERSEDES + reconciles the MVP1c bridge fork (20:30:15), which was WRONG: I made a later-stage requirement (specialist CLAUDE_CONFIG_DIR scoping) true too early by forcing the CEO's dispatch to satisfy it -> invented a bridge (same error as the reverted MCP). KILL that. The reconciled model = TWO HALVES that DO NOT COMMUNICATE except sharing the same backend: (HALF 1 = CEO) the CEO is a claude -p that HAS EVERY SINGLE GLOBAL USER SKILL + the skill version of every agent's package = it can do literally everything the whole program does; it has a .claude with EVERY skill and EVERY specialist registered in .claude/agents/; its SYSTEM PROMPT tells it that when it runs any agent it MUST tell that agent (in the prompt) to only work in the appropriate dir. The CEO calling specialists is NATIVE claude-code subagent calling — NO bridge, NO MCP, NO config_dir gymnastics; the dir constraint is just a SENTENCE in the subagent prompt. (HALF 2 = PromptGym) each specialist is ALSO set up as an individual scoped AIOS-dir agent in the PromptGym half (the 7 dirs, already done) where the USER talks to it directly. The two halves never make agents talk to each other; the only CEO->specialist path is INCIDENTAL (claude-code native subagents). USER can talk to either the CEO or any specialist. V1 REDUCTION (Isaac verbatim): 'SET UP THE DIFFERENT AGENTS. GIVE EVERY SINGLE SKILL TO THE CEO. THERE. V1.' BUILD = register the 7 specialists as subagent defs in the CEO's agents dir (each body = its identity + 'only work in promptgym/{type}/') + give the CEO every skill (it is UNSCOPED so already inherits user ~/.claude global skills; provision the component skills too) + the CEO system-prompt dir-instruction. NO bridge/MCP. Then run the agent, check implemented, loop until done.  tags:[Promptworld, Mvp1, V1, Ceo, Two_Halves, Reconcile]
