# vision: Promptgym (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T17:44:42  DECISION: CORRECTED MODEL (Isaac, confirmed 1=yes 2=yes) — I built the departments WRONG TWICE (persona .md + --append-system-prompt + Python routes = 'coding the departments'); REVERT commit 3d1aea5. THE RIGHT MODEL: PROMPTGYM is a SEPARATE MODULE inside the PromptWorld app = the gym OF AGENTS (standalone agents). Each PromptGym agent = an AIOS *DIRECTORY* for ONE component type (a claude-code project dir with its OWN CLAUDE.md + skills + rules). **THE DIRECTORY IS WHAT CODES THE AGENT** — NOT a persona string. Each runs as a claude -p agent SCOPED to its dir (CLAUDE_CONFIG_DIR + cwd = the dir, creds shared/symlinked in) so the agent IS that AIOS, isolated from user-level + other agents (uses the verified CLAUDE_CONFIG_DIR isolation, journal 2026-06-06T22:51). PROMPTWORLD = the CEO agent + a team; the CEO CONTROLS the SAME PromptGym agents (calls them as agents told to work EXCLUSIVELY in their respective directories). SAME agents, SAME way (the directory). Standalone-chat (in PromptGym) vs under-CEO-team (in PromptWorld) SHOULD NOT MATTER except team-runs aren't visible in standalone chats — WHICH IS WHY: CEO uses CAVE TEAMS (NOT claude-code-native teams), because CAVE teams UNIFY the frontend events -> a GALLERY view of every agent's chat window, all streaming live via the unified /ws (slice 1), potentially talking simultaneously. (claude-code-native teams = invisible sub-agents = rejected.) WHY the SDK alone is 'useless' (Isaac): claude code SEPARATES the SDK (headless agent) from the agent-framework features (teams/inboxes/gallery) -> the SDK gives an agent + nothing else -> you MUST wrap it in an agent framework (CAVE) to get coordination+visibility. CAVE-teams-on-claude-p IS that fix. BUILD SEQUENCE: (FIX slice now) revert 3d1aea5 + build the AIOS-directory model: promptgym/{type}/ AIOS dirs (CLAUDE.md per type) + extend ClaudePMainAgent with config_dir->CLAUDE_CONFIG_DIR per-agent scoping + a PromptGym registry/runner; E2E prove an agent runs scoped to its dir, is isolated, and THE DIRECTORY CODES IT (edit the dir's CLAUDE.md -> behavior changes). (NEXT) CEO-controls-via-CAVE-teams. (NEXT) gallery frontend. The pre-existing 'prompt' department/compile_prompt = assess against this model (likely also the wrong abstraction).  tags:[Promptgym, Promptworld, Aios, Directory, Claude_Config_Dir, Cave_Teams, Gallery, Ceo, Correction]
- [v2]  2026-06-07T18:15:49  FINDING: INVESTIGATION (read-to-closure, my crux re-verified): how CEO controls PromptGym agents + gallery. CRITICAL: 'CAVE teams' Isaac means = a SEPARATE pip package application/cave-teams/cave_teams/ (claude-p NATIVE: primitives.py:91 run_opus, leader.py:129 TeamLeader, harness.py file-JSONL routing) — DISTINCT from the 'cave' package CAVEAgent registry (cave_agent.py:370-398 factory types=code/chat/claw/service/remote, NO claude-p branch; minimax/heaven only via spawn_remote mixins/remote_agent.py:51 gated config.sdna_enabled). VERIFIED MYSELF: cave_teams primitives.py:85-87 _run_claude_p = subprocess.run(capture_output=True) BUFFERED + :106 --system-prompt REPLACE; grep config_dir/CLAUDE_CONFIG_DIR/on_event in primitives.py = ZERO. So (A) use-cave_teams-as-is DOES NOT FIT our PromptGym agents: (1) cave_teams has NO config_dir AIOS-dir scoping (promptgym.py:67 needs config_dir=d so the DIRECTORY codes the agent); (2) --system-prompt REPLACE vs our --append-system-prompt+--resume; (3) buffered run, NO on_event → cannot live-stream the gallery (our p_main_agent _run_turn Popen line-by-line on_event → broadcast_event → /ws). RECOMMENDATION: (C) NOW = a PromptWorld route/MCP tool → pw.gym.run(type,req,on_event=lambda ev: broadcast_event(type,ev)) (mirrors compile_prompt route promptworld_server.py:198; reuses built+streaming machinery; tradeoff: no shared task-list/SOP). (B) WHEN true cave_teams coordination wanted = register PromptGym agents as duck-typed cave_teams Conversation adapters whose .send (contract harness.py:96 → conversation.py:45 .send(content)->result) calls promptgym.run(on_event=broadcast_event) instead of buffered run_opus → gets cave_teams Harness bus + live gallery; cost = adapter + watcher. Findings /tmp/cave_teams_for_claudep_ceo_control.md. UNKNOWN: no runtime E2E run of promptgym.run/cave_teams leader; frontend /ws consumer (gallery per-window) not read; whether a gym MCP tool already exists (no /api/gym route today). NOTE bug: cave/core/remote_agent.py:39 uses Path/os never imported → NameError on lazy spawn_remote import. NO BUILD this pass (per team-lead; Isaac picks A/B/C).  tags:[Promptworld, Ceo_Control, Cave_Teams, Promptgym, Gallery]
- [v3]  2026-06-07T18:18:46  FINDING: VERIFIED BY ME (read application/cave-teams/cave_teams/primitives.py): cave_teams is a SEPARATE pip package that is CLAUDE-P NATIVE, NOT SDNA. run_opus = claude -p Opus (primitives.py:91,104); continue_opus = resume; run_minimax = MiniMax via Anthropic-compatible API (primitives.py:170-189); line 176 literally 'No SDNA, no chains'. Uses --system-prompt REPLACE (primitives.py:106) + BUFFERED subprocess.run capture_output (primitives.py:87, no live on_event) + ZERO config_dir. CONTRADICTS Isaac's premise 'cave_teams adapts agents via SDNA' — it does NOT. CONSEQUENCE: promptworld does NOT need to be SDNA-backed to use cave_teams (cave_teams = claude -p; our PromptGym agents = claude -p; same family). So the earlier 'back promptworld on SDNA' urgency is MOOT for the cave_teams integration; SDNA-unification is a SEPARATE/LATER concern (the provider-agnostic worlds), not a blocker here. INTEGRATION = option B: a duck-typed cave_teams Conversation adapter (the only contract Harness.deliver needs = Conversation.send(content)->AgentResult, conversation.py:45 / harness.py:87) whose .send calls OUR promptgym.run(type, on_event=broadcast_event) instead of cave_teams buffered run_opus -> gets cave_teams COORDINATION (Harness/leader) + our AIOS-config_dir + live /ws gallery. Option C = a simpler PromptWorld dispatch route, faster interim. NEITHER needs SDNA. THE MARKDOWN-VS-CODE RECONCILIATION (resolves Isaac's confusion): claude code is config/markdown-based (CLAUDE.md + skills), our libs (SDNA/skillmanager/paia-builder) are CODE-based -> the AIOS-DIRECTORY model we just built IS the bridge: the agent is DEFINED by markdown (its directory/CLAUDE.md, claude-code-native) and RUN by a thin runner (claude -p); the runner does NOT handle markdown, claude code does. 'Give them the skillmanager skillset in their persona' -> in the directory model the skills are IN the dir's .claude/skills, claude code auto-loads them, NO runtime-equip needed; the DIRECTORY is the persona+skillset. So for promptworld you do NOT need to unify skillmanager/paia-builder/CAVE into SDNA — the directory model + cave_teams ARE the claude-code-native stack. SDNA-unification stays the separate later question for the agnostic worlds.  tags:[Promptworld, Cave_Teams, Sdna, Claude_Code_Sdk, Aios, Directory, Skillmanager, Markdown_Vs_Code]
- [v4]  2026-06-07T18:22:23  FINDING: CONSOLIDATED + RE-VERIFIED (my own reads on the 2 cruxes). (1) CAVE does NOT require SDNA: cave/core/agent.py:355-382 Agent.run delegates to self._runtime duck-type (.run(content) else .handle_message else callable else no-op); _create_agent_from_entry sets NO runtime (cave_agent.py:362-426) until set_runtime; only SDNA-hard path = spawn_remote gated config.sdna_enabled. Raw ClaudePMainAgent (send_and_wait, no .run) doesn't satisfy as-is but a ~3-line adapter (.run(content)=pmain.send_and_wait(content,on_event=broadcast_event)) does. Note agent.py:388-393 routes the agent's RETURN to central_channel deliverable channels = a DIFFERENT path than /ws, so the live gallery rides our on_event bridge, not CAVE's channel. (2) SDNA = GOAL-LEVEL ONLY (verified runner.py:88 resolve_goal both claude + heaven_runner.py:201; no per-turn-message-into-resumed-session API; ClaudeCodeSession=tmux). Our ClaudePMainAgent IS the missing headless chat-level claude runner. Parity: SDNA-claude has streaming(on_message)/cwd+env(config_dir expressible)/resume+session_id; LACKS append-system-prompt(replace default config.py:233), /compact-as-turn (CLI-only), named ConvoRegistry (only /tmp/current_claude_session_id global). VERDICT (to Isaac): (C) ship now = /api/gym route → promptgym.run(type,req,on_event=broadcast_event) [lightest, reuses built streaming]; (B) cheap when cave_teams coordination wanted = duck-type adapter wrapping ClaudePMainAgent onto CAVE agent.run + keep on_event→/ws gallery; (D) only as deliberate investment = add ~150-250 LOC chat-level claude runner to SDNA (keep CLI core to retain /compact) + ConvoRegistry port. (A) cave_teams-as-is still doesn't fit. Findings /tmp/cave_teams_for_claudep_ceo_control.md (323 lines). UNKNOWN: whether claude_agent_sdk query() forwards options.env(CLAUDE_CONFIG_DIR) to the child (SDK transport not read); heaven hermes_step internals; SDK stream cadence. NO BUILD — Isaac picks.  tags:[Promptworld, Ceo_Control, Cave_Teams, Sdna, Verdict]
- [v5]  2026-06-07T18:25:31  DECISION: ISAAC PICKED option A: keep it separated, ship faster = promptworld runs on CAVE + claude -p directly, NO SDNA; SDNA-unification deferred as its own project. TWO DECIDING FACTS VERIFIED BY ME (read the code, not the report): (1) CAVE Agent.run DUCK-TYPES its runtime (cave/core/agent.py:374-381: .run/.handle_message/callable; result routes to central_channel deliverable channels, a DIFFERENT path than our /ws so the gallery rides OUR on_event bridge) -> NO SDNA required; a ~3-line adapter (runtime.run(content)=pmain.send_and_wait(content,on_event=broadcast_event)) registers ClaudePMainAgent as a CAVE agent runtime. (2) SDNA is GOAL-LEVEL ONLY (runner.py:88 + heaven_runner.py:201 both prompt=config.resolve_goal(merged_inputs) — a templated goal, NOT a per-turn message; no chat-turn API; the only 'chat' = ClaudeCodeSession=TMUX). So OUR ClaudePMainAgent IS the missing headless chat-level claude runner; backing promptworld on SDNA = option D (ADD ~150-250 LOC chat_runner.py + ConvoRegistry port + append/creds wiring to SDNA) = a deliberate 'SDNA owns the claude chat backend' investment, SKIPPED for now. MINIMAX: claude -p + ANTHROPIC_BASE_URL env swap reaches minimax (claude-code-native, no SDNA) — mechanism IS-by-doc, NOT tested this session (deferred). PLAN: (C) NOW = CEO controls PromptGym via a dispatch tool (a PromptWorld MCP tool the CEO's claude -p calls, or a route+skill) -> promptgym.run(type, on_event=broadcast_event(type)) -> gallery via /ws + result to CEO; (B) CHEAP LATER = the ~3-line cave_teams/CAVE duck-type adapter for real shared-task-list coordination + simultaneous agents; (D) only as the conscious SDNA-owns-claude-chat decision. Dispatching (C). builder parity gaps for the future-D swap (per his read): SDNA on_message yields SDK message OBJECTS not CLI NDJSON (our eventMapping consumes NDJSON); SDNA system_prompt=REPLACE not --append; SDNA CANNOT do /compact-as-a-turn (CLI-only); no ConvoRegistry. All preservable behind the promptgym.run seam if/when we do D.  tags:[Promptworld, Cave, Claude_Code_Sdk, Sdna, Cave_Teams, Ceo, Gallery, Ship]
