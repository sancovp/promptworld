# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T18:18:40  FINDING: INDEPENDENT CRUX VERIFICATION (my own reads, base/sdna/sdna): SDNA headless claude = GOAL-LEVEL. runner.py:47-88 agent_step builds prompt = config.resolve_goal(merged_inputs) (:88); config.py:106 goal:str templated; NO per-turn user-message param (variable_inputs only interpolate the goal). BUT chat primitives EXIST: config.py:150-152 resume/fork_session/continue_conversation; to_sdk_options :224-245 sets resume/fork/continue + cwd(:244) + env(:245 → CLAUDE_CONFIG_DIR expressible); agent_step has on_message(:50,103) + returns session_id(:110-113). So a CHAT loop is CONSTRUCTIBLE on agent_step (goal:=this-turn message, resume:=sid) but there is NO first-class chat API. The only named 'chat' surface = ClaudeCodeSession (claude_code_commands.py:34) is TMUX (send-keys/capture-pane, compact=/compact via tmux), NOT headless. PARITY flags: (a) SDNA on_message yields claude_agent_sdk MESSAGE OBJECTS, not the CLI raw stream-json NDJSON our gallery eventMapping.ts consumes → swap needs event-shape re-map; (b) compact()-as-/compact-turn is a CLI feature; SDK replaces with PreCompact hook (not a user turn); (c) config_dir AIOS scoping IS expressible via to_sdk_options env+cwd; (d) resume/session parity OK. LEANS verdict (b): our ClaudePMainAgent (CLI subprocess, send/Enter/--resume/on_event-NDJSON/config_dir) IS the chat-level claude runner SDNA lacks headless; adding it to SDNA = wrap ClaudePMainAgent OR build a thin chat loop over agent_step (goal:=msg+resume) + swap on_event shape. Background agent completing full read (heaven_runner chat?, CAVE call-on-agent site, DUOAgent) → consolidated report next. NO BUILD.  tags:[Promptworld, Ceo_Control, Sdna, Chat_Vs_Goal, Parity]
