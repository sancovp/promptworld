# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T20:47:42  INTENT: Building HALF 1 (CEO) of Isaac's V1 two-halves model (journaled 20:43:05). HALF 2 (7 scoped AIOS dirs) is done. HALF 1: CEO stays UNSCOPED (already inherits every global user skill = 'every skill'); at PromptWorldAgent init, RUNTIME-GENERATE 7 native claude-code subagent defs into promptworld_dir/.claude/agents/{type}-specialist.md (DRY — body derived from the committed promptgym/{type}/CLAUDE.md + a HARD 'work ONLY in promptgym/{type}/' constraint) so the CEO reaches specialists by plain native Task calling (NO bridge/MCP/config_dir); ALSO symlink the doc-mirror plugin (component-build) skills into promptworld_dir/.claude/skills/; and rewrite agents/engineer-ceo.md (the CEO system prompt) to state it HAS every skill + a subagent per component type and MUST tell each specialist (in the prompt) to work only in its dir. .claude/ is fully gitignored (root :32) so the subagent defs + skills are runtime/env-specific = regenerated per environment; tracked changes = only engineer-ceo.md + promptworld_agent.py. E2E: boot server, prove (a) CEO names a build skill and (b) CEO Task-delegates to skill-specialist which writes a token file INTO promptgym/skill/ (proves project-subagent-discovery + native dispatch + dir-constraint + real result).  tags:[Promptworld, V1, Ceo, Two_Halves, Subagents]
- [v2]  2026-06-07T20:51:05  FINDING: V1 HALF 1 (CEO) BUILT + E2E-VERIFIED BY ME (/tmp/test_pw_v1_ceo.py OVERALL PASS). PromptWorldAgent._provision_ceo_aios() runs at init: (1) RUNTIME-GENERATES 7 native subagent defs promptworld_dir/.claude/agents/{type}-specialist.md, each body DERIVED from the committed promptgym/{type}/CLAUDE.md + a hard 'work ONLY in promptgym/{type}/' constraint (DRY: one source of truth = the specialist CLAUDE.md); (2) symlinks the 12 doc-mirror plugin (component-build) skills into promptworld_dir/.claude/skills/. CEO stays UNSCOPED so it also inherits every global user skill. engineer-ceo.md (the CEO system prompt) rewritten: states it HAS every skill + a subagent per component type (skill/mcp/harness/operating_system/prompt/team/workflow) and MUST tell each specialist in the prompt to work only in its dir (removed the stale 'departments DEFERRED' line). E2E PROVED: server boots; all 7 subagent defs + doc-mirror-boot present in CEO .claude; (a) CEO named build skills 'make-skill ... make-mcp ...'; (b) CEO used native Task -> skill-specialist subagent -> wrote promptgym/skill/PROBE_CEOSUB.txt='CEOSUB-OK' touching nothing else (= project-subagent-discovery WORKS [resolves the flagged unknown] + native dispatch + dir-constraint-as-a-sentence + real result). NO bridge, NO MCP, NO config_dir for the CEO. promptworld_dir/.claude is fully gitignored (root :32) so subagent defs (derived) + skill symlinks (env-specific) regenerate per environment; tracked changes = ONLY agents/engineer-ceo.md + server/promptworld_agent.py. V1 COMPLETE: HALF 2 (7 scoped AIOS dirs, user-direct) done+verified earlier and structurally untouched by this change; HALF 1 done now. The two halves share only the backend (promptgym/{type}/ dirs); they never make agents talk to each other.  tags:[Promptworld, V1, Ceo, Subagents, Verified]
