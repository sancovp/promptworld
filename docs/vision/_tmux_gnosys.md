# vision: Tmux_Gnosys (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T02:25:58  VISION: REMEMBER FOR LATER (Isaac, 2026-06-08; task #42): the agent-substrate SPLIT. PromptWorld WORKER agents (CEO + 7 specialists) = SDNA HermesConfig + Heaven + MiniMax (cheap, isolated by construction). BUT talking to GNOSYS ITSELF (the full gnosys agent/persona) is back to needing TMUX-driven interactive Claude Code — because that is the un-throttled Max-SUBSCRIPTION lane (claude dash p / Agent SDK on subscription is being throttled to near-uselessness; a 1M-context conversation is ~800 dollars of API value). So: GNOSYS = tmux interactive Claude Code (subscription); PromptWorld workers = SDNA-Heaven-MiniMax. FUTURE NEED (not now): a BETTER tmux integration for GNOSYS inside SANCREV OPERA (the desktop-app shell). Existing primitives to build on: sdna/claude_code_commands.py (ClaudeCodeSession, find_claude_session, list_tmux_sessions) + sdna/selfbot.py (get_tmux_session). Deferred until SANCREV OPERA / the GNOSYS-chat surface is active work.  tags:[Tmux_Gnosys, Sancrev_Opera, Subscription_Lane]
