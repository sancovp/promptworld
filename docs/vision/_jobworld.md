# vision: Jobworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:53:49  FINDING: JOBWORLD vs PROMPTWORLD (subagent /tmp/jobworld_tmux_or_headless.md, file:line proofs). KIKI'S JOBWORLD = TMUX: CEO runs as an interactive 'claude' Claude Code TUI inside a tmux pane, puppeteered via tmux send-keys (input) + capture-pane (output). Proof: cave/core/agent.py:936-944 spawn = tmux send-keys 'claude'; per-turn input = server/jobworld_agent.py:104-108 + jobworld_server.py:65 send_keys. Headless claude -p used ONLY for isolated sub-workers (ClaudeCodeSubAgent), NEVER the CEO. Code /home/GOD/twi-jobworld/; container 'jw-kiki' (image jobworld-cave:latest, port 8502) EXISTS but EXITED(255) ~4 days ago. Has its OWN UI = ink-ceo dashboard (Express+ws, ink-ceo/server.js, port 8501/8502) -> /api/* -> python API :3847 -> tmux send_keys. CONTAINERIZED with per-instance own .claude/ + own CLAUDE.md (Kiki = bespoke 'Kiki Voss Agency CEO' persona); host ~/.claude never mounted; sync_globals.sh symlinks shared rules/skills. SO: jobworld = the OLD tmux-wrapped approach ('build UI around tmux like we did before' = THIS); promptworld = the NEWER headless-claude-p + custom-UI approach = the post-tmux evolution. Isaac's 'didnt we already do this for kiki's jw' -> YES but WITH tmux; promptworld redoes it WITHOUT tmux. REUSE NOT REBUILD: jobworld already has (a) a real dashboard UI (ink-ceo) to port into promptworld's minimal UI, (b) the CEO+departments structure, (c) per-instance scoped .claude/CLAUDE.md = the scoped-AIOS idea achieved via CONTAINER — which my CLAUDE_CONFIG_DIR test shows promptworld can achieve WITHOUT a container (lighter). NET DECISION CONFIRMED: forward = promptworld (headless + custom UI, drop tmux); port jobworld's ink-ceo UI patterns + per-instance scoping; scoped-CLAUDE_CONFIG_DIR AIOS as default isolation, Docker only for true sandbox, CAVE-hub to join. UNKNOWN: in-container claude auth source for jobworld's TUI (no creds copy in Dockerfile/entrypoint; likely supplied at docker run).  tags:[Jobworld, Kiki, Tmux, Promptworld, Headless, Cave, Ink_Ceo, Scoped_Aios]
- [v2]  2026-06-08T18:03:01  DECISION: ISAAC DECISION (verbatim, 2026-06-08, GREENLIGHTS the compiler direction now that (1)(2)(3-self-inspection) are done — REVERSES the earlier 'do not build a World-compiler' under the new condition): 'well if this is all done then the correct move *IS* to build the world compiler... but the problem is that this PromptWorld is not set up the same way Jobworld and Healthworld are. Why dont you go look at the differences. I think if nomicon is folding, then we probably want to add the *app compiler* (if we didnt) and then add Worlds as types of apps. We have gameworlds, jobworlds, healthworlds, promptworlds... each one of these could be a pattern to reference. we already have skills for most of these that can be looked at... idk...'. PARSE: (a) FIRST CONCRETE ASK = investigate the DIFFERENCES between promptworld and jobworld/healthworld (promptworld 'is not set up the same way') — go look. (b) ARCHITECTURE HYPOTHESIS (his, tentative 'idk'): nomicon-folding => add an *app compiler* (CHECK: do we already have it = the app-rung ingest-into-nomicon-app?) => then add Worlds as TYPES OF APPS (a World is a KIND of app; gameworld/jobworld/healthworld/promptworld = reference patterns). (c) RESOURCES: existing *World impls + skills 'for most of these' exist to reference. NOT a locked spec — a direction + an investigation mandate. NEXT (commander, do NOT build yet): dispatch a checking-team to MAP the 4 *World structures + their skills + what the app-compiler is/should be + Worlds-as-app-types, write findings to durable docs, then I synthesize the design + precise OPEN forks before any build (braid: map the order before acting).  tags:[promptworld, jobworld, healthworld, gameworld, app-compiler, world-as-app-type, ingest-into-nomicon-app]
- [v3]  2026-06-08T18:09:19  FINDING: SYNTHESIS of the 4-agent *World investigation (maps: /tmp/pw_worldmaps/{jobworld,healthworld,gameworld_and_skills,app_compiler_and_diff}.md; crux SKILL.md re-read BY ME). THE DIFFERENCES (Isaac's 'promptworld not set up the same way' — answered): JobWorld+HealthWorld = CAVE-fork web apps where the DOMAIN IS HARDCODED INTO A PYTHON SUBCLASS ({World}Agent(CAVEAgent)+{World}HTTPServer(CAVEHTTPServer); DOMAIN_ENUM/DEPARTMENT_SPECS baked in; HealthWorld is literally a renamed JobWorld fork w/ a latent  bug), instances spun by instantiate.sh copying a template + sed {{PORT}} only (domain NOT parameterized); agents run via tmux/claude-p .md Conversations on a file-based cave-teams Harness (CEO+departments ORG pattern). GameWorld = a DIFFERENT team pattern (peer economic MARKET, shared game.json) + the ONLY real spec->world generator (gameworld-generator) but it parameterizes MECHANICS not DOMAIN. PromptWorld = ALSO a CAVE fork (forked from HealthWorld) BUT the CLEAN pattern: claude_agent_sdk per-DIRECTORY AIOS agents (directory-codes-the-agent via CLAUDE.md) + carries the NOMICON LADDER + re-skinnable world-context.md — domain is clean AIOS-dir config, NOT a tangled subclass. So 'set up differently' = set up CLEANER/nomicon-native. THE APP-COMPILER REALITY: only nomicon-atomize is real code; the ladder is prompt-skills; ingest-into-nomicon-app (the app-rung) ORCHESTRATES ingest->convert->agentify but STAGE-3 app-boot is UNPROVEN (skill VERIFY says 'Mark IS/UNKNOWN honestly'; E2E only on a TOY single-AIOS) AND it INGESTS-an-existing-dir, it does NOT SYNTHESIZE-a-World-from-a-domain-spec. THE DESIGN PICTURE (mine, for Isaac to confirm — VISION): app = World-module + (opt) Gym-module OVER the nomicon ladder (already PromptWorld's self-def). A WORLD = a parameterization of an app: {team-pattern in {org,market}, persona-set, department/domain spec, dashboard}. So JobWorld=app(org, CEO+biz-depts, hiring); HealthWorld=app(org, OSMOSYS+14-body-systems, health); GameWorld=app(market, GM+players, game); PromptWorld=app(org, Engineer-CEO+7-crafts, claude-code-components). The 4 existing *Worlds = REFERENCE PATTERNS/templates to fold into the compiler. PIVOTAL FORK FOR ISAAC (do NOT auto-decide): does the World/app compiler EMIT clean PromptWorld-style apps (AIOS-dirs+nomicon+re-skinnable world-context; old job/health re-expressed in the clean form) [my recommend], OR emit CAVE-subclass-style Worlds (generalize instantiate.sh+gameworld-generator into a domain-parameterized generator that bakes a {World}Agent subclass)? + is the compiler the INGEST path (ingest-into-nomicon-app, exists, prove stage-3) or a new SYNTHESIZE-from-spec path (gameworld-generator is the seed) or BOTH?  tags:[promptworld, jobworld, healthworld, gameworld, app-compiler, world-as-app-type, ingest-into-nomicon-app, gameworld-generator]
- [v4]  2026-06-08T18:20:37  DECISION: ISAAC VERBATIM PATTERN DEFINITIONS (2026-06-08, the load-bearing model — NO WORK authorized, design only: 'Im not comfortable with you doing any work right now'). EACH *World = a SIM = {team-leader role + specialist set + domain + what-drives-it}: (1) JOBWORLD = 'a sim where the team leader is a CEO of departments, receiving heartbeats and events from automations about real world.' (2) GAMEWORLD = 'a sim where you can set up an economic/trading game around something the agents can craft and the team leader is a game master.' (3) HEALTHWORLD = 'like a jobworld but for your body, like osmosis jones, so the agents work together to make holistic analyses from your records and stats.' (4) PROMPTWORLD = 'a reactor where the CEO (we didnt rename this but we should) ie WrightMaster or whatever able to call all the individual *Wrights (or whatever) to work on a nomicon together. PromptWorld can even make all these things as AIOS + Teams variants for itself and use them, or compose them into apps and publish them, etc.' KEY REFRAME (corrects my 'promptworld is fundamentally different/cleaner'): Isaac — 'PromptWorld works differently because its team system isnt hooked up, but i dont understand why that means it is overall different? if we are adding the nomicon compiler, we want to have the CEO run the team... lol. We'll get into this and work it out.' => the difference is NOT architectural — PromptWorld's TEAM ISN'T HOOKED UP YET; with the nomicon compiler the CEO (WrightMaster) SHOULD run the team like the other Worlds' team-leaders. THE 'WORLD COMPILER' = NOT a code generator (yet): Isaac — 'its going to be a skill that just gives the architecture patterns and examples. we already have generator for gameworld, we dont have a generator for *worlds though do we? and we already have generators for new jobworld and new gameworld right? we can just give those as examples, they use those skills, learn them, but know that the real objective is to combine all the knowledge and come up with something new.' OR they use one of those patterns directly. SO the deliverable (LATER, not now) = a SKILL teaching the 4 *World architecture patterns + pointing at the existing per-World generators (gameworld-generator real; jobworld/healthworld instantiate.sh) as worked examples, whose objective = reuse a pattern OR combine-the-knowledge to synthesize something new. TODOs noted when we 'get into it': hook up PromptWorld's CEO-runs-team; rename CEO -> WrightMaster; there is NO general *World generator yet (per-World only).  tags:[promptworld, jobworld, healthworld, gameworld, world-as-sim, team-leader, wrightmaster, world-compiler-skill]
