# vision: promptworld-separation (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-01T14:59:23  DECISION: KEEP PROMPTWORLD SEPARATE (Isaac, fear-abating decision). Isaac (verbatim): 'im worried we are overconnecting everything. So the key here to abate my fear is to just keep promptworld separate in terms of integrations until i figure it out. and while it is separate, it will still look like everything is copying each other and making individual systems.' OPERATIONAL CONSEQUENCE: build PromptWorld STANDALONE — do NOT wire it to jobworld/healthworld/starsystem CEOs, do NOT force PromptWorld to BE the *World compiler, do NOT cross-integrate the compiler-agents yet. Each package keeping its own agent-compiler skills (apparent duplication) is ACCEPTABLE and expected for now. The unification question is DEFERRED to Isaac, not auto-decided.  tags:[promptworld-separation, world-pattern, compiler-agents]
