# vision: publishing (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T18:52:33  DECISION: ISAAC PROPOSAL (verbatim sense, 2026-06-08 — refines the scalable-publishing-system to a CI/CD mechanism): 'we could easily have a CICD setup where the monorepo has EVERYTHING ALL IN ONE for us, and then whenever stuff individually updates it pushes to the public if we want to. we had that set up before with heaven-bml! ... some kind of heaven-bml v2 setup: you push to monorepo which is PRIVATE with a specific TAG, that publishes the specific subdir on its specific public repo by commit-pushing it there via a workflow. then a SKILL for setting up these yamls inside the monorepo so github can read them.' MY ASSESSMENT (answer to his 'how is that?'): YES — this is the automated form of the rule's publish step (tag-triggered GitHub Actions workflow per subdir = mechanized subtree-split/mirror; better than manual for scalable+always-follow). TWO THINGS IT MUST ACCOUNT FOR: (1) MONOREPO VISIBILITY FLIP — his model makes the monorepo PRIVATE (it is currently PUBLIC per canonical-source-dirs: sancovp/sanctuary-revolution-alpha public/GPBL). Cleaner (everything-in-one stays private; only curated subdirs go public) but it IS a change with license/visibility implications = his decision to confirm. (2) THE DEP WRINKLE PERSISTS — CI publishing the promptworld subdir doesn't make it BUILD standalone; the published repo still needs cave/sdna/doc-mirror-plugin. CLEAN FIT: treat those as their OWN published subdirs (each its own public repo via the SAME workflow) and promptworld's build pulls them from their public repos (or pip). So the CI model + deps-as-published-units solves sync AND the standalone-build coherently. THE SKILL = a dev-flow that scaffolds the per-subdir publish workflow YAML + the push-secret/PAT wiring in the monorepo (fits every-build-ends-in-a-dev-flow-skill). GROUND FIRST: heaven-bml's actual setup exists on disk (/home/GOD/heaven_bml_sqlite/ per canonical-source-dirs) — read its real workflow shape before building 'heaven-bml v2', don't reinvent. NEXT (pending his monorepo-visibility confirm): update ~/.claude/rules/scalable-publishing-system.md to make tag-triggered CI the canonical publish mechanism; then this supersedes M8's manual subtree-split.  tags:[publishing, cicd, github-actions, monorepo, heaven-bml, subtree-split, per-subdir-publish, workflow-scaffold-skill]
- [v2]  2026-06-08T19:00:17  FINDING: HEAVEN-BML GROUNDED (subagent map /tmp/heaven_bml_map.md; load-bearing claims RE-VERIFIED BY ME). LOCATION: /home/GOD/tmp/heaven-bml-{private,public}/ (NOT heaven_bml_sqlite = newer SQLite descendant). WHAT: a GitHub-labels Build-Measure-Learn kanban + installable GitHub Actions that mirror private->public + publish PyPI. THE PRIVATE->PUBLIC MECHANISM (verified: .github/workflows/{public-release,simple-public-release,private-release,sync-sender}.yml all exist): tag 'public-*' on the private repo -> Action clones the PUBLIC repo -> rsync --exclude=ideas/ -> commit -> push main (a clean SNAPSHOT, NOT subtree/filter-repo), authed by PUBLIC_REPO_TOKEN secret; then a derived 'v*' tag -> pypi-publish.yml (build+twine, PYPI_API_TOKEN). ONE private -> ONE HARDCODED public repo today. SECURITY (VERIFIED BY ME, real): /home/GOD/tmp/heaven-bml-public/.git/config remote URL has an EMBEDDED CREDENTIAL (https://<cred>@github.com/sancovp/heaven-bml.git) — plaintext token in a git config on the dev box (not publicly exposed, but leaks if imaged/shared). FLAGGED to Isaac: rotate the PAT + scrub the local remote to a credential helper (did NOT touch it — his cred/possibly in-use). REUSABLE FOR V2 (the 3-tier topology): the rsync clone-and-push primitive, PARAMETERIZED (target repo + subdir) + LOOPED one-to-many (N tagged subdirs -> N public repos) + gh repo create each missing public repo — fixes the 3 gaps (hardcoded target, inline-PAT, no repo-creation). NEXT (when Isaac says build): heaven-bml-v2 = the tier1->tier2 publish workflow + the scaffold skill, based on this proven shape.  tags:[heaven-bml, cicd, public-release, rsync, PUBLIC_REPO_TOKEN, security-pat, github-actions]
- [v3]  2026-06-08T19:15:34  FINDING: FOUND the README templates Isaac meant (IN HEAVEN-BML, not metastack — I'd looked in the wrong place; saw them now). heaven-bml-private/ has a CONFIG-DRIVEN AUTO-UPDATING ECOSYSTEM-README system = the proven TIER-3 index mechanism: (1) ecosystem_templates/{ecosystem.json,personal.json} = the config 'template' — {name, description, sections{Core Framework/Specialized Libraries/Community Tools -> repos[], auto_discover, show_stats}, template:'ecosystem', auto_update:true, badges{license,version,stars,last_updated}, links{Documentation,Discord,Website}}; (2) generate_ecosystem_readme.py = renders README from ecosystem.json (get_repo_data via GitHub API -> generate_badges [shields.io stars/version/last_updated] -> generate_repo_section [### [name](url) + desc + ⭐stars + Latest version] -> generate_readme [header + landing image + Quick Links + per-section repo cards]); (3) github_workflows/ecosystem-readme-update.yml = on push to ecosystem.json OR daily cron '0 6 * * *' OR workflow_dispatch -> pip install requests -> python generate_ecosystem_readme.py ecosystem.json -> if README.md changed, commit+push. SO the 'README always stays updated' Isaac wants ALREADY EXISTS as a shape = the tier-3 index/marketplace README auto-generator. CLARIFIES the metastack remark: heaven-bml's renderer is a HAND-ROLLED python f-string generator (NOT metastack); 'supe it up with metastack' = (a) replace the ad-hoc renderer with TYPED metastack templates (render+VALIDATE), (b) EXTEND from the ecosystem-index README down to PER-REPO README + CHANGELOG (heaven-bml only does the index README today; per-repo readme/changelog is the new part). REUSE FOR THE PLUGIN: ecosystem.json (tier-3 catalog/index config) + the daily-cron regenerate-and-commit workflow = proven; add metastack typing + per-repo README/CHANGELOG render+validate + the tier1->tier2 publish (rsync clone-and-push from /tmp/heaven_bml_map.md).  tags:[heaven-bml, ecosystem-readme, ecosystem.json, generate_ecosystem_readme, readme-template, changelog, metastack, tier3-index, auto-update]
- [v4]  2026-06-08T19:21:19  FINDING: PUB-1 DONE + VERIFIED BY ME (step 1 of the goal). Scrubbed the inline fine-grained PAT (github_pat_1...) from /tmp/heaven-bml-public/.git/config: git remote set-url origin + --push origin -> clean https://github.com/sancovp/heaven-bml.git. VERIFIED via raw config read: url + pushurl both clean, grep -c github_pat_ = 0, grep -cE inline-cred = 0. (An intermediate masked origin	https://@github.com/sancovp/sanctuary-revolution-alpha.git (fetch)
origin	https://@github.com/sancovp/sanctuary-revolution-alpha.git (push) showed a stale '@' = display artifact; the raw .git/config is the ground truth = clean.) REMAINING SECURITY ACTION = ISAAC's (I cannot): the token is still VALID on GitHub (only removed from this on-disk config) -> ROTATE/REVOKE the github_pat_1... fine-grained PAT in GitHub settings. NEXT: PUB-2 heaven-bml-v2 publish core — first READ heaven-bml's real public-release.yml + generate_ecosystem_readme.py IN FULL (only saw structure so far) to ground the parameterized v2.  tags:[pat-scrub, security, heaven-bml, rotate-token, PUB-1]
- [v5]  2026-06-08T19:22:55  FINDING: PUB-2 GROUNDED (read heaven-bml public-release.yml in full). PROVEN PRIMITIVE: trigger on push tags public-star or star-public; job checkout, then git clone the public repo using secret PUBLIC_REPO_TOKEN, then rsync -av --exclude ideas/ --exclude .git/ dot into /tmp/public-repo/, then git add/commit (or echo no changes)/push origin main, then derive VERSION via sed public- to v-, tag and push (fires pypi-publish). ONE private to ONE HARDCODED public repo; inline token in the clone URL is OK in CI (ephemeral runner) — the leak was the persisted LOCAL clone (PUB-1, scrubbed). V2 PLAN (author next turn): (1) monorepo .github/workflows/publish-subdir.yml triggered by publish-UNIT-vX tags; (2) a PUBLISH MANIFEST mapping unit to subdir + public_repo (+ optional pypi flag); (3) per unit: gh repo create the public repo if missing, clone it, rsync only the SUBDIR, commit, push main, optional v-tag; (4) loop one-to-many; (5) PUBLIC_REPO_TOKEN as a monorepo Actions secret. TESTABLE CORE = extract per-unit publish as publish_unit.sh (args subdir + public_repo) so it runs+tests LOCALLY vs a throwaway public repo before wiring the trigger. PUB-3 rides the same workflow (regenerate README/CHANGELOG from config, include in the rsync'd tree). COMPACTING at this seam: PUB-1 done+verified, PUB-2 grounded, goal+tasks 35-39 set. NEXT TURN: author publish_unit.sh + manifest + publish-subdir.yml, test locally, then PUB-3/4/5.  tags:[PUB-2, heaven-bml, public-release, rsync, gh-repo-create, manifest, publish-core]
