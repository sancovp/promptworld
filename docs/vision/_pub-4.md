# vision: PUB-2 (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T19:26:41  FINDING: READ the 4 existing monorepo .github/workflows (Isaac: 'read those'). ALL FOUR are SANCREV OPERA ELECTRON-APP CICD, NOT subdir-publishing: build-shell.yml (builds Electron shell -> versioned .app/.dmg/.exe on electron/ or meta-shell.html changes), validate-inner-app.yml (tsc+eslint+vite build of src/ React source), package-inner-app.yml (tarball inner app SOURCE for the shell hot-mode, attach to release), install-sancrev-cicd.yml (THE INSTALLER: workflow_dispatch -> curl the 3 workflows from sancovp/sancrev-cicd raw -> generate .sancrev.yml config heredoc -> git commit+push -> step summary; 'delete this file after setup'). SO: the tier1->tier2 PUBLISH mechanism is NOT here (that's heaven-bml public-release.yml = rsync clone-and-push). TWO REUSABLE THINGS: (1) install-sancrev-cicd.yml = the PROVEN SCAFFOLD-INTO-A-REPO PATTERN for PUB-4's scalable-publishing scaffold skill (drop workflows + a config into a repo + commit); (2) there is a CANONICAL CICD REPO sancovp/sancrev-cicd (prior art: central repo holds workflows, installer pulls them). DESIGN CALL (mine, proceeding): the scalable-publishing PLUGIN SHIPS the workflow TEMPLATES in resources/ and its scaffold skill WRITES them into the target repo's .github/workflows/ (self-contained, no curl-from-repo dependency — more robust than install-sancrev-cicd's network fetch, which stays as the documented alternative). The publish workflow lives where it RUNS (.github/workflows/); the skill installs it there. NOTE: relates to task #9 'Set up SOMA CI/CD properly' + the sancrev-cicd repo — separate concern (electron build) from publishing, do not conflate. NEXT: build PUB-2 = publish_unit.sh (testable core) + publish/manifest + .github/workflows/publish-subdir.yml; test publish_unit.sh against a LOCAL bare repo first (no outward action), real gh-repo-create deferred to PUB-5.  tags:[PUB-2, PUB-4, sancrev-cicd, install-sancrev-cicd, scaffold-pattern, electron-opera, github-workflows]
