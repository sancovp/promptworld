# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:41:00  FINDING: PROMPTWORLD STATUS MAP (subagent /tmp/promptworld_status_map.md; load-bearing claims re-verified by me). RUNNABLE TODAY (3): (1) CEO chat over claude -p — POST /api/chat (server:94) -> chat() (promptworld_agent.py:141) -> ClaudePMainAgent; subscription-billed (ANTHROPIC_API_KEY scrub p_main_agent.py:182), --session-id/--resume, NO tmux = most-proven piece; (2) convo registry alias<->session_id (ConvoRegistry, .promptworld/convos.json has ceo+prompt-engineer); (3) ONE department 'prompt' (IMPLEMENTED_DEPARTMENTS={'prompt'} server/promptworld_agent.py:61) compiling real prompt .md artifacts to disk. On a CAVE-fork server (PromptWorldAgent(CAVEAgent)+PromptWorldHTTPServer(CAVEHTTPServer)). docs/mirror=0, docs/vision=17. DESIGNED-BUT-UNBUILT: 6 of 7 departments declared-only (skill/mcp/harness/operating_system/team/workflow, DEPARTMENT_TYPES :52-60, implemented:false, no route); persona-engineer = orphan 8th; UI index.html = 29-line single chat box NOT the designed dashboard (no convo-switch, no dept UI, /ws exists but UI never connects); CEO persona NOT injected (bare claude); paia hooks designed+transferable but settings_dir never passed; skills/start-promptworld = EMPTY dir (verified); no Dockerfile; product flywheel (assemble->project->publish->install, GEAR, *World compiler) unbuilt (Isaac: don't force promptworld to BE the *World compiler yet). NOMICON = NAME ONLY within promptworld: grep returns ONLY the 2026-06-06T22:23:33 decision line I wrote today from Isaac's sentence (+vision projections). NO design, NO code, NO scope. NUANCE: stale 2026-06-04 cursor referenced 'AC v3 in sancrev nomicon' -> nomicon likely lives in the SANCREV repo not promptworld; search sancrev to ground it before building. NET: promptworld has a clear finish-list (inject CEO persona / real dashboard UI / wire more departments / paia settings / start-skill+Dockerfile); nomicon is the one BLOCKED item — needs Isaac's definition or a sancrev search.  tags:[Promptworld, Nomicon, Departments, Status, Sancrev]
- [v2]  2026-06-07T16:18:44  DECISION: ISAAC CONVERGED on the editor + deployment-package design. EDITOR = B (Monaco panel) CONFIRMED — reasoning (Isaac): Monaco is AUTOMATABLE (full programmatic control of dropdowns/every aspect) vs VSCode/code-server (C) which are 'extremely complicated for almost no reason'. C (openvscode-server) would NOT be in the image — it'd only give LSP, and you never USE the IDE aspects because YOU never run anything (the AGENT does); not worth it. NOT ELECTRON (reconsidered): because this will likely go INTO SANCREV eventually -> a WEB SPA, not a desktop app (embeds in sancrev later). DEPLOYMENT PACKAGE (Isaac): the install-skill the agent runs on the user's MAIN Claude Code creates a TMP/host WORKSPACE dir, docker-compose mounts it into the image at build, the system serves the vite/react frontend + Monaco; user goes to localhost on HOST and sees the frontend + Monaco showing the host dir they volume-mounted into the container. Package = docker-compose (bind-mount host workspace + publish port) + the image (FastAPI backend + agents + serves the built SPA) + a skill (what to run / how to check). MY CLARIFICATION (keeps slice-2 architecture, no host/container split): the frontend stays SERVED FROM THE CONTAINER (slice 2 already does FastAPI-serves-built-SPA) — NO need to run the frontend on host. User opens localhost:PORT (published container port). Monaco edits via the backend FILE API operating on /workspace = the host-bind-mounted dir, so the user's host dir reflects every change AND they can open it in their own editor too; works LOCAL and REMOTE. vite is the BUILD tool; production serves the BUILT static SPA from the container (slice 2), NOT a runtime vite dev server (dev-only). FILE EXPLORER = NEXT SLICE (Monaco panel + backend file API tree/read/write via plain Python I/O — NO docker-exec/b64, the fix to the old bad version; + live file-change events over /ws to watch the agent edit). DIST DECISION (builder force-committed frontend/dist for immediate serve): RECOMMEND dropping dist/ from git + building it in the image layer (Dockerfile: npm ci && npm run build); defer to Isaac. Build prompt-skill additive-module-change-with-e2e GOLDENIZED (3/3 verified).  tags:[Promptworld, Monaco, File_Explorer, Vite, Docker_Compose, Volume_Mount, Sancrev, Electron]
