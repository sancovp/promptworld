# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T22:32:34  VISION: GOD-FROM-HEAVEN = the autonomous self-fork + hot-reload loop on top of PromptWorld V1 (Isaac, reflective — capture as VISION, not asserted). THE PATTERN (Isaac verbatim): an agent that dispatches to a remote container where there is a COPY OF ITSELF that can develop itself; all capabilities become prompts to that agent; it evolves itself by working on ANOTHER sandbox, then the main loads the updates and restarts it. Originally the pre-Claude-Code influencer product (the CICD self-improving-agent, 'this is how I use agents'); now Claude Code makes the subagent/dispatch substrate NATIVE, so it collapses to wiring the self-fork + reload around Claude Code. ISAAC'S MECHANISM FOR CLOSING IT (verbatim intent): 'to make this happen, we just fold the entire promptworld and gym system into the nomicon system.' = run the app-rung (ingest a claude-code dir -> {name}-nomicon -> agentify into a sidecar app) on PROMPTWORLD ITSELF: promptworld+gym become foldable frameworks held in a promptworld-nomicon (homoiconic self-representation), the app-rung re-agentifies that nomicon into a fresh promptworld app = the SELF-FORK; the main loads the new image + restarts = the RELOAD. PIECES THAT ALREADY MAP (IS, verified this session): CEO + 7 specialists + universal-worker = verbatim copies of the doc-mirror system prompt + role block = literal 'copies of itself'; container + AGENT create->cp creds->start deploy = the sandbox + reload (currently ME-driven, not autonomous); app-rung skill = the self-forking half (E2E-proven on a MINIMAL synthetic single-AIOS only, NOT on promptworld's own multi-AIOS self). PROOF OBLIGATION to call it real (VISION until then): (1) run the app-rung on promptworld's OWN dir and get a working promptworld-nomicon that re-agentifies a fresh, runnable promptworld app (untested at promptworld's scale); (2) close the loop AUTONOMOUSLY — a running promptworld forks the sandbox, develops the next version, the main reloads+restarts ITSELF, with NO human dispatching+redeploying. (2) is the actual product. Today the loop runs only because I (the commander) am the 'main that loads the updates and restarts it'. STATUS: the pieces exist; the closed autonomous fold-self-into-nomicon -> regenerate -> reload loop is UNPROVEN VISION.  tags:[Promptworld, Nomicon, God_From_Heaven, Self_Fork, App_Rung, Cicd, Influencer_Product]
- [v2]  2026-06-08T18:52:33  DECISION: ISAAC PROPOSAL (verbatim sense, 2026-06-08 — refines the scalable-publishing-system to a CI/CD mechanism): 'we could easily have a CICD setup where the monorepo has EVERYTHING ALL IN ONE for us, and then whenever stuff individually updates it pushes to the public if we want to. we had that set up before with heaven-bml! ... some kind of heaven-bml v2 setup: you push to monorepo which is PRIVATE with a specific TAG, that publishes the specific subdir on its specific public repo by commit-pushing it there via a workflow. then a SKILL for setting up these yamls inside the monorepo so github can read them.' MY ASSESSMENT (answer to his 'how is that?'): YES — this is the automated form of the rule's publish step (tag-triggered GitHub Actions workflow per subdir = mechanized subtree-split/mirror; better than manual for scalable+always-follow). TWO THINGS IT MUST ACCOUNT FOR: (1) MONOREPO VISIBILITY FLIP — his model makes the monorepo PRIVATE (it is currently PUBLIC per canonical-source-dirs: sancovp/sanctuary-revolution-alpha public/GPBL). Cleaner (everything-in-one stays private; only curated subdirs go public) but it IS a change with license/visibility implications = his decision to confirm. (2) THE DEP WRINKLE PERSISTS — CI publishing the promptworld subdir doesn't make it BUILD standalone; the published repo still needs cave/sdna/doc-mirror-plugin. CLEAN FIT: treat those as their OWN published subdirs (each its own public repo via the SAME workflow) and promptworld's build pulls them from their public repos (or pip). So the CI model + deps-as-published-units solves sync AND the standalone-build coherently. THE SKILL = a dev-flow that scaffolds the per-subdir publish workflow YAML + the push-secret/PAT wiring in the monorepo (fits every-build-ends-in-a-dev-flow-skill). GROUND FIRST: heaven-bml's actual setup exists on disk (/home/GOD/heaven_bml_sqlite/ per canonical-source-dirs) — read its real workflow shape before building 'heaven-bml v2', don't reinvent. NEXT (pending his monorepo-visibility confirm): update ~/.claude/rules/scalable-publishing-system.md to make tag-triggered CI the canonical publish mechanism; then this supersedes M8's manual subtree-split.  tags:[publishing, cicd, github-actions, monorepo, heaven-bml, subtree-split, per-subdir-publish, workflow-scaffold-skill]
- [v3]  2026-06-08T18:55:52  DECISION: ISAAC TOPOLOGY (verbatim, 2026-06-08, locks the 3-tier publish shape): 'Dev monorepo PRIVATE --> individual repos PUBLIC --> MONOREPO PUBLIC (index of everything/help everyone find everything across all projects ever), monorepo public has github markdown wiki or link to seed wiki or something'. THE 3 TIERS (named precisely): TIER 1 = the PRIVATE DEV MONOREPO (sancovp/sanctuary-revolution-alpha — VERIFIED private via gh, isPrivate:true; 'everything in one' = source of truth; my earlier 'public' was a stale canonical-source-dirs note, now corrected). TIER 2 = per-subdir PUBLIC repos (each shippable unit — promptworld, cave, sdna, doc-mirror-plugin, etc. — its own public repo; CI publishes a tagged subdir to it = heaven-bml-v2). TIER 3 = a SEPARATE PUBLIC INDEX/MARKETPLACE repo = 'help everyone find everything across all projects ever' = the ONE ship-a-plugin marketplace CATALOG (marketplace.json references all the tier-2 public repos) + a GitHub markdown WIKI (or a link to the SEED WIKI — Seed/SeedPublishing exists: Skill_Understand_Seed_Publishing / Tool_Show_Seed_Publishing_Interface). NET this RESOLVES the publishing system cleanly: private dev mono (no exposure) -> CI tag-push -> per-unit public repos -> public index/marketplace+wiki for discovery. The dep wrinkle dissolves: cave/sdna/doc-mirror-plugin are themselves tier-2 public units, and a published app's build pulls them from their public repos. NEXT: update ~/.claude/rules/scalable-publishing-system.md to encode this 3-tier topology as the canonical structure (CI tag-push = the mechanism); then M8 = apply it (promptworld + its dep-units as tier-2 publics, the index/marketplace repo as tier-3). 'idk something like that' = tentative; locked as the working topology, correct on sight.  tags:[publishing, topology, private-monorepo, public-repos, index-repo, marketplace, wiki, seed-wiki, cicd]
- [v4]  2026-06-08T19:00:17  FINDING: HEAVEN-BML GROUNDED (subagent map /tmp/heaven_bml_map.md; load-bearing claims RE-VERIFIED BY ME). LOCATION: /home/GOD/tmp/heaven-bml-{private,public}/ (NOT heaven_bml_sqlite = newer SQLite descendant). WHAT: a GitHub-labels Build-Measure-Learn kanban + installable GitHub Actions that mirror private->public + publish PyPI. THE PRIVATE->PUBLIC MECHANISM (verified: .github/workflows/{public-release,simple-public-release,private-release,sync-sender}.yml all exist): tag 'public-*' on the private repo -> Action clones the PUBLIC repo -> rsync --exclude=ideas/ -> commit -> push main (a clean SNAPSHOT, NOT subtree/filter-repo), authed by PUBLIC_REPO_TOKEN secret; then a derived 'v*' tag -> pypi-publish.yml (build+twine, PYPI_API_TOKEN). ONE private -> ONE HARDCODED public repo today. SECURITY (VERIFIED BY ME, real): /home/GOD/tmp/heaven-bml-public/.git/config remote URL has an EMBEDDED CREDENTIAL (https://<cred>@github.com/sancovp/heaven-bml.git) — plaintext token in a git config on the dev box (not publicly exposed, but leaks if imaged/shared). FLAGGED to Isaac: rotate the PAT + scrub the local remote to a credential helper (did NOT touch it — his cred/possibly in-use). REUSABLE FOR V2 (the 3-tier topology): the rsync clone-and-push primitive, PARAMETERIZED (target repo + subdir) + LOOPED one-to-many (N tagged subdirs -> N public repos) + gh repo create each missing public repo — fixes the 3 gaps (hardcoded target, inline-PAT, no repo-creation). NEXT (when Isaac says build): heaven-bml-v2 = the tier1->tier2 publish workflow + the scaffold skill, based on this proven shape.  tags:[heaven-bml, cicd, public-release, rsync, PUBLIC_REPO_TOKEN, security-pat, github-actions]
