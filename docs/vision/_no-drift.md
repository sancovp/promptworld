# vision: scalable-publishing (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T19:17:32  DECISION: REFINEMENT (Isaac: 'maybe it doesnt need metastack?' — my honest assessment: PROBABLY RIGHT, DROP metastack for v1). WHY: metastack (pydantic_stack_core + its REGISTRY service + HEAVEN_DATA_DIR) is a HEAVY dep to bake into EVERY published repo's CI; heaven-bml's generate_ecosystem_readme.py needs only . The publishing plugin's whole point is LIGHTWEIGHT + STANDALONE-SHIPPABLE, so dragging metastack into each repo's workflow is friction with little payoff (READMEs/changelogs are simple structured data). KEY REALIZATION: the 'README always stays updated' guarantee does NOT come from metastack — it comes from the AUTO-REGENERATE WORKFLOW (regenerate README from config on every publish + daily cron => physically cannot drift), which heaven-bml ALREADY does. metastack would only validate the config SHAPE, and a TINY INLINE PYDANTIC MODEL in the plugin does that more cheaply than the metastack registry. SO v1 = heaven-bml's ecosystem.json + python renderer pattern, EXTENDED to per-repo README + CHANGELOG, no-drift from the regenerate-workflow, optional small inline pydantic validation; NO metastack dep in shipped repos. metastack EARNS its place only if we later want README/doc templates SHARED ACROSS THE ECOSYSTEM via the registry (defer). SUPERSEDES the earlier 'supe it up with metastack' note (2026-06-08T19:06:49) — the freshness goal is met by the workflow, not metastack. (Prescan flagged Assumption_Metastack_Available — exactly the fragile assumption we avoid by not depending on it in shipped CI.)  tags:[scalable-publishing, metastack, readme, changelog, no-drift, auto-regenerate, pydantic, lightweight]
