# vision: Nomicon (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T20:17:12  DECISION: AUTHORED + committed the app-rung skill application/promptworld/skills/ingest-into-nomicon-app/SKILL.md (commit 071349e, branch promptworld-event-stream), marked UNVERIFIED. It orchestrates the 3 stages: INGEST (copy dir to workspace) + CONVERT (the dogfooded skill2framework UP pipeline 6 prompt-skills + nomicon-atomize -> fresh {name}-nomicon) + AGENTIFY (CEO carries the {name}-nomicon, ingested AIOS dirs = scoped agents). Container/packaging = deployment boundary #1 (handoff, not inlined). DISPATCHED background agent apprung-e2e@background-streams to PROVE it E2E on a minimal synthetic single-AIOS dir (acme): build src -> run the 3 stages -> verify {name}-nomicon valid + Frameworks table + atomized symlinks resolve + Stage-3 wiring. Its report is a CLAIM; I re-verify the artifacts myself before scoring. PARALLEL: pw-builder completing the system core (doc-mirror provisioning per dir + universal-worker subagent). Division of labor per Isaac: builder completes the promptworld system; nomicon (app-rung) is mine.  tags:[Nomicon, Promptworld, App_Rung, Dispatch]
