# vision: Live_On_Minimax (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T05:38:50  FINDING: PROMPTWORLD LIVE + VERIFIED ON MiniMax-M3 BY ME (the real user surface, literal outputs captured). Image fb102343558c (commits cc6ff22 MiniMax wiring + b5de0be claude-agent-sdk==0.1.19 + pinned mcp==1.12.4 to avoid the pydantic 2.10.6->2.13.4 cascade). Deployed: docker create --name promptworld-v1 -p 3858:3858 -e MINIMAX_API_KEY=... promptworld:latest -> cp creds -> start. Container HEALTHY (exit 0, NO OOM — the through-SDK turn that OOM'd on the dev box runs fine in the container's own memory). PROOF (both surfaces, real turns): (1) CEO via POST /api/chat -> 'AnAutoApheroPhysioMeGnoMorph (PromptWorld Engineer-CEO), running on MiniMax-M3' (the AnAuto name is BY DESIGN — CEO persona = verbatim gnosys seed + Engineer-CEO role addition, Isaac's requirement); (2) Skill specialist via POST /api/gym/skill -> 'I am the PromptWorld Skill Specialist... running on MiniMax-M3', dir=/app/promptworld/promptgym/skill (identifies as its ROLE = isolated; pw-builder4 separately proved 41/42 skills not 185, host skills absent). The earlier deploy crash (ModuleNotFoundError claude_agent_sdk) was a packaging gap from the #41 SDK migration — fixed by the Dockerfile dep add, caught ONLY by deploying + hitting the live surface (not the subprocess proof). NET: PromptWorld V1 is DONE + LIVE on MiniMax-M3 — full frontend (3-page interface, geometry-fixed Monaco workbench, terminal) + CEO + 7 isolated specialists on the cheap un-throttled model. KNOWN optional cleanup (out of scope, tokens irrelevant per Isaac): the per-dir CLAUDE.md inlines the 125k gnosys seed -> tight-identity trim would cut cost + OOM risk.  tags:[Live_On_Minimax, End_To_End_Verified, Promptworld_Done]
- [v2]  2026-06-08T11:54:17  FINDING: CONVOS-FIRST (#44, commit 89d1b69) VERIFIED LIVE ON MiniMax-M3 BY ME + deployed :3858 (image a9477f4f44b6). Ran the full convo-switch flow through the REAL API in-container (literal outputs): (1) initial 0 convos; (2) POST /api/chat -> 'ALPHA' real MiniMax turn, session b3f31c59, convo auto-titled from first msg; (3) list=1; (4) POST /api/conversations/ceo/new; (5) list=2 (fresh Untitled + ALPHA); (6) POST /api/chat -> 'BETA' real MiniMax turn, DISTINCT session c4bb43b6; (7) POST .../active {session_id=ALPHA} -> active flips to ALPHA convo; (8) GET .../{ALPHA sid} transcript -> 2 msgs (the ALPHA exchange). ALL acceptance criteria pass: convos grew 1->2, both turns on MiniMax, new minted a distinct session, resume+transcript correct. NO OOM (container's own memory). The list response carries a per-conversation 'active' boolean (resumed ALPHA=true, BETA=false) — correct; my first test wrongly looked for a top-level 'active' field (parsing error, NOT a bug). NET: PromptWorld V2 (e) convos-first is DONE + LIVE. Remaining V2 (journaled, not started): avatar+name on agent header, crons+CEO-heartbeat page, Tier-1 declarative per-agent layouts; 'dynamic add' = emit-a-new-World via the nomicon app-rung (separate from PromptWorld).  tags:[Convos_Live_Minimax, End_To_End_Verified]
