# vision: Compaction (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T14:36:59  FINDING: COMPACTION MECHANISM for the headless architecture — ANSWERED + VERIFIED (I ran the tests). (1) self_compact is 100% TMUX (cat /usr/local/bin/self_compact + /tmp/self-claude-commands/README 'Triggers /compact via tmux'): it tmux send-keys '/compact' to a pane. DEAD for headless — do NOT use it in promptworld. (2) NO --compact CLI flag (claude --help). (3) claude_agent_sdk exposes a PreCompact HOOK (PreCompactHookInput, types.py:221) but NO programmatic compact() method. (4) DECISIVE: /compact IS triggerable HEADLESS — 'claude -p --resume <sid> --output-format stream-json /compact' emits a 'compact_boundary' event (exit 0, success). So you TRIGGER compaction on a headless agent by SENDING THE TURN '/compact' (same path as any user message). => THE MECHANISM for 'CAVE can call compact': TRIGGER = CAVE sends the turn '/compact' to the claude -p agent whenever it decides (context threshold / clean seam / agent-requested) — Isaac's PostToolUse-detect-self_compact-then-queue-30s hack is UNNECESSARY, just send the turn. OBSERVE = the 'compact_boundary' stream event + the SDK PreCompact hook (PreCompactHookInput) = the hook point where CAVE captures pre-compaction state into CartON (the SAME pattern as carton_precompact.py ingestion). Next turn --resume loads the compacted session. ALL HEADLESS, CAVE-controlled, observable, no tmux. OPTIONAL v2 = slinky-based compaction (rewrite the .jsonl with CartON-summary refs, then --resume) when you want CartON-integrated compaction instead of claude's native /compact summary; v1 = native /compact-via-turn (verified, simplest). NET: the pre-build question is closed; promptworld build is unblocked.  tags:[Compaction, Claude_Code_Sdk, Cave, Promptworld, Hooks, Slinky, Self_Compact]
