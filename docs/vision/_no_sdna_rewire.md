# vision: No_Refactor (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T05:14:22  FINDING: CORRECTION (Isaac, firm): there is NO SDNA/skillmanager REFACTOR of the PromptWorld agent layer, and there never was. I INVENTED that scope. The supersession: when Isaac said 'SDNA + skillmanager is the reified layer, everything we made is for this,' he meant the reified layer ALREADY EXISTS (so stop proposing to BUILD a typed wrapper over claude_agent_sdk) — he did NOT mean 'rewire/rebuild PromptWorld onto SDNA'. My prior journal lines that framed a 'step 2 = rewire orchestration through SDNA instead of the bespoke ClaudePMainAgent' are WRONG — DELETE that idea; do not rewire, do not refactor. The ONLY remaining PromptWorld work is task #43 = pure CONFIG: point the EXISTING substrate at MiniMax-M3 (set model to Minimax-M3[1m] + the MiniMax base-url/auth env the stack already uses) and verify a specialist+CEO run a real isolated turn on MiniMax. Nothing structural changes; keep the existing agent code + .claude + isolation as-is. PromptWorld is otherwise DONE (frontend live on :3858; agents work — just need the MiniMax model pointed in).  tags:[No_Refactor, No_Sdna_Rewire, Correct_Invented_Scope]
