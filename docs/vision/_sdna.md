# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:23:33  DECISION: ISAAC DECISION (verbatim intent): out of everything in flight, PROMPTWORLD is THE priority, and that INCLUDES the nomicon system; THEN we revisit what to do. The deeper why: Isaac is trying to make what we've built LEGIBLE to Claude Code users at the same time as fixing it — promptworld (a coding-agent chat UI on claude -p) is the vehicle for both. DESIGN PREFERENCE (his, for the SDNA<->claude interface): SDNA should NOT pass claude SDK objects UP and force us to keep parity with the heaven interface; instead PASS A DICT DOWN and unpack into claude-code — then never maintain parity. Then: make skills for programming with SDNA, give them to SOPHIA -> SOPHIA becomes important again, SOPHIA becomes like PromptWorld. OPEN questions he wants TESTED by subagents (not assumed): (1) does the SDNA claude -p path accurately handle TRANSCRIPTS with the utils, or not; (2) is there a cron in SDNA, or is cron in CAVE — and if CAVE, is it importable WITHOUT booting the full CAVE HTTP server; (3) what are slinky v2 + the ContextEngineering lib in SDNA, and how do they bear on maximizing Chain + Compiler (and a Compoctopus that runs only on the Claude Code SDK); (4) does promptworld work with a TEAM / can teams be called via claude -p / do the previously-built CAVE teams work. Launching a 4-agent testing team to answer these; promptworld+nomicon execution waits on their reports.  tags:[Promptworld, Nomicon, Sdna, Sophia, Legibility]
- [v2]  2026-06-07T18:12:23  FINDING: ISAAC CAUGHT IT (likely a real architecture error to fix): promptworld's agents = a BESPOKE ClaudePMainAgent (raw  CLI subprocess, ZERO SDNA — verified, no sdna/heaven imports). ISAAC'S POINT: CAVE is the ADAPTOR runtime; agents are adapted into CAVE via SDNA; SDNA is the bridge to BOTH heaven AND claude -p. So a CAVE-team agent IS an SDNA agent. The bespoke ClaudePMainAgent BYPASSES SDNA -> it's an orphan that doesn't plug into CAVE-teams as an adapted agent. CORRECTION OF MY EARLIER ADVICE (state-what-is): earlier this session I told Isaac 'don't retrofit SDNA into promptworld' — that reasoned ONLY from provider-agnosticism (which promptworld doesn't need) and MISSED the CAVE-teams-adapts-via-SDNA angle (a separate, valid reason promptworld's agents SHOULD be SDNA-backed). LIKELY FIX: back promptworld's CEO + PromptGym agents with SDNA's CLAUDE backend (claude_agent_sdk via to_sdk_options/runner.agent_step — which IS claude -p), replacing/wrapping the bespoke ClaudePMainAgent. This UNIFIES: one runner (SDNA), bridges both providers, plugs into CAVE-teams. It's an ENGINE SWAP not a rebuild — the AIOS-dir model, /ws streaming, config_dir/CLAUDE_CONFIG_DIR scoping all PRESERVABLE; only the turn-runner changes (CLI subprocess -> SDNA's SDK-library path). INTERFACE NUANCE: ClaudePMainAgent uses the claude CLI BINARY (subprocess: stream-json, /compact-as-a-turn, --resume); SDNA uses the claude_agent_sdk LIBRARY (query() + ClaudeAgentOptions: cwd/env/resume/fork). Must confirm the LIBRARY covers the CLI features (stream events, CLAUDE_CONFIG_DIR scoping, compaction, resume). DISPATCHED the sharpened investigation (cave_teams_for_claudep_ceo_control.md): A=does CAVE require SDNA agents (exact call site)? B=SDNA-claude-backend feature parity vs ClaudePMainAgent? C=A/B/C verdict + swap cost. NO IMPLEMENTATION until Isaac picks. NOTE: if we SDNA-back the agents, this also resolves the earlier runner.py:138 'return a clean dict not raw objects' interface gap (we'd be the consumer).  tags:[Promptworld, Sdna, Cave, Claude_Code_Sdk, Adaptor, Correction, Ceo, Promptgym]
- [v3]  2026-06-07T18:18:40  FINDING: INDEPENDENT CRUX VERIFICATION (my own reads, base/sdna/sdna): SDNA headless claude = GOAL-LEVEL. runner.py:47-88 agent_step builds prompt = config.resolve_goal(merged_inputs) (:88); config.py:106 goal:str templated; NO per-turn user-message param (variable_inputs only interpolate the goal). BUT chat primitives EXIST: config.py:150-152 resume/fork_session/continue_conversation; to_sdk_options :224-245 sets resume/fork/continue + cwd(:244) + env(:245 → CLAUDE_CONFIG_DIR expressible); agent_step has on_message(:50,103) + returns session_id(:110-113). So a CHAT loop is CONSTRUCTIBLE on agent_step (goal:=this-turn message, resume:=sid) but there is NO first-class chat API. The only named 'chat' surface = ClaudeCodeSession (claude_code_commands.py:34) is TMUX (send-keys/capture-pane, compact=/compact via tmux), NOT headless. PARITY flags: (a) SDNA on_message yields claude_agent_sdk MESSAGE OBJECTS, not the CLI raw stream-json NDJSON our gallery eventMapping.ts consumes → swap needs event-shape re-map; (b) compact()-as-/compact-turn is a CLI feature; SDK replaces with PreCompact hook (not a user turn); (c) config_dir AIOS scoping IS expressible via to_sdk_options env+cwd; (d) resume/session parity OK. LEANS verdict (b): our ClaudePMainAgent (CLI subprocess, send/Enter/--resume/on_event-NDJSON/config_dir) IS the chat-level claude runner SDNA lacks headless; adding it to SDNA = wrap ClaudePMainAgent OR build a thin chat loop over agent_step (goal:=msg+resume) + swap on_event shape. Background agent completing full read (heaven_runner chat?, CAVE call-on-agent site, DUOAgent) → consolidated report next. NO BUILD.  tags:[Promptworld, Ceo_Control, Sdna, Chat_Vs_Goal, Parity]
- [v4]  2026-06-07T18:18:46  FINDING: VERIFIED BY ME (read application/cave-teams/cave_teams/primitives.py): cave_teams is a SEPARATE pip package that is CLAUDE-P NATIVE, NOT SDNA. run_opus = claude -p Opus (primitives.py:91,104); continue_opus = resume; run_minimax = MiniMax via Anthropic-compatible API (primitives.py:170-189); line 176 literally 'No SDNA, no chains'. Uses --system-prompt REPLACE (primitives.py:106) + BUFFERED subprocess.run capture_output (primitives.py:87, no live on_event) + ZERO config_dir. CONTRADICTS Isaac's premise 'cave_teams adapts agents via SDNA' — it does NOT. CONSEQUENCE: promptworld does NOT need to be SDNA-backed to use cave_teams (cave_teams = claude -p; our PromptGym agents = claude -p; same family). So the earlier 'back promptworld on SDNA' urgency is MOOT for the cave_teams integration; SDNA-unification is a SEPARATE/LATER concern (the provider-agnostic worlds), not a blocker here. INTEGRATION = option B: a duck-typed cave_teams Conversation adapter (the only contract Harness.deliver needs = Conversation.send(content)->AgentResult, conversation.py:45 / harness.py:87) whose .send calls OUR promptgym.run(type, on_event=broadcast_event) instead of cave_teams buffered run_opus -> gets cave_teams COORDINATION (Harness/leader) + our AIOS-config_dir + live /ws gallery. Option C = a simpler PromptWorld dispatch route, faster interim. NEITHER needs SDNA. THE MARKDOWN-VS-CODE RECONCILIATION (resolves Isaac's confusion): claude code is config/markdown-based (CLAUDE.md + skills), our libs (SDNA/skillmanager/paia-builder) are CODE-based -> the AIOS-DIRECTORY model we just built IS the bridge: the agent is DEFINED by markdown (its directory/CLAUDE.md, claude-code-native) and RUN by a thin runner (claude -p); the runner does NOT handle markdown, claude code does. 'Give them the skillmanager skillset in their persona' -> in the directory model the skills are IN the dir's .claude/skills, claude code auto-loads them, NO runtime-equip needed; the DIRECTORY is the persona+skillset. So for promptworld you do NOT need to unify skillmanager/paia-builder/CAVE into SDNA — the directory model + cave_teams ARE the claude-code-native stack. SDNA-unification stays the separate later question for the agnostic worlds.  tags:[Promptworld, Cave_Teams, Sdna, Claude_Code_Sdk, Aios, Directory, Skillmanager, Markdown_Vs_Code]
- [v5]  2026-06-07T18:22:23  FINDING: CONSOLIDATED + RE-VERIFIED (my own reads on the 2 cruxes). (1) CAVE does NOT require SDNA: cave/core/agent.py:355-382 Agent.run delegates to self._runtime duck-type (.run(content) else .handle_message else callable else no-op); _create_agent_from_entry sets NO runtime (cave_agent.py:362-426) until set_runtime; only SDNA-hard path = spawn_remote gated config.sdna_enabled. Raw ClaudePMainAgent (send_and_wait, no .run) doesn't satisfy as-is but a ~3-line adapter (.run(content)=pmain.send_and_wait(content,on_event=broadcast_event)) does. Note agent.py:388-393 routes the agent's RETURN to central_channel deliverable channels = a DIFFERENT path than /ws, so the live gallery rides our on_event bridge, not CAVE's channel. (2) SDNA = GOAL-LEVEL ONLY (verified runner.py:88 resolve_goal both claude + heaven_runner.py:201; no per-turn-message-into-resumed-session API; ClaudeCodeSession=tmux). Our ClaudePMainAgent IS the missing headless chat-level claude runner. Parity: SDNA-claude has streaming(on_message)/cwd+env(config_dir expressible)/resume+session_id; LACKS append-system-prompt(replace default config.py:233), /compact-as-turn (CLI-only), named ConvoRegistry (only /tmp/current_claude_session_id global). VERDICT (to Isaac): (C) ship now = /api/gym route → promptgym.run(type,req,on_event=broadcast_event) [lightest, reuses built streaming]; (B) cheap when cave_teams coordination wanted = duck-type adapter wrapping ClaudePMainAgent onto CAVE agent.run + keep on_event→/ws gallery; (D) only as deliberate investment = add ~150-250 LOC chat-level claude runner to SDNA (keep CLI core to retain /compact) + ConvoRegistry port. (A) cave_teams-as-is still doesn't fit. Findings /tmp/cave_teams_for_claudep_ceo_control.md (323 lines). UNKNOWN: whether claude_agent_sdk query() forwards options.env(CLAUDE_CONFIG_DIR) to the child (SDK transport not read); heaven hermes_step internals; SDK stream cadence. NO BUILD — Isaac picks.  tags:[Promptworld, Ceo_Control, Cave_Teams, Sdna, Verdict]
- [v6]  2026-06-07T18:25:31  DECISION: ISAAC PICKED option A: keep it separated, ship faster = promptworld runs on CAVE + claude -p directly, NO SDNA; SDNA-unification deferred as its own project. TWO DECIDING FACTS VERIFIED BY ME (read the code, not the report): (1) CAVE Agent.run DUCK-TYPES its runtime (cave/core/agent.py:374-381: .run/.handle_message/callable; result routes to central_channel deliverable channels, a DIFFERENT path than our /ws so the gallery rides OUR on_event bridge) -> NO SDNA required; a ~3-line adapter (runtime.run(content)=pmain.send_and_wait(content,on_event=broadcast_event)) registers ClaudePMainAgent as a CAVE agent runtime. (2) SDNA is GOAL-LEVEL ONLY (runner.py:88 + heaven_runner.py:201 both prompt=config.resolve_goal(merged_inputs) — a templated goal, NOT a per-turn message; no chat-turn API; the only 'chat' = ClaudeCodeSession=TMUX). So OUR ClaudePMainAgent IS the missing headless chat-level claude runner; backing promptworld on SDNA = option D (ADD ~150-250 LOC chat_runner.py + ConvoRegistry port + append/creds wiring to SDNA) = a deliberate 'SDNA owns the claude chat backend' investment, SKIPPED for now. MINIMAX: claude -p + ANTHROPIC_BASE_URL env swap reaches minimax (claude-code-native, no SDNA) — mechanism IS-by-doc, NOT tested this session (deferred). PLAN: (C) NOW = CEO controls PromptGym via a dispatch tool (a PromptWorld MCP tool the CEO's claude -p calls, or a route+skill) -> promptgym.run(type, on_event=broadcast_event(type)) -> gallery via /ws + result to CEO; (B) CHEAP LATER = the ~3-line cave_teams/CAVE duck-type adapter for real shared-task-list coordination + simultaneous agents; (D) only as the conscious SDNA-owns-claude-chat decision. Dispatching (C). builder parity gaps for the future-D swap (per his read): SDNA on_message yields SDK message OBJECTS not CLI NDJSON (our eventMapping consumes NDJSON); SDNA system_prompt=REPLACE not --append; SDNA CANNOT do /compact-as-a-turn (CLI-only); no ConvoRegistry. All preservable behind the promptgym.run seam if/when we do D.  tags:[Promptworld, Cave, Claude_Code_Sdk, Sdna, Cave_Teams, Ceo, Gallery, Ship]
- [v7]  2026-06-08T13:52:35  FINDING: #46 BUGFIX (team-lead live-verify caught it): cron-EXPRESSION automations never fired (interval + heartbeat fired fine). ROOT CAUSE base/sdna/sdna/cron.py CronJob.is_due() cron branch: 'base = self.last_run or now' -> when last_run is None, base=now -> croniter.get_next() is ALWAYS strictly future -> 'now >= next_run' always False -> never fires -> last_run never advances -> never fires forever. The INTERVAL branch correctly handles last_run-None (return True); the cron branch lacked the anchor. FIX: added CronJob.__post_init__ that stamps last_run=now at creation FOR CRON-EXPR JOBS ONLY (so the first fire = the next real boundary; correct cron semantics, no surprise immediate fire, no catch-up of missed boundaries). INTERVAL jobs deliberately keep last_run=None -> still fire once immediately (UNCHANGED). CONSUMER CHECK: only other cron-expr user = cave/sanctum_automations.py (daily/weekly/monthly ritual notifications, '{m} {h} * * *') — they hit this SAME bug and have been silently never-firing; the fix REPAIRS them (next-boundary, no storm) = aligned with intent, not a regression; nobody relies on never-fire. selfbot.py uses sdna.cron via subprocess scheduling, not is_due -> unaffected. VERIFIED (pip install --no-deps base/sdna + test): cron-expr last_run set at creation + is_due False immediately + True after a passed minute boundary; interval last_run None + is_due True immediately (unchanged); CAVE Automation.create cron path is_due False->True across boundary. Shared lib -> Dockerfile COPY sdna + pip --no-deps picks it up on rebuild. Source-only; commander rebuilds + live-verifies '* * * * *' fires within ~1-2min via the Heart tick.  tags:[promptworld, cron, sdna, bugfix, sanctum]
