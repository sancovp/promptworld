# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T23:06:15  VISION: TWO V1 frontend gaps (Isaac, from localhost:3858). (1) CHAT DOES NOT RENDER MARKDOWN: the CEO/agent messages show as PLAIN TEXT; code blocks, lists, formatting must render as markdown (add a markdown renderer to the message parts in Thread.tsx). (2) WORKSPACE EDITOR DOESN'T MAKE SENSE AS AN EDITOR: the editor PANE IS the real Monaco component (@monaco-editor/react = the actual VS Code Monaco engine, confirmed by syntax highlight/line-numbers in the screenshot) — that part is correct — BUT it is wrapped in a HAND-ROLLED minimal file tree (FileExplorer.tsx), NO TABS, opens ONE file at a time, no editor chrome, so it reads as 'a bare Monaco textbox bolted to a janky list', not an editor. Isaac wants THE ENTIRE EDITOR: keep the real Monaco editor + a PROPER file explorer (folders, expand/collapse) + TABS (open multiple files) = a real code-editor workbench experience over /workspace (the /api/files tree/read/write API already exists). VERIFY (render rule): render the SPA in headless chromium, SCREENSHOT, assert in the DOM that the chat renders markdown (e.g. a code block becomes a <pre>/<code>, not raw text) + the editor has tabs + a real explorer; the commander OPENS the screenshot. INTERPRETATION MARK: 'the entire editor' = full Monaco code-editor workbench (explorer + tabs + editor); if Isaac meant a specific drop-in component, adjust.  tags:[Promptworld, V1, Frontend, Monaco, Markdown, Editor, Tabs]
- [v2]  2026-06-07T23:17:47  DECISION: APPROACH CORRECTION (Isaac caught it): we were HAND-ROLLING what assistant-ui ships PREBUILT, because we installed only the HEADLESS core. VERIFIED: package.json has @assistant-ui/react ^0.11 + raw react-markdown ^10.1; node_modules/@assistant-ui has only react (+tap) — NO @assistant-ui/react-markdown (the prebuilt MarkdownText EXISTS on npm @0.14), NO styled UI package; Thread.tsx imports raw ReactMarkdown+remarkGfm+rehypeHighlight + a custom Thread + custom styles.css = reinventing assistant-ui's prebuilt layer. WHY it went wrong: used headless primitives; the one styled attempt grabbed @assistant-ui/styles@0.3.7 against react@0.11 = VERSION MISMATCH = skeleton -> builder gave up on assistant-ui UI + hand-rolled. CORRECT (dispatched): use assistant-ui's PREBUILT components, version-COHERENT — pick a coherent @assistant-ui/* set (current line = @assistant-ui/react 0.14.x + shadcn/Tailwind styled Thread + @assistant-ui/react-markdown 0.14 MarkdownText), use the prebuilt MarkdownText (delete hand-rolled markdown) + the prebuilt styled Thread (set up Tailwind — Tailwind does NOT OOM, only Monaco self-host did) on CEO chat + gallery; if bumping react breaks the useExternalStoreRuntime adapter, fix the adapter to the new API, do NOT abandon the prebuilt UI. SCOPING: assistant-ui owns the CHAT (markdown+styling+composer+tool-cards+code-blocks all OOTB); the MONACO editor workbench (explorer+tabs) is SEPARATE + custom (Monaco is not assistant-ui). RULE going forward: do NOT hand-roll what the chosen library provides prebuilt; install + use the prebuilt layer, version-coherent. Verify via render+screenshot (commander opens PNGs).  tags:[Promptworld, V1, Frontend, Assistant_Ui, Markdown, Prebuilt, Correction]
- [v3]  2026-06-08T00:17:39  FINDING: FRONTEND AT MAXIMUM — VERIFIED BY ME (opened the PNGs) + DEPLOYED on :3858. pw-builder2 commit 96b1b4e. (1) MARKDOWN now full: I opened /tmp/pw_ui_md_full.png — code block syntax-highlighted in a oneDark PANEL (def/print/return colored, python+Copy header), list WITH bullet markers, styled heading. (2) EDITOR now the full heaven feature set: I opened /tmp/pw_ui_workbench.png — tree w/ lucide icons + new-file + new-folder + delete + save + refresh + theme dropdown (Dark/Light/HC) + multi-file TABS + Monaco+minimap, all rewired to /api/files (no docker-exec, self-hosted Monaco). Served bundle verified in-container: react-syntax-highlighter x2 + registerLanguage; mkdir 200; monaco 200; CEO chat OK. CAUSAL MECHANISM the builder found (now a 1-line rule): assistant-ui's AsyncLight syntax highlighter uses a dynamic import Vite CANNOT bundle -> code renders plain; use makePrismLightSyntaxHighlighter (PrismLight + registerLanguage) + restore ul/ol bullets in CSS (assistant-ui's reset strips them). PRACTICE LANDED: this fight produced 1 one-line causal rule (assistant-ui-syntax-highlighter-use-prismlight) banked for reuse. ROUNDS-TO-RESULT was high here (markdown took 4 rounds: hand-rolled -> assistant-ui-unstyled -> still-unstyled -> PrismLight) because I was ASKING not CAUSING; the banked rule means next time it is 1 round. UNKNOWN: on-screen final judgment is Isaac's at localhost:3858 (render was the byte-identical bundle, not the container process).  tags:[Promptworld, V1, Frontend, Markdown, Editor, Verified, Deployed, Rounds_To_Result]
