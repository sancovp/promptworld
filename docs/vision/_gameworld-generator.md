# vision: promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T18:09:19  FINDING: SYNTHESIS of the 4-agent *World investigation (maps: /tmp/pw_worldmaps/{jobworld,healthworld,gameworld_and_skills,app_compiler_and_diff}.md; crux SKILL.md re-read BY ME). THE DIFFERENCES (Isaac's 'promptworld not set up the same way' — answered): JobWorld+HealthWorld = CAVE-fork web apps where the DOMAIN IS HARDCODED INTO A PYTHON SUBCLASS ({World}Agent(CAVEAgent)+{World}HTTPServer(CAVEHTTPServer); DOMAIN_ENUM/DEPARTMENT_SPECS baked in; HealthWorld is literally a renamed JobWorld fork w/ a latent  bug), instances spun by instantiate.sh copying a template + sed {{PORT}} only (domain NOT parameterized); agents run via tmux/claude-p .md Conversations on a file-based cave-teams Harness (CEO+departments ORG pattern). GameWorld = a DIFFERENT team pattern (peer economic MARKET, shared game.json) + the ONLY real spec->world generator (gameworld-generator) but it parameterizes MECHANICS not DOMAIN. PromptWorld = ALSO a CAVE fork (forked from HealthWorld) BUT the CLEAN pattern: claude_agent_sdk per-DIRECTORY AIOS agents (directory-codes-the-agent via CLAUDE.md) + carries the NOMICON LADDER + re-skinnable world-context.md — domain is clean AIOS-dir config, NOT a tangled subclass. So 'set up differently' = set up CLEANER/nomicon-native. THE APP-COMPILER REALITY: only nomicon-atomize is real code; the ladder is prompt-skills; ingest-into-nomicon-app (the app-rung) ORCHESTRATES ingest->convert->agentify but STAGE-3 app-boot is UNPROVEN (skill VERIFY says 'Mark IS/UNKNOWN honestly'; E2E only on a TOY single-AIOS) AND it INGESTS-an-existing-dir, it does NOT SYNTHESIZE-a-World-from-a-domain-spec. THE DESIGN PICTURE (mine, for Isaac to confirm — VISION): app = World-module + (opt) Gym-module OVER the nomicon ladder (already PromptWorld's self-def). A WORLD = a parameterization of an app: {team-pattern in {org,market}, persona-set, department/domain spec, dashboard}. So JobWorld=app(org, CEO+biz-depts, hiring); HealthWorld=app(org, OSMOSYS+14-body-systems, health); GameWorld=app(market, GM+players, game); PromptWorld=app(org, Engineer-CEO+7-crafts, claude-code-components). The 4 existing *Worlds = REFERENCE PATTERNS/templates to fold into the compiler. PIVOTAL FORK FOR ISAAC (do NOT auto-decide): does the World/app compiler EMIT clean PromptWorld-style apps (AIOS-dirs+nomicon+re-skinnable world-context; old job/health re-expressed in the clean form) [my recommend], OR emit CAVE-subclass-style Worlds (generalize instantiate.sh+gameworld-generator into a domain-parameterized generator that bakes a {World}Agent subclass)? + is the compiler the INGEST path (ingest-into-nomicon-app, exists, prove stage-3) or a new SYNTHESIZE-from-spec path (gameworld-generator is the seed) or BOTH?  tags:[promptworld, jobworld, healthworld, gameworld, app-compiler, world-as-app-type, ingest-into-nomicon-app, gameworld-generator]
