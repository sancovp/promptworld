# vision: promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T18:43:20  FINDING: PUBLISHING STATE (Isaac flagged: promptworld is git-published INSIDE the monorepo but 'has to be its own public facing repository... never addressed how'). GROUNDED: (1) NO sancovp/promptworld repo exists (sancovp has starship-mcp/heaven-ecosystem/duo + an old private PromptGymv01); (2) NO split decision on record (confirmed by grep). (3) promptworld lives at application/promptworld/ inside the PUBLIC sanctuary-revolution-alpha monorepo — public but buried, NOT a clone-and-go surface. (4) DEPENDENCY WRINKLE: promptworld does NOT stand alone — deploy/Dockerfile COPYs cave/ + sdna/ + doc-mirror-plugin/ from the staged monorepo context (build.sh stages them) + pip-installs them --no-deps. So a bare standalone clone can't build without those 3. README STALE after M5/M6: line 135 'Gym module (in revision)'/VISION but Gym SHIPPED (M5); line 15 'not a World compiler' + zero mention of compile-a-world though M6 added the meta-compiler. OPEN FORK FOR ISAAC (release architecture, do NOT auto-decide): (A) repo/sync approach — git subtree-split -> sancovp/promptworld keeping monorepo canonical [my recommend, preserves monorepo-is-source-of-truth] | submodule | CI-mirror | plain-copy; (B) the dep wrinkle — publish cave/sdna as pip pkgs + plugin as release artifact the build pulls | vendor them into the standalone repo | build clones monorepo; (C) what 'give people promptworld' MEANS — a git repo to clone vs a claude-code PLUGIN/installer (ship-a-plugin) that boots the container = 'installs super easy from claude code'. These two surfaces (public source repo AND easy claude-code installer) may BOTH be wanted.  tags:[promptworld, publishing, monorepo, standalone-repo, cave, sdna, doc-mirror-plugin, README]
- [v2]  2026-06-08T19:06:49  VISION: ISAAC REFINEMENT (verbatim sense, 2026-06-08 — 'starting to sound good', brainstorming, NOT yet a build-go): 'then we wanna supe it up with metastack templates or something for how it RENDERS and VALIDATES the README so it always stays updated, has CHANGELOG etc. needs to have these RULES or something. then we make this a PLUGIN and give it to OURSELVES ya? so then we have doc-mirror, promptworld, whatever this will be called (scalable-publishing i guess) and nomicon system... yeah i think thats starting to sound good.' PARSE: (1) the publish step isn't just rsync — on each publish it RENDERS+VALIDATES the public README (and a CHANGELOG) from a METASTACK template, enforced by RULES, so the public README never drifts = the Pattern_Single_Source_Multi_Render (metastack exists = pydantic_stack_core, the structured-text compiler). (2) 'make this a plugin and give it to ourselves' = THIS publishing system becomes its OWN claude-code PLUGIN that we DOGFOOD = exactly the self-replicating base publishing plugin telos (Doc_Mirror_System_Publishing_Plugin_Self_Replicating_Base_2026_06_03). (3) THE SUITE he's enumerating (the plugins we'd give people/ourselves): doc-mirror, promptworld, scalable-publishing [tentative name], nomicon-system. WHAT THE scalable-publishing PLUGIN CONTAINS: heaven-bml-v2 publish workflow (parameterized rsync clone-and-push + gh repo create, per /tmp/heaven_bml_map.md) + metastack README/CHANGELOG render+validate + the scaffold SKILL (drops the per-subdir workflow YAML into the monorepo) + an install-wizard + the enforcing RULES. COHERENCE NOTE (light, for Isaac): 'nomicon system' as a 4th sibling is in mild tension with what we verified — the nomicon ladder currently LIVES INSIDE doc-mirror (skill2framework/fold-into-nomicon/nomicon-atomize); so is nomicon its OWN plugin or part of doc-mirror? (resolve when packaging the suite). NOT A BUILD-GO YET — captured as vision; build order when he says go: scrub the leaked PAT -> heaven-bml-v2 core -> metastack README/changelog render+validate+rules -> package as the scalable-publishing plugin (dogfood) -> apply to promptworld + the tier-3 index/marketplace repo.  tags:[scalable-publishing, metastack, readme, changelog, single-source-multi-render, plugin, self-replicating-base, doc-mirror, promptworld, nomicon, suite]
- [v3]  2026-06-08T19:17:32  DECISION: REFINEMENT (Isaac: 'maybe it doesnt need metastack?' — my honest assessment: PROBABLY RIGHT, DROP metastack for v1). WHY: metastack (pydantic_stack_core + its REGISTRY service + HEAVEN_DATA_DIR) is a HEAVY dep to bake into EVERY published repo's CI; heaven-bml's generate_ecosystem_readme.py needs only . The publishing plugin's whole point is LIGHTWEIGHT + STANDALONE-SHIPPABLE, so dragging metastack into each repo's workflow is friction with little payoff (READMEs/changelogs are simple structured data). KEY REALIZATION: the 'README always stays updated' guarantee does NOT come from metastack — it comes from the AUTO-REGENERATE WORKFLOW (regenerate README from config on every publish + daily cron => physically cannot drift), which heaven-bml ALREADY does. metastack would only validate the config SHAPE, and a TINY INLINE PYDANTIC MODEL in the plugin does that more cheaply than the metastack registry. SO v1 = heaven-bml's ecosystem.json + python renderer pattern, EXTENDED to per-repo README + CHANGELOG, no-drift from the regenerate-workflow, optional small inline pydantic validation; NO metastack dep in shipped repos. metastack EARNS its place only if we later want README/doc templates SHARED ACROSS THE ECOSYSTEM via the registry (defer). SUPERSEDES the earlier 'supe it up with metastack' note (2026-06-08T19:06:49) — the freshness goal is met by the workflow, not metastack. (Prescan flagged Assumption_Metastack_Available — exactly the fragile assumption we avoid by not depending on it in shipped CI.)  tags:[scalable-publishing, metastack, readme, changelog, no-drift, auto-regenerate, pydantic, lightweight]
