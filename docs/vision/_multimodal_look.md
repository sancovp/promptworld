# vision: Recursive_Screenshot_Loop (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T01:17:03  DECISION: ISAAC CORRECTION (frontend dev METHOD, furious — I had it wrong twice): a frontend developer agent must develop in a TIGHT RECURSIVE VISUAL LOOP — run the app (Vite dev server + HMR), launch Playwright, screenshot the running page at a real viewport, READ the PNG itself and actually LOOK at it (the agent is multimodal — it can SEE the layout), edit from what it SEES, screenshot again, look again — recursively until the page is visually correct. The looking-at-the-image is EVERY iteration's driver, NOT a final check. NOT edit->assert-DOM->report; NOT bundle-grep; NOT render-once-at-end; NOT 'report screenshots for the commander to open instead of looking itself'. This is how every LLM frontend dev should work. MY FAILURE: I spawned pw-builder4 with a DOM-assertion + final-screenshot framing instead of the continuous screenshot->look->edit loop. FIX: (1) messaged pw-builder4 with the corrected method (overrides its spawn prompt); (2) banked project rule frontend-agent-recursive-screenshot-look-edit-loop.md = the agent works by the recursive playwright-screenshot->LOOK->edit loop as its PRIMARY method, composing with verify-frontend-by-rendering (the geometry/quality BAR). Commander does the same loop as the final gate before it reaches Isaac.  tags:[Recursive_Screenshot_Loop, Playwright, Multimodal_Look]
