# vision: Three_Page_Interface (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T01:05:03  FINDING: 3-PAGE CHAT INTERFACE DELIVERED + VERIFIED BY ME (opened all 5 PNGs myself, mtime 2026-06-08T00:59) + rebuilding image for :3858. pw-builder3 commit cc6264d on promptworld-event-stream. (1) MAIN = CEO chat + Monaco workspace (agent dirs) + collapsible terminal. (2) SPECIALIST = shadcn Select -> chosen agent (Harness) window + Monaco. (3) GROUP = multi-select pills -> N side-by-side COLORED windows (CEO blue/Skill green/MCP purple) + per-window color swatch/label input/reorder arrows/remove; Load-template dropdown + Save. CUSTOMIZE proven: relabel skill->Brain, recolor red, reorder-to-first applies live. PERSISTENCE proven E2E: full page reload + Load 'Trio' restores order+label+color exactly = server-side group_templates (server/group_templates.py + GET/PUT/DELETE /api/group-templates over JSON at $PROMPTWORLD_DATA). ARCH: per-alias chat lifted into a global chatStore singleton (useSyncExternalStore, subscribes agentBus per alias, folds /ws) so history+live stream survive page switches + unmounted windows; AgentWindow now stateless view. All at MAXIMUM bar — no refire needed. Banked rule: render-verify-react-controlled-input-use-native-setter (color input set via el.value doesn't fire React onChange — use prototype setter; was a test false-FAIL, not a product bug).  tags:[Three_Page_Interface, Group_Templates, Render_Verify]
- [v2]  2026-06-08T01:07:22  FINDING: DEPLOYED the 3-page chat interface live on localhost:3858 (rebuilt promptworld:latest from commit cc6264d, agent create->cp-creds->start). Port 3858 had been held by the OLD container named 'promptworld'; removed it, started the freshly-created promptworld-v1 (new image + creds cp'd). VERIFIED in-container: /api/health ok (dir /app/promptworld, CEO session live); SPA serves; /api/group-templates -> {templates:[]} = the NEW server-side group-templates endpoint is LIVE (proves the new dist+server are running, not the stale container); /api/files/tree rooted at /app/promptworld shows the agent dirs. Floor = render-verified byte-identical dist + new-endpoint-live in-container; final on-screen judgment is Isaac's at :3858. DEPLOY NOTE for next time: build.sh NAME=promptworld-v1 but a prior live deploy used name 'promptworld' -> port-already-allocated on boot; must rm the OTHER-named container holding :3858 first.  tags:[Deploy, Render_Verify]
