# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T22:13:57  DECISION: BUILDER HANDOFF ANCHOR (old pw-builder rotting at 800k -> killing it, spawning fresh; fresh builder rehydrates from THIS vision, not old context). CURRENT STATE (all verified by me, LIVE at localhost:3858): V1 BUILT + DEPLOYED, branch promptworld-event-stream, latest commit f69eba8. Image promptworld:latest on the host daemon; I deploy via AGENT create then docker cp /home/GOD/.claude/.credentials.json then start (NO baked creds, NO bind mounts, NO -e). DONE+VERIFIED: (1) all 8 agent prompts (CEO agents/engineer-ceo.md + 7 promptgym/TYPE/CLAUDE.md) + universal-worker = EXACT verbatim copy of doc-mirror system prompt /home/GOD/.claude/CLAUDE.md + appended ROLE ADDITION block; zero GUY; every head = SYSTEM_PROMPT open-tag. (2) CEO has every skill + a native subagent per type, tells each to work only in its promptgym TYPE dir; CEO to specialist subagent dispatch works in-container. (3) the 7 scoped AIOS-dir specialists = HALF 2. (4) deploy agent create-cp-start; image ships UNAUTHENTICATED; embedded TERMINAL panel /ws/terminal PTY bash as ceo = live claude-auth-login surface. (5) Monaco file explorer present (pinned monaco-editor 0.55.1 CDN loader via src/monacoSetup.ts) + /api/files. (6) TERMINAL is COLLAPSIBLE. REMAINING (the ONLY open item): assistant-ui STYLING — UI uses @assistant-ui/react HEADLESS PRIMITIVES with NO Tailwind / NO styled pkg / NO theme css (only hand-rolled styles.css) so it looks bad; FIX = adopt assistant-ui styled layer (Tailwind + assistant-ui shadcn Thread + tailwind plugin, OR @assistant-ui/react-ui + theme css) on CEO chat + gallery windows; then npm run build, rebuild image, I re-deploy on :3858. DEPLOY NOTE: build RAM ~1GB (self-hosting Monaco OOMs vite -> pinned CDN). FRESH BUILDER MANDATE: read application/promptworld/docs/vision/ (latest entries SUPERSEDE older), implement the styling item until the SPA looks like real assistant-ui, rebuild, report literal proof; I verify + re-deploy + refire.  tags:[Promptworld, V1, Handoff, Fresh_Builder, Frontend]
- [v2]  2026-06-08T00:44:43  DECISION: BUILDER HANDOFF ANCHOR #2 (rotating pw-builder2 -> fresh builder; rehydrate from THIS vision + the project rules, not chat). CURRENT STATE (all verified by me opening screenshots, LIVE at localhost:3858, branch promptworld-event-stream HEAD ~534e018): V1 COMPLETE + the frontend at MAXIMUM. Deploy = AGENT create->docker cp /home/GOD/.claude/.credentials.json -> start (image promptworld:latest on host daemon; NO baked creds). BUILT + VERIFIED: CEO + 7 PromptGym specialists (prompts = verbatim doc-mirror system prompt + ROLE ADDITION block); prebuilt assistant-ui chat (@assistant-ui/react 0.14 + react-ui styled Thread + react-markdown MarkdownText + PrismLight syntax highlighter, bullets/headings) on CEO + gallery; the FULL heaven Monaco workbench (tree + tabs + new-file + new-folder + delete + RENAME + save + theme select + self-hosted /monaco) rewired to /api/files, ROOTED at /app/promptworld so it shows the agent dirs (agents/, promptgym/{skill,...}, server/, .claude/agents/); right-click shadcn ContextMenu (New/Folder/Rename/Delete); collapsible terminal = live claude-auth-login PTY; Tailwind + shadcn/ui set up (vite @ alias). PROJECT RULES at application/promptworld/.claude/rules/ (READ + OBEY: use-libraries-at-maximum-never-hand-roll, verify-frontend-by-rendering-not-bundle-grep [+ DOM-exists≠styled + fresh-screenshot-filename], porting-heaven-components-rewire-fileservice-to-api-files, assistant-ui-syntax-highlighter-use-prismlight). NEXT READY VISION (journal 2026-06-08T00:44:21): the 3-PAGE chat interface (left side panel: Main = CEO+Monaco; Specialist = choose-one+Monaco; Group = select-any -> side-by-side windows + save group templates with order/color/label). OPERATING MODEL: simple 'implement the ready vision' dispatch + obey rules + verify by rendering (commander opens PNGs) + refire until maximum; bank each new lesson as a one-sentence rule.  tags:[Promptworld, Handoff, Fresh_Builder, Frontend]
- [v3]  2026-06-08T00:47:04  DECISION: COMPACT ANCHOR (2026-06-08 ~00:46). READ FIRST POST-COMPACT: the HANDOFF ANCHOR #2 (journal 2026-06-08T00:44:43, vision _handoff.md) = the authoritative current state. === CURRENT STATE (all verified BY ME via opening screenshots, LIVE at localhost:3858) === PromptWorld V1 is COMPLETE + the frontend at MAXIMUM. Branch promptworld-event-stream (HEAD ~534e018 + my rule commits 839b3bf/be21150). DEPLOY = AGENT does it: docker create --name promptworld -p 3858:3858 promptworld:latest -> docker cp /home/GOD/.claude/.credentials.json promptworld:/creds/.credentials.json -> docker start (image on host daemon via mind_of_god socket; NO baked creds, NO bind mounts). BUILT+VERIFIED: CEO + 7 PromptGym specialists (prompts = verbatim doc-mirror system prompt /home/GOD/.claude/CLAUDE.md + ROLE ADDITION block, zero GUY); prebuilt assistant-ui chat (@assistant-ui/react 0.14 + react-ui styled Thread + react-markdown MarkdownText + PrismLight syntax highlighter + bullets/headings) on CEO + gallery; FULL heaven Monaco workbench (tree+tabs+new-file+new-folder+rename+delete+save+theme, self-hosted /monaco) rewired to /api/files, ROOTED at /app/promptworld so it shows the agent dirs (agents/, promptgym/{skill,...}, server/, .claude/agents/); right-click shadcn ContextMenu; collapsible terminal = live claude-auth-login PTY; Tailwind+shadcn (vite @ alias). === IN FLIGHT: pw-builder3 (team background-streams, survives compact) building the NEXT vision = the 3-PAGE chat interface (journal 2026-06-08T00:44:21): left side panel with MAIN (CEO+Monaco), SPECIALIST (choose-one+Monaco), GROUP (select-any -> side-by-side windows + save group templates with order/color/label), reusing the prebuilt assistant-ui Thread per agent. It reports to my inbox; artifacts stand alone. === PROJECT RULES at application/promptworld/.claude/rules/ (5: use-libraries-at-maximum-never-hand-roll, verify-frontend-by-rendering-not-bundle-grep [+DOM-exists≠styled +fresh-screenshot-filename], porting-heaven-components-rewire-fileservice-to-api-files, assistant-ui-syntax-highlighter-use-prismlight). === OPERATING MODEL (tonight's core lesson, journal 2026-06-08T00:16:02 + 23:35:06): a prompt must CAUSE the result not ASK; main dispatch = 'implement the ready vision for {repo}'; rules guide + prevent recurring mistakes; VERIFY BY RENDERING + COMMANDER OPENS THE PNGs (DOM-element-exists ≠ styled/works); refire until MAXIMUM; bank each new lesson as a ONE-SENTENCE rule; doc-mirror-prompts = the causal-prompt-skill store (build composition into it; update my system prompt with cause-not-ask = future system work). === POST-COMPACT FIRST MOVES: (1) check pw-builder3's report on the 3-page interface; OPEN its screenshots myself; judge each page (Main/Specialist/Group) at the max bar; refire if not maximum; then rebuild image + redeploy via the agent create->cp creds->start on :3858. (2) the deploy + render-verify pattern is in handoff anchor #2. === FUTURE HORIZONS journaled tonight (NOT active, captured as VISION): GOD-from-HEAVEN = self-fork by folding promptworld+gym into the nomicon (2026-06-07T22:32:34); nomicon = recursive self-similar operator nomicon^n tower (22:42:01); operating-model synthesis = science+library-science+ontology+composition self-updating (00:16:02); editor-future = entire Monaco + custom extensions (23:55:56). === ME = commander; pw-builder3 = active builder; all verified-by-me-via-screenshots. Task list backed up (28 tasks). CONTINUABLE.  tags:[Promptworld, Compact, Handoff, Pw_Builder3, Operating_Model]
