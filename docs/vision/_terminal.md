# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T21:36:05  INTENT: NEW direction (Isaac, supersedes baked-creds): build a TERMINAL into the frontend = the universal live login surface. Copied/baked creds don't reliably authenticate (claude falls back to /login, unavailable headless) + baked = wrong per-user. So the user opens http://localhost:3858, runs 'claude auth login' in an embedded xterm, does the browser OAuth on THEIR subscription. BUILD (additive, branch promptworld-event-stream): (1) BACKEND /ws/terminal FastAPI WebSocket -> spawns a PTY (python pty) running interactive bash AS ceo (the server already runs as ceo, so the PTY child is ceo -> claude auth login writes /home/ceo/.claude where claude -p reads); bidirectional stream via loop.add_reader(master_fd)->queue->ws.send + ws.receive->os.write; resize via TIOCSWINSZ control msg. (2) FRONTEND xterm.js panel/tab in the SPA wired to /ws/terminal (term.onData->ws, ws.onmessage->term.write, onResize->json). (3) LOGIN FLOW: default PRODUCT image UNAUTHENTICATED — gate the creds-bake behind build ARG BAKE_CREDS (default 0; dev convenience only); frontend hint 'Not logged in? open the terminal, run claude auth login'. (4) keep CEO + 7 specialists + Monaco + file API intact. VERIFY headlessly: fresh container NO baked creds; /ws/terminal ws -> (a) whoami=ceo, (b) claude auth login LAUNCHES + emits the OAuth URL (the click+paste is the user's interactive step = UNKNOWN-by-design; prove plumbing to the URL), (c) xterm renders + round-trips keystrokes. Report diff + literal terminal-ws output, honest IS/UNKNOWN.  tags:[Promptworld, V1, Terminal, Xterm, Login, Pty]
- [v2]  2026-06-07T21:45:32  FINDING: TERMINAL LOGIN SURFACE BUILT + E2E-VERIFIED BY ME. BACKEND: promptworld_server._register_terminal_ws() -> /ws/terminal WebSocket spawns a PTY (python pty) running interactive bash -il as the server's user = ceo (entrypoint drops to ceo); bidirectional via loop.add_reader(master_fd)->asyncio.Queue->ws.send_text + ws.receive->os.write; resize via {type:resize,cols,rows}->TIOCSWINSZ. FRONTEND: @xterm/xterm + @xterm/addon-fit; components/TerminalPanel.tsx (term.onData->ws, ws.onmessage->term.write, onResize->json); App.tsx adds a Terminal pane + a 'Not signed in? run claude auth login' login-hint banner; styles.css terminal CSS. LOGIN GATING: Dockerfile ARG BAKE_CREDS=0 (default) -> PRODUCT image ships UNAUTHENTICATED (creds.json staged empty, RUN installs only if BAKE_CREDS=1 && non-empty); build.sh BAKE_CREDS env (default 0) stages real creds only when =1, passes --build-arg. So default boot = no creds = user logs in live via the terminal. E2E (rebuilt DEFAULT image BAKE_CREDS=0 -> NO baked creds confirmed; booted; ran a websockets client to ws://localhost:3858/ws/terminal INSIDE the container): (a) whoami -> ceo; (c) keystroke round-trip 'echo RT_42_END' -> 'RT_42_END' echoed; (b) 'claude auth login' LAUNCHED + emitted the REAL OAuth URL https://claude.com/cai/oauth/authorize?...&code_challenge_method=S256... + 'Paste code here if prompted >'. claude auth login confirmed correct (claude auth --help: login=Sign in). GET / serves the new SPA (assets/index-CkaqiWWX.js), xterm IN the bundle. The browser click+paste is the user's interactive step = UNKNOWN-by-design; plumbing-to-URL PROVEN. DROPPED committed frontend/dist (git rm --cached) — the Dockerfile builds dist in-layer (npm ci && npm run build), so committed built bundles are redundant noise (team-lead's prior recommendation). docker deliverable unaffected (builds its own dist, verified).  tags:[Promptworld, V1, Terminal, Xterm, Pty, Login, Verified]
- [v3]  2026-06-07T21:58:24  VISION: V1 UI fixes (Isaac, from the running app at localhost:3858 — it WORKS): (1) the TERMINAL window must be COLLAPSIBLE (a collapse/expand toggle so it can be hidden/shown). (2) the MONACO FILE EXPLORER is MISSING from the current SPA — it was built earlier (slice 3, commit 32c7b08, file_api + Monaco panel) but is NOT wired into the current App.tsx (the gallery/terminal frontend work dropped it). RESTORE/wire the Monaco file-explorer panel into the SPA (it reads/writes /workspace via the existing /api/files tree/read/write API). Both are frontend-only changes -> require npm run build -> rebuild image -> I re-deploy (create->cp->start) so localhost:3858 serves the new SPA.  tags:[Promptworld, V1, Frontend, Terminal, Monaco]
- [v4]  2026-06-07T22:09:04  FINDING: Two V1 UI fixes BUILT + verified in-container. (1) TERMINAL COLLAPSE: App.tsx termOpen state + a 'term-toggle' button in the pane title; collapsed renders just the title bar (pane-terminal.collapsed) and unmounts TerminalPanel (login persists — creds are on disk in /home/ceo/.claude regardless of the terminal session); styles.css toggle CSS. (2) MONACO FILE EXPLORER: CORRECTION — it was NOT dropped from App.tsx; App.tsx:57 already imports+renders <FileExplorer/>, and FileExplorer.tsx:133 mounts the Monaco <Editor> over /api/files. The REAL issue: @monaco-editor/react loads Monaco from a CDN at runtime (not bundled — self-host OOMs the vite build at ~1GB free RAM), and the default loader version can drift/404 -> blank editor. FIX: src/monacoSetup.ts pins loader.config({paths:{vs:'https://cdn.jsdelivr.net/npm/monaco-editor@0.55.1/min/vs'}}) (the installed version), imported in main.tsx before App -> deterministic editor load. VERIFIED in the rebuilt image's served bundle (docker exec): monaco-editor@0.55.1/min/vs present; term-toggle + 'Hide the terminal' present; GET / serves the SPA; GET /api/files/tree -> {root:/workspace,...} responds. Frontend-only; image rebuilt (npm run build in-layer) so the served SPA has both; booted via create->cp creds->start. NOTE for later: true offline Monaco (self-host bundle) needs more build RAM than the ~1GB currently free.  tags:[Promptworld, V1, Frontend, Monaco, Terminal, Collapse, Verified]
- [v5]  2026-06-07T23:01:14  FINDING: 3 UI BUGS FIXED + VERIFIED BY ME VIA THE RENDERED PAGE (I opened the screenshots myself — the real surface). pw-builder2 commit 3c66c64, re-deployed on :3858 by me (create->cp->start, latest image). I OPENED /tmp/pw_ui_after.png + /tmp/pw_ui_after_termcollapsed.png: (1) TERMINAL COLLAPSE now actually shrinks the flex band to the title bar (pane 300px->34px, .xterm unmounts) and the gallery reclaims the space (HARNESS/OPERATING_SYSTEM windows + composers appear) — FIX: .pane-terminal flex 0 0 300px open / 0 0 auto collapsed. (2) MONACO now RENDERS syntax-highlighted (def f / return 42, line numbers, Save btn) — FIX: SELF-HOSTED (npm build copies node_modules/monaco/min/vs -> dist/monaco/vs, server mounts /monaco, loader points at /monaco/vs; NO jsdelivr CDN). Verified served: /monaco/vs/loader.js 200. (3) STYLES clean (styled composers + blue Send on every agent window) — ROOT CAUSE was @assistant-ui/styles@0.3.7 version-MISMATCHED to @assistant-ui/react@0.11 so its BEM theme rendered the skeleton; FIX = DROPPED @assistant-ui/styles + reverted Thread.tsx to the custom .thread/.msg/.bubble/.composer classes + polished styles.css (css 107KB->10.8KB, 0 aui-thread-root served). HONEST: we are NOT using assistant-ui's styled theme anymore — it is polished CUSTOM CSS. If Isaac wants TRUE assistant-ui design later = separate task: align @assistant-ui package versions first. META: this round closed the verify-via-rendered-page gap — builder rendered headless chromium + screenshot (byte-identical to the deployed bundle by vite hash), I OPENED the PNGs myself. UNKNOWN: builder could not render INSIDE the container (no chromium in jobworld-cave base) so render was of the byte-identical local dist; final on-screen judgment = Isaac at localhost:3858.  tags:[Promptworld, V1, Frontend, Verified, Screenshot, Monaco, Terminal, Assistant_Ui]
