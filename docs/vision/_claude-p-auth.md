# vision: promptworld-system (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-01T14:31:38  FINDING: CRITICAL (proven, exact-symptom): claude -p bills API CREDIT not the SUBSCRIPTION whenever ANTHROPIC_API_KEY is set in env. Proof: 'claude -p ...' → 'Credit balance is too low'; 'env -u ANTHROPIC_API_KEY claude -p ...' → 'OK'. The OAuth subscription IS present (.credentials.json claudeAiOauth, billingType stripe_subscription) but ANTHROPIC_API_KEY (sk-ant-api03-...) in env overrides it onto API-key billing (depleted). NOT a code bug: the agent's p_main_agent.py is pure subprocess+claude CLI (no anthropic base SDK, no claude_agent_sdk — verified by grep). IMPLICATION (load-bearing for the whole *World pattern): every claude -p agent PromptWorld/JobWorld/HealthWorld spawn MUST run with ANTHROPIC_API_KEY scrubbed from the subprocess env (and likely ANTHROPIC_MODEL=claude-3-5-sonnet-latest too — the agent hit 404 model_not_found on that alias via the API key, needing explicit --model sonnet; with the subscription the default should resolve). The design premise 'runs IN mind_of_god, already claude login'd → agents authenticate via subscription' is FALSE until the API key is scrubbed per-subprocess. FIX = p_main_agent.subprocess.run must pass env without ANTHROPIC_API_KEY.  tags:[promptworld-system, claude-p-auth]
