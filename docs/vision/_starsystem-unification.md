# vision: compiler-agents (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-01T14:59:24  VISION: DESIGN LANDSCAPE (Isaac think-through, captured; NOT decided — open). COMPILER-AGENTS PER DOMAIN (the 'compiler agent' is the top agent of each system): OSMOSYS = healthworld compiler agent (health 'CEO', makes specialists='departments'); PRAXYS = jobworld compiler agent (TENTATIVE name; makes new jobworlds; inside a jobworld, CEOs make agents); GNOSYS = starsystem compiler agent (starsystem operates over DIRS, each dir = a PLANET — fits the metaphor); PromptWorld will have an ENGINEER agent 'CEO' that makes agents. TENSIONS Isaac raised (open): should jobworld CEOs talk to the engineer? ALL packages seem to have ALL these capabilities — maybe each just has its own agent-compiler skills, OR the skills are CANONICAL things each top-agent gets. 'promptworld is not the world compiler, although maybe it could be... maybe PRAXYS is the prompt agent and also the *World compiler. idk.' THE REAL QUESTION (verbatim): 'where do compiler compilers go and are they agents above the *Worlds or even the **things or just skills on the agents at the top of the *Worlds and **things?' (**things = anything like a *World; e.g. Jobworlds, Healthworld, Gameworlds.) IS '*World' EVEN REAL? Isaac: jobworld + healthworld have NO shared class; gameworld is a TOTALLY DIFFERENT pattern (different team pattern). 'i think maybe *World doesnt even exist maybe im making that up.' It is NOT the same as CAVE's World(). TENTATIVE DEFINITION (verbatim): 'a *World is a domain specific AI Operating System inside of some overall operations framework (like "my life" where i have to have a healthworld, jobworld, promptworld, gameworld).' Isaac: 'see im trying to cohere stuff because im scared.'  tags:[compiler-agents, world-pattern, starsystem-unification]
