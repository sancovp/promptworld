# vision: promptgym/world-context.md (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T17:57:14  FINDING: DEFERRED LIVE VERIFICATION PASSED (the one verification journal 2026-06-08T17:03:23 postponed: 'will verify via live CEO turn after deploy'). Ran the self-inspection question through the REAL loopback surface (docker exec promptworld-v1 curl 127.0.0.1:3858/api/chat {message:'How is PromptWorld built, and what is the nomicon? Inspect your own structure and show me.'}) on the live MiniMax-M3 container (up 11min, health 200, session cb6bb1d8). The CEO BEHAVIOR FIRED correctly: (1) it ACTUALLY inspected its own dirs — reply opens 'Benign persona/config file. I have enough now' (a real file read fired), then printed the true /app/promptworld/ tree, read promptgym/skill/CLAUDE.md, cited world-context.md:33-37/:55-60/:60-69/:65-69 WITH LINE NUMBERS; (2) explained the nomicon ladder component->AIOS->framework->fold-into-nomicon->app as real skills/CLIs; (3) World module (CEO+7 promptgym/<craft>/ specialists)+Gym module; (4) stated VERBATIM the caveat '*World is a CAVE-fork RESEMBLANCE, NOT a type and NOT a compiler... PromptWorld inspects itself, it doesn't generate other Worlds.' = EXACTLY Isaac's 2026-06-08T16:20:17 verbatim decision, demonstrated LIVE through the user surface. So nomicon item (3) self-inspection-as-example is now IS (live-verified), not just awareness-in-context. STOP-HOOK TENSION (surface to Isaac, do NOT auto-decide): the post-compact Stop hook reads the goal's 'folds on itself so PromptWorld can compile World apps' as REQUIRING a World-compiler and flags (3) unimplemented; but Isaac EXPLICITLY forbade building a World-compiler on 2026-06-08T16:20:17 ('DO NOT build a World type / World-compiler / arbitrary-dir->World factory'). The literal goal-text vs Isaac's verbatim clarification is a genuine fork = HIS call, not mine.  tags:[promptgym/world-context.md, agents/engineer-ceo.md, nomicon, self-inspection]
