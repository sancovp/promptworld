# vision: gym (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T17:11:17  FINDING: CORRECTION from Isaac 2026-06-08 (note for later, goal still active): the Gym module is NOT a layer for USERS to write/track specs + do research. WRONG. The Gym module is for AGENTS to do research on the things THEY make, and for USERS to TALK TO those agents under those circumstances — with dashboards and everything hooked up. So: agents (the specialists/CEO) run research on their own builds; the user converses with the agents in that research context; the UI is dashboards wired to that. What was built (a user-facing spec CRUD + /run) is the wrong shape. Do NOT rebuild now — finish the active goal (canonical container plugin registration -> README -> website -> github publish) first, then re-derive the Gym module to this corrected shape.  tags:[gym, promptworld, research-layer]
- [v2]  2026-06-08T17:23:07  OPEN: OPEN (needs Isaac): the corrected Gym shape (agents research the things THEY make + users talk to those agents + dashboards) overturns the only written spec (Promptworld_Gym_Module_Conceptual_Spec 15:59, which is the wrong user-driven spec-CRUD frame). The unresolved fork: WHAT TRIGGERS an agent research run (does a specialist auto-research a component right after building it — a test/measure/iterate loop?) and WHAT IS THE USER SURFACE (per-build dashboard + chat-with-that-agent-about-its-research, vs a cross-build research feed)? That choice determines the data model + page. Do NOT auto-decide. Meanwhile continuing to M2 README (unblocked).  tags:[gym, promptworld, open, architecture]
- [v3]  2026-06-08T17:28:51  DECISION: RESOLVED (Isaac verbatim 2026-06-08) — the OPEN gym fork is answered. A GYM = a system where you TALK TO any agent, and what you talk about is FILES describing things you want them to use their SPECIALIZATION to achieve. Flow: you spec 'I want this to exist OR know why it can't yet' (e.g. 'make a skill that lets an AI agent make a realistic 3D model from scratch') -> the agent (which KNOWS to research because of its gym context) researches it IN A LOOP FOREVER by making+firing a SUBAGENT or system that reviews whether it works (empirical) -> produces actual RESEARCH PAPERS about the insights + what gets empirically proven or not and why -> produces CORPUS. It is a CRON system: you fill TEMPLATES about WHAT the agent should research and HOW LONG/WHEN it fires (the cron). The DASHBOARD is for YOUR SPECS: CRUD them, PUBLISH them, work on their PAPERS. CAVEAT: teams DON'T work in gym (a team must be run by the researcher) unless we build a remote system -> put a caveat on the 'team' craft's gym. RECONCILE w/ what's built: keep Spec+CRUD+status (server/gym_specs.py, /api/gym-specs, GymPage); ADD: assigned-agent + cron schedule (reuse promptworld_automations/CAVE-SDNA cron + the Crons page) + the autonomous research-LOOP (cron fires -> run specialist w/ gym context -> it spawns a review subagent -> appends observations + writes a research PAPER) + papers/corpus store + talk-to-agent-about-a-spec + publish. Teams craft flagged.  tags:[gym, promptworld, spec, cron, research, resolved]
- [v4]  2026-06-08T17:32:17  INTENT: M5 BUILD PLAN (braid). Reuse: gym_specs.py (Spec store), /api/gym-specs* routes, promptworld_automations.py (fire_agent_turn loopback + schema_dict/register_automation/set_enabled/fire_now — the CRON engine ALREADY EXISTS), _loopback_url, run_gym/pw.chat. ADD, staged: (A backend store) gym_specs schema += assigned_agent + schedule + published + papers[]{id,ts,title,body,published}; funcs add_paper/update_paper/set_published; create/update accept assigned_agent+schedule. (B backend research+cron) POST /api/gym-specs/{id}/research = build research prompt (gym-context CoR + spec body) -> dispatch assigned_agent (run_gym, or chat for ceo) -> record full reply as a PAPER + 1-line observation; POST /api/gym-specs/{id}/schedule = register a CronAutomation via promptworld_automations whose loopback url = /api/gym-specs/{id}/research, alias=assigned_agent, schedule=spec.schedule (reject if assigned_agent=='team' — TEAMS CAVEAT: a team must be run by the researcher); reuse automations enable/disable/fire. (C gym-context) a constant CoR injected into the research prompt: 'research whether this can exist or WHY NOT; make+fire a review subagent that empirically tests; record what is proven/not and WHY; write a markdown research paper.' (D frontend) GymPage: assigned-agent picker + schedule control (start/stop research loop) + papers list+viewer + talk-to-agent-about-spec + publish + team caveat note. Dispatch backend builder (A+B+C, coherent) -> verify E2E -> frontend builder (D) -> render-verify -> deploy -> E2E the full loop -> commit/push.  tags:[gym, build, braid, cron, research]
- [v5]  2026-06-08T17:48:54  FINDING: M5 GYM BUILD — verification result (honest provenance). BACKEND RESEARCH LOOP PROVEN E2E through the LIVE /api on :3858 (the proof obligation): (1) create spec w/ assigned_agent+schedule+published+papers ✓; (2) POST /schedule registers a real CAVE CronAutomation gym-{id} (every:120, alias=skill) that appears in /api/automations ✓; (3) /schedule/stop removes it + clears schedule ✓; (4) TEAMS CAVEAT: schedule team-spec -> HTTP 400, research team-spec -> skipped ✓; (5) POST /research on the skill spec ran a LIVE agent turn that DISPATCHED A UNIVERSAL-WORKER SUBAGENT to empirically test building a 3D model and recorded a real markdown RESEARCH PAPER ('# Research Paper... ## Method: Dispatched a universal-worker subagent to empirically test...') + observation onto the spec ✓ = the agent-researches-its-own-builds loop WORKS. FRONTEND: vite build clean (TS typecheck, 4985 modules), all wiring present (researchNow/scheduleSpec/stopSchedule/addPaper/updatePaper/publishSpec + Research-loop card + Papers + Research-now + team caveat + assigned_agent + publish). VISUAL RENDER: env-blocked (no chromium in container; netns blocks reaching :3858 from mind_of_god; static-mock dist crashes on MainPage Monaco/file-tree deps before Gym mounts) — documented honest constraint; the live pixel view is on :3858 for Isaac's eyes. STATUS: backend = VERIFIED IS; frontend = BUILT+DEPLOYED, visual-unconfirmed-in-env.  tags:[gym, verify, e2e, research-loop]
