# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T18:15:49  FINDING: INVESTIGATION (read-to-closure, my crux re-verified): how CEO controls PromptGym agents + gallery. CRITICAL: 'CAVE teams' Isaac means = a SEPARATE pip package application/cave-teams/cave_teams/ (claude-p NATIVE: primitives.py:91 run_opus, leader.py:129 TeamLeader, harness.py file-JSONL routing) — DISTINCT from the 'cave' package CAVEAgent registry (cave_agent.py:370-398 factory types=code/chat/claw/service/remote, NO claude-p branch; minimax/heaven only via spawn_remote mixins/remote_agent.py:51 gated config.sdna_enabled). VERIFIED MYSELF: cave_teams primitives.py:85-87 _run_claude_p = subprocess.run(capture_output=True) BUFFERED + :106 --system-prompt REPLACE; grep config_dir/CLAUDE_CONFIG_DIR/on_event in primitives.py = ZERO. So (A) use-cave_teams-as-is DOES NOT FIT our PromptGym agents: (1) cave_teams has NO config_dir AIOS-dir scoping (promptgym.py:67 needs config_dir=d so the DIRECTORY codes the agent); (2) --system-prompt REPLACE vs our --append-system-prompt+--resume; (3) buffered run, NO on_event → cannot live-stream the gallery (our p_main_agent _run_turn Popen line-by-line on_event → broadcast_event → /ws). RECOMMENDATION: (C) NOW = a PromptWorld route/MCP tool → pw.gym.run(type,req,on_event=lambda ev: broadcast_event(type,ev)) (mirrors compile_prompt route promptworld_server.py:198; reuses built+streaming machinery; tradeoff: no shared task-list/SOP). (B) WHEN true cave_teams coordination wanted = register PromptGym agents as duck-typed cave_teams Conversation adapters whose .send (contract harness.py:96 → conversation.py:45 .send(content)->result) calls promptgym.run(on_event=broadcast_event) instead of buffered run_opus → gets cave_teams Harness bus + live gallery; cost = adapter + watcher. Findings /tmp/cave_teams_for_claudep_ceo_control.md. UNKNOWN: no runtime E2E run of promptgym.run/cave_teams leader; frontend /ws consumer (gallery per-window) not read; whether a gym MCP tool already exists (no /api/gym route today). NOTE bug: cave/core/remote_agent.py:39 uses Path/os never imported → NameError on lazy spawn_remote import. NO BUILD this pass (per team-lead; Isaac picks A/B/C).  tags:[Promptworld, Ceo_Control, Cave_Teams, Promptgym, Gallery]
- [v2]  2026-06-07T18:18:40  FINDING: INDEPENDENT CRUX VERIFICATION (my own reads, base/sdna/sdna): SDNA headless claude = GOAL-LEVEL. runner.py:47-88 agent_step builds prompt = config.resolve_goal(merged_inputs) (:88); config.py:106 goal:str templated; NO per-turn user-message param (variable_inputs only interpolate the goal). BUT chat primitives EXIST: config.py:150-152 resume/fork_session/continue_conversation; to_sdk_options :224-245 sets resume/fork/continue + cwd(:244) + env(:245 → CLAUDE_CONFIG_DIR expressible); agent_step has on_message(:50,103) + returns session_id(:110-113). So a CHAT loop is CONSTRUCTIBLE on agent_step (goal:=this-turn message, resume:=sid) but there is NO first-class chat API. The only named 'chat' surface = ClaudeCodeSession (claude_code_commands.py:34) is TMUX (send-keys/capture-pane, compact=/compact via tmux), NOT headless. PARITY flags: (a) SDNA on_message yields claude_agent_sdk MESSAGE OBJECTS, not the CLI raw stream-json NDJSON our gallery eventMapping.ts consumes → swap needs event-shape re-map; (b) compact()-as-/compact-turn is a CLI feature; SDK replaces with PreCompact hook (not a user turn); (c) config_dir AIOS scoping IS expressible via to_sdk_options env+cwd; (d) resume/session parity OK. LEANS verdict (b): our ClaudePMainAgent (CLI subprocess, send/Enter/--resume/on_event-NDJSON/config_dir) IS the chat-level claude runner SDNA lacks headless; adding it to SDNA = wrap ClaudePMainAgent OR build a thin chat loop over agent_step (goal:=msg+resume) + swap on_event shape. Background agent completing full read (heaven_runner chat?, CAVE call-on-agent site, DUOAgent) → consolidated report next. NO BUILD.  tags:[Promptworld, Ceo_Control, Sdna, Chat_Vs_Goal, Parity]
- [v3]  2026-06-07T18:22:23  FINDING: CONSOLIDATED + RE-VERIFIED (my own reads on the 2 cruxes). (1) CAVE does NOT require SDNA: cave/core/agent.py:355-382 Agent.run delegates to self._runtime duck-type (.run(content) else .handle_message else callable else no-op); _create_agent_from_entry sets NO runtime (cave_agent.py:362-426) until set_runtime; only SDNA-hard path = spawn_remote gated config.sdna_enabled. Raw ClaudePMainAgent (send_and_wait, no .run) doesn't satisfy as-is but a ~3-line adapter (.run(content)=pmain.send_and_wait(content,on_event=broadcast_event)) does. Note agent.py:388-393 routes the agent's RETURN to central_channel deliverable channels = a DIFFERENT path than /ws, so the live gallery rides our on_event bridge, not CAVE's channel. (2) SDNA = GOAL-LEVEL ONLY (verified runner.py:88 resolve_goal both claude + heaven_runner.py:201; no per-turn-message-into-resumed-session API; ClaudeCodeSession=tmux). Our ClaudePMainAgent IS the missing headless chat-level claude runner. Parity: SDNA-claude has streaming(on_message)/cwd+env(config_dir expressible)/resume+session_id; LACKS append-system-prompt(replace default config.py:233), /compact-as-turn (CLI-only), named ConvoRegistry (only /tmp/current_claude_session_id global). VERDICT (to Isaac): (C) ship now = /api/gym route → promptgym.run(type,req,on_event=broadcast_event) [lightest, reuses built streaming]; (B) cheap when cave_teams coordination wanted = duck-type adapter wrapping ClaudePMainAgent onto CAVE agent.run + keep on_event→/ws gallery; (D) only as deliberate investment = add ~150-250 LOC chat-level claude runner to SDNA (keep CLI core to retain /compact) + ConvoRegistry port. (A) cave_teams-as-is still doesn't fit. Findings /tmp/cave_teams_for_claudep_ceo_control.md (323 lines). UNKNOWN: whether claude_agent_sdk query() forwards options.env(CLAUDE_CONFIG_DIR) to the child (SDK transport not read); heaven hermes_step internals; SDK stream cadence. NO BUILD — Isaac picks.  tags:[Promptworld, Ceo_Control, Cave_Teams, Sdna, Verdict]
- [v4]  2026-06-07T18:35:55  FINDING: BUILT + E2E-VERIFIED option (C): CEO dispatches PromptGym specialists + gallery (no SDNA, no cave_teams). server/gym_mcp.py = thin stdio FastMCP, tool run_gym_agent(component_type,request) HTTP-POSTs /api/gym/{type}. promptworld_agent: self.gym=PromptGym(); _write_gym_mcp_config(port) writes .promptgym_mcp.json (PROMPTWORLD_PORT baked) giving CEO the tool via mcp_config + dispatch instruction appended to system prompt; run_gym(type,req)=gym.run(type,req,on_event=broadcast_event(type)) IN-PROCESS so the specialist stream hits /ws tagged by type. promptworld_server: POST /api/gym/{ctype}. p_main_agent: ClaudePMainAgent mcp_config→--mcp-config. CRITICAL FIX: --mcp-config is VARIADIC (<configs...>) and SWALLOWED the trailing positional prompt (error: 'MCP config file not found: .../Call your run_gym_agent...'); FIX = feed prompt via STDIN not argv (verified echo|claude -p stream-json returns reply; affects all _run_turn agents, verified via both CEO+gym paths). E2E /tmp/test_ceo_controls_gym.py PASS: /ws shows BOTH 'ceo' AND 'skill' streams (skill init/assistant/result tagged skill = gallery, distinct alias) + CEO reply relays skill specialist verbatim ('I build Skills: a SKILL.md ... and resources/'). Nested claude -p works: server→claude(CEO)→gym_mcp→HTTP→/api/gym→claude(skill). Commit d6451ee. CEO is NOT config_dir-scoped (inherits user ~/.claude, omniscient per D2) — only gym specialists are AIOS-scoped. NEXT (per team-lead): cave_teams adapter (B), gallery frontend (per-window UI). compact()-via-stdin not re-tested this slice.  tags:[Promptworld, Ceo_Control, Promptgym, Gallery, Mcp]
- [v5]  2026-06-07T20:30:15  FINDING: LOAD-BEARING FINDING (pw-builder investigated, I confirmed from the Agent-tool schema myself): claude-code-NATIVE Task/TeamCreate CANNOT run a teammate scoped to a different CLAUDE_CONFIG_DIR. The Agent tool params = subagent_type/name/team_name/model/isolation(worktree)/run_in_background — NO config_dir/cwd; native subagent .md frontmatter = name/description/tools/model — NO cwd. A Task subagent runs AS the parent CEO's claude (CEO config/tools), not a separate claude -p. The ONLY way to run an AIOS-dir specialist = PromptGym.run(type) = a claude -p with config_dir->CLAUDE_CONFIG_DIR (a separate scoped process). So the LOCKED AIOS-dir model (directory-codes-the-agent) and pure-native-subagents (persona-.md-in-parent) are MUTUALLY EXCLUSIVE: the CEO can only reach scoped specialists through a BRIDGE. 3 options: A native Task + 7 thin proxy subagents in promptworld_dir/.claude/agents/{type}.md that shell to the already-kept POST /api/gym/{type} (-> run_gym -> scoped gym.run) = keeps AIOS-dir scoping + per-agent doc-mirror/universal-worker + free gallery; the only non-pure-native bit = the proxy's one shell line (RECOMMENDED). B pure-native persona subagents (copy CLAUDE.md into CEO config) = ABANDONS the AIOS-dir model = exactly the departments-as-personas Isaac REVERTED (ba00fca) = NO. C native TeamCreate = same scoping limit + overkill; real scoped version = cave_teams Conversation.send->promptgym.run = MVP2. FORK FOR ISAAC: A's thin Task->/api/gym bridge vs his 'no custom mechanism' intent — A is the only option preserving the locked model; B/C only look more-native by discarding scoping. Builder HOLDING pending Isaac's pick. Cites: promptgym.py agent/run, p_main_agent.py:275-279, promptworld_server.py:206-217, Agent-tool schema.  tags:[Promptworld, Mvp1, Ceo_Control, Native_Teams, Aios, Open]
