# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T23:35:06  DECISION: MY OPERATING MODEL — CORRECTED (Isaac, verbatim, furious I had not internalized it). My ENTIRE job = USE CLAUDE CODE RIGHT: as I LEARN things, I WRITE the rules, skills, and vision-journal entries that make the dev-system (the AIOS) organically come into existence. The MAIN dispatch should be just 'IMPLEMENT {READY VISIONS} FOR {REPO}' — PERIOD. I should be: writing RULES (to guide the agent + prevent the mistakes that keep recurring), writing SKILLS (to guarantee the agent receives the right prompt for xyz when needed), writing VISION journal entries, and CALLING THE AGENT OVER AND OVER until it is all correct — the rules/skills doing the guidance, not giant ad-hoc per-dispatch prompts. 'What do you think the skills you keep being given are for!?' — doc-mirror / doc-mirror-prompts / make-skill ARE this system; USE them. MY FAILURE tonight: ad-hoc hand-writing huge bespoke prompts each round instead of building the rule/skill layer, so the same mistakes recurred (hand-rolling vs library-max, bundle-grep vs render, minimal vs maximum). FIX: tonight's lessons are now PROJECT RULES at application/promptworld/.claude/rules/ (use-libraries-at-maximum-never-hand-roll, verify-frontend-by-rendering-not-bundle-grep, porting-heaven-components-rewire-fileservice-to-api-files). Going forward: simple 'implement ready visions' dispatch + the agent reads the project rules + I refire until correct + I harvest each new lesson into a new rule/skill on sight.  tags:[Promptworld, Commander, Rules, Skills, Vision, Aios, Operating_Model]
- [v2]  2026-06-07T23:55:56  VISION: EDITOR FUTURE DIRECTION (Isaac, reflective VISION — capture, not asserted). We need the ENTIRE Monaco system (not a stub editor) — the full Monaco ecosystem: multi-model/URI document system, language services/LSP, the diff editor, commands, decorations, custom inline widgets/zones, code lens, the full editor API — AND THEN we add a bunch of CUSTOM stuff INSIDE it. The heaven file-editor.tsx port (now in progress) is the FOUNDATION (the real workbench: tree+tabs+monaco+new/delete/save over /api/files); on top of that real-Monaco foundation we extend with custom features INSIDE the editor (candidate directions, NOT yet specced: inline agent actions / code-lens that dispatch a specialist, custom decorations + widgets, diff views of agent edits, custom panels/views, command palette entries that call promptworld agents, etc.). So the editor is not just a file viewer — it becomes the surface where user + agents co-edit, with promptworld-specific custom Monaco extensions layered in. IS (now): the heaven workbench port (real Monaco, real file ops). VISION (future): the FULL Monaco capability surface + the custom promptworld extensions inside it. PROOF/SCOPE: this rides ON the heaven port landing first (the real Monaco foundation); the custom-inside-it features are a separate future build once the foundation is solid. Marked future-horizon, not this round.  tags:[Promptworld, Monaco, Editor, Custom_Extensions, Vision]
