# vision: promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T18:26:27  INTENT: ISAAC FINAL FRAMING + GREENLIGHT (verbatim, 2026-06-08 'ok i'll let you work now. Go.'): 'this sort of makes promptworld the META-COMPILER of all these things weve made, and each one has its own specialized meta compiler (jobworld can make new jobworlds, gameworld can make new gameworlds, maybe healthworld cant idk) -- so let me correct: SOME of them have their own specialized meta compilers. But just remember PromptWorld is the one that treats them ALL as PATTERNS now and is really the META FOR ALL OF MY PATTERNS and stuff, so we can just give people promptworld and we can start adding COURSES and everything into it directly. its like heres what/how, heres the program, heres the research system, its also claude code so use it however you want, its also usable remotely INSIDE your own claude code... its everything i know how to do for free basically rolled into this system that just installs super easy from claude code if youre already on it.' MY INTENT (MILESTONE 6, now authorized): author the META-COMPILER SKILL = the *World-patterns skill Isaac specified — teaches the unified model (a *World = a sim{team-leader,specialists,domain,driver}) + the 4 named patterns (verbatim defs) + points at the existing per-World generators/examples (gameworld-generator real; jobworld/healthworld instantiate.sh; the app-rung ingest-into-nomicon-app; the nomicon ladder) + the objective (reuse a pattern OR combine-knowledge to synthesize new; PromptWorld treats them all as patterns = the meta). Commander discipline: I author the skill (steering layer = my job); I VERIFY via a live CEO turn (does the WrightMaster/CEO carry+use it). FIRST: ground the wiring — how a promptworld-local skill (application/promptworld/skills/, like ingest-into-nomicon-app) actually reaches the CEO's config (read _provision_ceo_aios) so the new skill is genuinely carried. Scope guard: M6 = the patterns+examples skill carried+verified. DEFERRED follow-on milestones (Isaac 'we'll get into it'): hook up CEO-runs-team, rename CEO->WrightMaster, courses-into-promptworld, the general *World generator ('something new').  tags:[promptworld, meta-compiler, world-patterns, wrightmaster, courses, nomicon]
