# vision: Claude_Code_Sdk (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T00:20:07  FINDING: DECISIVE (I ran claude -p --output-format stream-json --verbose live; the event stream proves it). claude -p emits a LIVE TYPED EVENT STREAM: system/init, system/thinking_tokens, assistant(text), assistant(tool_use->Bash), user(tool_result), result/success, rate_limit_event — AND system/hook_started + system/hook_response = HOOKS SURFACE INLINE IN THE SAME STREAM. So: (1) Isaac's 'you CANT get callback events from claude code, you need hooks' is FALSE-as-stated — you CAN: the stream IS callback events. (2) His deeper instinct is RIGHT in a better form: hooks (paia/cave) ARE the richer lifecycle events, and they appear AS events IN the -p stream — so cave-hooks + the SDK message stream = ONE unified event feed your custom UI renders. That is WHY cave matters (the hooks), NOT tmux. THE CORE FEAR ANSWERED ('we'll lose capabilities by not using tmux') = NO. claude -p IS Claude Code (same binary/subscription/tools/skills/MCPs/hooks/teams/sessions/resume) — headless = NO TERMINAL, not fewer capabilities. PROVEN this session: subscription works headless (API-key-scrub), team tools present (TeamCreate/Task/SendMessage), config/skills/MCP isolation via CLAUDE_CONFIG_DIR, now the event+hook stream. The ORIGINAL reason for tmux ('thought you couldn't use the subscription outside claude code') is DEAD — -p uses the subscription. What tmux gave = a terminal a human watches/types in = exactly what a custom UI REPLACES on purpose. Visibility headless = STRUCTURED EVENTS (richer than scraping a tmux pane). ONE honest re-home (not a loss): interactive mid-turn approval — use --permission-mode bypassPermissions, OR wire the SDK canUseTool permission callback into the UI for approve/deny; interactive-only slash-commands mostly replaced by skills. ISAAC'S TEAM REALIZATION (sound): cave ALREADY has the team primitive — agents with INBOXES + HEARTBEATS + conditional wake-on-inbox-message + routing (verified via jobworld: cave_agents/agent_registry/message_router/CentralChannel/inbox poll loops). A 'team' = shared task-list + inboxes + messaging = the SAME as cave PARTIES (the inbox/heartbeat fabric). So you don't NEED claude-code TeamCreate for coordination; cave parties do it AND are observable through your server/UI. (CAVEAT: the inbox/channel/heartbeat FABRIC is built+verified; 'parties' as a named higher construct = likely the planned name over that fabric, built-vs-designed not yet confirmed — check.) NET ARCHITECTURE (no split): run agents HEADLESS via claude -p (= full Claude Code) inside your own frontend (promptworld) that renders the event+hook stream; coordinate multiple agents via cave's inbox/heartbeat/parties fabric; NO tmux anywhere; nothing lost — terminal replaced by UI, visibility richer. The 'too many ways' feeling collapses: tmux was never a real alternative, it was a workaround for a constraint (subscription-only-in-CC) that no longer exists.  tags:[Claude_Code_Sdk, Tmux, Events, Hooks, Cave, Teams, Parties, Promptworld]
- [v2]  2026-06-07T14:45:16  INTENT: DISPATCHED v1 build on the team (realigned to Isaac's process: TeamCreate worker reads a PROMPT-SKILL, not a sidecar wall-of-text). Authored reusable prompt-skill engineering/build/additive-module-change-with-e2e (golden:false, fresh) — additive change + reversible branch + E2E-through-real-surface + honest verdict. Worker 'builder'@background-streams dispatched with specifics: refactor application/promptworld/p_main_agent.py ClaudePMainAgent to stream-json --verbose + an on_event(ev) real-time callback (the seam CAVE/UI wires into) + a compact() method (sends literal '/compact' turn, returns True on compact_boundary), PRESERVING the CodeAgent surface (send_keys/Enter/capture_pane/send_and_wait) + registry + API-key scrub byte-for-byte; branch promptworld-event-stream; E2E = /tmp/test_pmain_stream.py forcing a tool call, must show intermediate tool_use+tool_result events stream live + returned text contains STREAMTEST + compact_boundary on compact(). I will VERIFY the artifact myself (read diff + re-run E2E) — report is a claim. PROCESS CORRECTION (honesty): the prior-session teammates were NOT dead as I earlier told Isaac — coherer/saas-mapper/ca-ts/prover were ALL still alive (orphaned from session 7c541898 but running). Per Isaac: shut down ca-ts+coherer (done, SIGTERM, confirmed gone), kept saas-mapper+prover (might need later). COMPACT FLOW confirmed w/ Isaac: agent calls a self_compact tool/skill (REQUEST) -> wrapper detects (PostToolUse hook or stream tool_use) -> wrapper calls compact() which sends '/compact' (ACTUATE) -> compact_boundary observed; the existing self_compact script is tmux (dead) — make a thin headless signal tool in v1.1.  tags:[Promptworld, Claude_Code_Sdk, Events, Compact, Team, Prompt_Skill]
- [v3]  2026-06-07T14:50:24  FINDING: v1 VERIFIED BY ME (re-ran E2E, read commit) — ClaudePMainAgent now streams claude -p events live via on_event(ev) + has compact() (sends /compact, detects compact_boundary). commit 278811f on branch promptworld-event-stream (+168/-37), py_compile OK. My re-run: intermediate tool_use+tool_result stream live, STREAMTEST round-trips, compact_boundary observed. Worker folded a pre-existing broken uncommitted state (_run_turn missing on_event param) into one working change. UNKNOWN (not exercised): timeout-kill + stderr-fallback paths. prompt-skill engineering/build/additive-module-change-with-e2e scored 1.00 runs:1 (first verified-good run). NEXT (Isaac roadmap): frontend = harvest jobworld's ink-ceo dashboard, REWIRED from tmux-capture to the promptworld event stream (on_event -> server /ws -> UI); then nomicon-into-promptworld -> per-component-type agents -> messaging system the CEO uses to call them -> the *Gym recursive-self-improvement-through-self-observation pattern -> pack into a claude plugin that launches the whole thing -> minimax scoring to complete SANCREV OPERA prototype.  tags:[Promptworld, Events, Compact, Verified, Frontend]
- [v4]  2026-06-07T15:31:35  DECISION: FRONTEND DECISION (Isaac): build promptworld's OWN frontend FOR ITSELF — do NOT recon/reuse SANCREV now (SANCREV is 'just a cave server like everything else', solves this eventually, but not the focus). USE assistant-ui (https://github.com/assistant-ui/assistant-ui) as the chat-UI base — do NOT rebuild a chat UI from scratch. assistant-ui = React/TS component library with a RUNTIME ABSTRACTION (AssistantRuntimeProvider + a custom runtime: useExternalStoreRuntime or @assistant-ui/react-data-stream) that connects to YOUR streaming backend; renders streaming text AND tool calls natively (Generative UI) — matches the -p event shape (assistant/tool_use/tool_result) exactly; primitives <Thread/> <Message/> <Composer/>. PLAN (verifiable slices): SLICE 1 (server, headless-verifiable) = wire promptworld server's dead /ws to broadcast ClaudePMainAgent.on_event events live to connected clients + run a turn on input, returning final text; verify with a python ws client (POST a tool-forcing message, assert the ws client receives intermediate assistant/tool_use + user/tool_result + result live). SLICE 2 (frontend) = assistant-ui React app + a custom runtime (useExternalStoreRuntime) over that /ws (events->message store; composer input->POST); verify in browser. v1 ClaudePMainAgent on_event (commit 278811f) is the source; this is the OTHER half (output mirror -> a real UI). Dispatching SLICE 1 to builder@background-streams (reads engineering/build/additive-module-change-with-e2e prompt-skill). I verify artifacts myself.  tags:[Promptworld, Frontend, Assistant_Ui, Events, Websocket, Ceo]
- [v5]  2026-06-07T15:38:13  FINDING: FRONTEND SLICE 1 VERIFIED BY ME (re-ran the ws E2E + read the diff). promptworld server /ws now broadcasts the CEO's live claude -p event stream to connected clients. commit 167daf46 (branch promptworld-event-stream, +69/-16, server/promptworld_agent.py + server/promptworld_server.py). MY re-run: 14 live events over /ws, every tagged alias=ceo, intermediate assistant/tool_use + user/tool_result + result streamed real-time, POST /api/chat still returns final reply (WSTEST). ROOT-CAUSE the builder found+fixed: /ws DID register clients + had broadcast(), but broadcast() used asyncio.create_task -> 'no running event loop' from the SYNC /api/chat worker thread (on_event fires off-loop) -> nothing reached ws. FIX: capture the server loop on /ws connect (self._loop), broadcast via run_coroutine_threadsafe; broadcast_event(alias,ev) wraps {alias,event} so a UI can filter; chat() runs send_and_wait(on_event=lambda ev: broadcast_event('ceo',ev)) + returns final text directly. UNKNOWN/not-exercised: multi-client fan-out (1 tested), dead-socket-drop path, dept (compile_prompt) routes not live-streamed yet (out of slice scope). prompt-skill additive-module-change-with-e2e now 2/2 verified-good (approaching golden, need >=3). NEXT = SLICE 2: assistant-ui React app + useExternalStoreRuntime over /ws (events->store; composer->POST /api/chat). The MIRROR half (output) is now DONE end-to-end (agent stream -> socket); slice 2 puts a real UI on it; the INPUT/WAKE half (group-chat, app input -> notify main agent) is the later piece.  tags:[Promptworld, Frontend, Websocket, Events, Verified, Slice1]
