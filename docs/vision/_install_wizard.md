# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T20:56:56  INTENT: ISAAC: V1 is NOT complete until the install wizard + packaging is done = the deployable is the V1 finish line. DISPATCHED pw-builder to build the deployment package per the vision (complexity-ladder rungs 1->6->7, docs/vision/_docker.md/_docker_compose.md/_ship.md/_containers.md): (1) Dockerfile (FastAPI server + built SPA via npm run build in-layer + claude-code CLI + 7 AIOS dirs + CEO config provisioned at runtime + doc-mirror by default + own creds docker-cp'd not baked); (2) docker-compose (bind-mount host workspace -> /workspace, publish port; neo4j/CartON only if runtime needs it else DOCMIRROR_CARTON=0); (3) build/boot .sh (mind_of_god constraint: host docker socket, cannot -v mount mind_of_god paths -> create->docker cp creds+workspace->start; user-host path = docker compose up); (4) install/image-wizard SKILL on the plugin (application/promptworld/skills/install-promptworld) the user's main CC runs to boot the container. Reference = jobworld-cave:latest (proven pattern, ink-ceo served from inside). E2E = actually build+boot the image, curl the port for the SPA, /api/chat CEO reply + a specialist subagent dispatch FROM INSIDE the container, file API on /workspace; loop until it works. Builder writes vision as it goes; I re-verify + re-run the boot myself.  tags:[Promptworld, V1, Packaging, Docker, Install_Wizard, Ship]
- [v2]  2026-06-07T21:01:57  INTENT: Building the V1 deployment package (4 artifacts in application/promptworld/deploy/ + the install-wizard skill). Grounded in the proven references: base = jobworld-cave:latest (already has claude CLI + node20 + python3.11 + the cave/sdna/llegos/heaven stack proven to boot a CAVE server), refresh current cave+sdna over it (--no-deps, since promptworld forks current CAVEHTTPServer/CAVEAgent); lean container pattern from doc-mirror-system/container (non-root user, doc-mirror via install.sh, creds at runtime NOT baked/mounted). DECISION (CartON): DOCMIRROR_CARTON=0 (file-only journal) => NO neo4j service = simplest deployable; agents building components do not need the graph at runtime (cave_agent/config/http_server have ZERO neo4j refs; CAVEAgent boots without it). Dockerfile: FROM jobworld-cave -> refresh cave/sdna -> COPY promptworld -> npm ci && npm run build the SPA in-layer (drops the committed-dist dependency) -> COPY doc-mirror plugin + run install.sh as ceo (bins on /home/ceo/.local/bin + ~/.docmirror_plugin_root so _provision_aios/_provision_ceo_aios resolve doc-mirror in-container) -> env PROMPTWORLD_DIR=/app/promptworld, PROMPTWORLD_WORKSPACE=/workspace, DOCMIRROR_CARTON=0 -> entrypoint chowns docker-cp'd creds+workspace then su ceo + python -m server. build.sh stages a temp build context (promptworld + doc-mirror-plugin + cave + sdna) since the Dockerfile COPYs cross monorepo dirs; subcommands build|boot|e2e|stop. mind_of_god constraint (host docker socket, cannot -v mount mind_of_god paths): boot = docker create (-p, -e, no -v) -> docker cp creds + workspace -> docker start; E2E reaches the server via docker exec <c> curl localhost:PORT (published-port-on-host is unreachable from mind_of_god, but exec is + proves 'from inside the container'). Normal-host path = docker-compose.yml (image: promptworld:latest, bind-mount ./workspace:/workspace + ~/.claude creds :ro, publish port). install-promptworld SKILL = the rung-6 wizard the user's main CC runs: walk through build.sh build && build.sh boot, then open http://localhost:PORT + verify. E2E to prove: build image; boot via create->cp->start; docker exec curl / = SPA; POST /api/chat = real CEO reply in-container; CEO->skill-specialist native subagent writes a token file in promptgym/skill/ in-container; /api/files/tree operates on /workspace. Loop until it works.  tags:[Promptworld, V1, Packaging, Docker, Ship, Install_Wizard]
