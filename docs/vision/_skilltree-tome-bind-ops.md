# vision: skilltree-tome-bind-ops (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-07-10T01:38:00  DECISION: -m DECISION: ISAAC DIRECTIVE 2026-07-10 (verbatim): ok so we should update promptworld now i think right? since PW is supposed to have this whole system in it, and be our app whatever you call this at the top -- the Wizard lmao and now we know how we should theme it and give it roles etc -- not Wrights they are Wizards. INTERPRETATION GROUNDED IN THE RECORD: the geometry doc SkillWizard row already says PromptWorld = the first SkillWizard (its CEO builds the tome, train 52.1), and the task-14 journal parked the coupled nomicon rename unit until promptworld is next touched — this directive touches it, so the rename unit FIRES NOW. THE UPDATE SCOPE: (1) THEME: the seven specialist Wrights (Skillwright/Toolwright/Promptwright/Harnesswright/Teamwright/Flowwright/Systemwright) become WIZARDS; the app ITSELF is THE SkillWizard (order-1 = the tome as app, an agent designed to wield the SkillTome); the tome metaphor is native — SkillTome = the spellbook, frameworks = chapters (already canon), folding = inscribing. (2) THE SYSTEM IN IT: wire agent-skilltree 0.3.0 tome ops + the framework package + the skill2framework chain + the sancovp tome into PW (deps in the image, the tome surfaced to the agents). (3) THE COUPLED RENAME UNIT: fold-into-nomicon stage dir -> fold-into-tome, nomicon-atomize succession, promptworld nomicon references -> tome vocabulary (history/journal files stay unrewritten). Reading the full PW boundary before editing: README (read), world-context, engineer-ceo, the seven specialist CLAUDE.mds, global-context, promptworld_agent.py, agent_profiles.py, the two PW skills, the wrightmaster + nomicon vision files, deploy wiring. No backticks.  tags:[skilltree-tome-bind-ops]
