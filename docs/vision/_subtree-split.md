# vision: publishing (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T18:48:57  DECISION: DECISION (Isaac delegated the call: 'figure out the scalable publishing system that YOU understand and always follow' + frustrated this was an early vision point I treated as open). GROUNDED, NOT invented: the system is already specified across (a) ship-a-plugin skill (plugin = dir+plugin.json; marketplace = ONE catalog REFERENCING plugins in their OWN repos; public distro ⇒ public repo; bin/scripts need an install-WIZARD skill), (b) complexity-thinking-ladder rungs 6/7 + the install/image wizard skill, (c) install-promptworld (the worked-example deploy wizard — agent builds image+boots+cps creds+hands URL; EXISTS), (d) CartON Doc_Mirror_System_Publishing_Plugin_Self_Replicating_Base_2026_06_03T19_48_37 (the telos: a plugin for making plugins+publishing anything, bundles the compilers, forks+publishes ITSELF; engine=seed_v0 GitHub-Contents-API push). ENCODED as ~/.claude/rules/scalable-publishing-system.md (NUCLEAR, cross-project) = the ONE invariant pipeline I always follow: containerize (build.sh) -> wrap as a claude-code PLUGIN with an install-WIZARD skill (plugin->skill->script->URL) -> OWN PUBLIC repo via git subtree-split (monorepo stays source of truth, public repo is a projection) -> ONE sancovp marketplace catalog REFERENCES it -> SHIP the deps (cave/sdna/doc-mirror-plugin: build fetches from the public monorepo or published pkgs, never assume monorepo present) -> user: add marketplace, install, wizard builds+boots, URL. APPLICATION TO PROMPTWORLD (next): plugin.json wrapping install-promptworld + deploy/; subtree-split application/promptworld -> sancovp/promptworld (PUBLIC); marketplace entry; build.sh fetches the 3 deps from the public monorepo at a pinned ref. The full self-replicating base plugin + seed_v0 engine = the telos (bigger build, after).  tags:[publishing, ship-a-plugin, install-promptworld, marketplace, subtree-split, self-replicating-base-plugin, seed_v0]
- [v2]  2026-06-08T18:51:09  FINDING: PUBLISH BLOCKER GROUNDED (read deploy/build.sh): build.sh hard-codes MONO=/home/GOD/gnosys-plugin-v2 and stage_context() rsyncs promptworld + (further down) COPYs cave/ + sdna/ + doc-mirror-plugin/ FROM THE MONOREPO into the build context; Dockerfile then COPY+pip-installs them. So a standalone sancovp/promptworld clone CANNOT build without the monorepo present at that path. APPLYING the scalable-publishing-system rule to PromptWorld = the NEXT MILESTONE (M8), precisely scoped: (1) rework build.sh so the 3 deps are obtained WITHOUT the monorepo — fetch cave/sdna/doc-mirror-plugin from the PUBLIC monorepo at a pinned ref (sparse/partial clone in build.sh), keeping monorepo=source-of-truth; (2) add .claude-plugin/plugin.json wrapping the EXISTING install-promptworld wizard skill + deploy/; (3) git subtree split application/promptworld -> push to a NEW PUBLIC repo sancovp/promptworld (the one irreversible outward step — authorized by Isaac 'has to be its own public facing repository'); (4) ONE sancovp marketplace catalog (marketplace.json) REFERENCES sancovp/promptworld; (5) VERIFY: in a clean dir WITHOUT the monorepo, clone sancovp/promptworld + run the install-promptworld wizard / build.sh build+boot+e2e -> the container comes up. DONE THIS TURN (milestone seam, compacting): scalable-publishing-system RULE encoded (~/.claude/rules/), README de-staled (Gym shipped + compile-a-world) committed 83c1555 + main FF'd+pushed. NEXT TURN: execute M8 with full context.  tags:[promptworld, build.sh, cave, sdna, doc-mirror-plugin, subtree-split, publish-blocker]
- [v3]  2026-06-08T18:52:33  DECISION: ISAAC PROPOSAL (verbatim sense, 2026-06-08 — refines the scalable-publishing-system to a CI/CD mechanism): 'we could easily have a CICD setup where the monorepo has EVERYTHING ALL IN ONE for us, and then whenever stuff individually updates it pushes to the public if we want to. we had that set up before with heaven-bml! ... some kind of heaven-bml v2 setup: you push to monorepo which is PRIVATE with a specific TAG, that publishes the specific subdir on its specific public repo by commit-pushing it there via a workflow. then a SKILL for setting up these yamls inside the monorepo so github can read them.' MY ASSESSMENT (answer to his 'how is that?'): YES — this is the automated form of the rule's publish step (tag-triggered GitHub Actions workflow per subdir = mechanized subtree-split/mirror; better than manual for scalable+always-follow). TWO THINGS IT MUST ACCOUNT FOR: (1) MONOREPO VISIBILITY FLIP — his model makes the monorepo PRIVATE (it is currently PUBLIC per canonical-source-dirs: sancovp/sanctuary-revolution-alpha public/GPBL). Cleaner (everything-in-one stays private; only curated subdirs go public) but it IS a change with license/visibility implications = his decision to confirm. (2) THE DEP WRINKLE PERSISTS — CI publishing the promptworld subdir doesn't make it BUILD standalone; the published repo still needs cave/sdna/doc-mirror-plugin. CLEAN FIT: treat those as their OWN published subdirs (each its own public repo via the SAME workflow) and promptworld's build pulls them from their public repos (or pip). So the CI model + deps-as-published-units solves sync AND the standalone-build coherently. THE SKILL = a dev-flow that scaffolds the per-subdir publish workflow YAML + the push-secret/PAT wiring in the monorepo (fits every-build-ends-in-a-dev-flow-skill). GROUND FIRST: heaven-bml's actual setup exists on disk (/home/GOD/heaven_bml_sqlite/ per canonical-source-dirs) — read its real workflow shape before building 'heaven-bml v2', don't reinvent. NEXT (pending his monorepo-visibility confirm): update ~/.claude/rules/scalable-publishing-system.md to make tag-triggered CI the canonical publish mechanism; then this supersedes M8's manual subtree-split.  tags:[publishing, cicd, github-actions, monorepo, heaven-bml, subtree-split, per-subdir-publish, workflow-scaffold-skill]
