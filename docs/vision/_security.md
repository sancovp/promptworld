# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T16:45:05  FINDING: BUILT + E2E-VERIFIED slice 3: Monaco file-explorer + PATH-JAILED workspace file API. Backend server/file_api.py: jail_resolve resolves requested path under workspace root (PROMPTWORLD_WORKSPACE>ceo cwd>instance dir) and rejects anything whose symlink-resolved real path is not inside root — blocks ../, absolute, symlink-escape. Routes GET /api/files/tree (skips node_modules/.git/dist/__pycache__, >1MB, depth-cap), GET read (403/404/400), PUT write (jailed; broadcasts file_changed on /ws). Frontend: @monaco-editor/react via CDN loader (local full-bundle OOM'd at 3.7GB free), FileExplorer.tsx (tree+Monaco+Save+Ctrl/Cmd-S via content-ref), split layout; explorer own /ws filters {type:file_changed} (vs {alias,event}). E2E: (A) /tmp/test_promptworld_files.py ALL pass incl PATH-JAIL — ../../../etc/passwd, /etc/passwd, sub/../../etc/passwd all 403 no-leak; escaping + absolute writes 403 no-file-created; PUT fires file_changed. (B) vite build dist 325KB. (C) curl / = built SPA. (D) headless chromium: tree renders, open→Monaco MODEL shows content, external write→file_changed→tree refresh; save-via-editor bonus not asserted in headless (Monaco hidden-textarea click timed out; write path proven by A). Commit 32c7b08. SKIPPED watchdog observer (not installed, no silent dep). dist force-committed for image-readiness. Visual=UNKNOWN pending Isaac. GOTCHAS: Monaco renders spaces as \xa0 (assert via model value not view-lines text); local monaco bundle OOMs build at low mem → CDN loader.  tags:[Promptworld, Frontend, File_Explorer, File_Api, Security]
- [v2]  2026-06-07T16:47:11  FINDING: FRONTEND SLICE 3 VERIFIED BY ME (security boundary read + attacks re-run + render seen). commit 32c7b088 (branch promptworld-event-stream, +552/-63; new server/file_api.py + 5 frontend files). PART A: path-jailed file API (server/file_api.py) — GET /api/files/tree (skips node_modules/.git/dist/__pycache__, >1MB, depth-capped, skips symlinks), GET read (403 jail/404/400 binary|oversize), PUT write (jailed, broadcasts {type:file_changed,path} on /ws). SECURITY VERIFIED: I READ jail_resolve (file_api.py:42-62) — resolves candidate (root/rel; absolute rel resets to absolute -> caught) then real==root OR root in real.parents (Path.parents membership, NOT string startswith -> no /home/work vs /home/work-evil prefix bug; resolve() follows symlinks so escapes caught). I RE-RAN /tmp/test_promptworld_files.py: all 5 attacks (../traversal x2, /etc/passwd, ../write-escape, absolute-write) -> 403, leaked_passwd=False, created_outside=False; jail_all_rejected+jail_no_leak True. PART B: Monaco panel (frontend, @monaco-editor/react via CDN loader — local bundle OOM-killed vite build at ~3.7GB free RAM; CDN keeps bundle 325KB, Monaco loads at runtime; flag: offline needs CDN or later self-host). Split layout: chat | file-tree+Monaco; opens own /ws, refreshes on file_changed (distinguished from {alias,event} by shape); 'changed on disk' badge if open file dirty. SCREENSHOT /tmp/promptworld_monaco.png CONFIRMED: 3-pane render (chat / Workspace tree sub-code.py+external_new.txt+hello.txt / Monaco showing 'hello world from monaco' + Save). UNKNOWN: save-via-EDITOR in headless (Monaco hidden-textarea click timed out — interaction nuance; the WRITE PATH is proven by test A). SKIPPED (no silent dep): watchdog observer (Part A #6) — not installed; API writes broadcast file_changed, agent's out-of-band edits surface on next tree refresh (add watchdog later for auto-refresh on agent edits). dist force-committed again (recommend drop + build in image layer). PROMPTWORLD CORE SURFACE NOW COMPLETE: talk to the CEO (live /ws stream + tool calls) + see/edit the workspace files (Monaco, path-jailed, live-refresh). NEXT THREADS: CAVE-inbox group-chat (input/wake half), image/docker-compose packaging, optional watchdog, the 6 other departments, nomicon-ladder confirm.  tags:[Promptworld, Monaco, File_Api, Path_Jail, Security, Verified, Slice3]
- [v3]  2026-06-08T19:21:19  FINDING: PUB-1 DONE + VERIFIED BY ME (step 1 of the goal). Scrubbed the inline fine-grained PAT (github_pat_1...) from /tmp/heaven-bml-public/.git/config: git remote set-url origin + --push origin -> clean https://github.com/sancovp/heaven-bml.git. VERIFIED via raw config read: url + pushurl both clean, grep -c github_pat_ = 0, grep -cE inline-cred = 0. (An intermediate masked origin	https://@github.com/sancovp/sanctuary-revolution-alpha.git (fetch)
origin	https://@github.com/sancovp/sanctuary-revolution-alpha.git (push) showed a stale '@' = display artifact; the raw .git/config is the ground truth = clean.) REMAINING SECURITY ACTION = ISAAC's (I cannot): the token is still VALID on GitHub (only removed from this on-disk config) -> ROTATE/REVOKE the github_pat_1... fine-grained PAT in GitHub settings. NEXT: PUB-2 heaven-bml-v2 publish core — first READ heaven-bml's real public-release.yml + generate_ecosystem_readme.py IN FULL (only saw structure so far) to ground the parameterized v2.  tags:[pat-scrub, security, heaven-bml, rotate-token, PUB-1]
