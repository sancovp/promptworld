# vision: Team_Not_Sidecar (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-08T01:05:13  DECISION: ISAAC CORRECTION (team mechanism): when rotating builders, the new builder MUST be ADDED INTO the existing team as a teammate (Agent tool with team_name=background-streams + a new name), NOT spawned as a standalone background Agent() sidecar and NOT in a new team. A standalone builder cannot be paused, cannot be rerun, has NO memory across rotation, and is not an addressable/continuable teammate. My mistake: I had been spawning builders as standalone background streams. FORWARD RULE (= team-means-teamcreate-not-sidecar applied to builder rotation): let the current standalone builder FINISH (never interrupt it — it has no memory), THEN add the next builder INTO background-streams via TeamCreate-style membership, and drive all subsequent build rounds through that teammate. background-streams is THIS session's team (I am lead, leadSessionId 7c541898...); pw-builder3 finished + was shut down.  tags:[Team_Not_Sidecar, Builder_Rotation, Operating_Model]
