# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-06T22:23:33  DECISION: ISAAC DECISION (verbatim intent): out of everything in flight, PROMPTWORLD is THE priority, and that INCLUDES the nomicon system; THEN we revisit what to do. The deeper why: Isaac is trying to make what we've built LEGIBLE to Claude Code users at the same time as fixing it — promptworld (a coding-agent chat UI on claude -p) is the vehicle for both. DESIGN PREFERENCE (his, for the SDNA<->claude interface): SDNA should NOT pass claude SDK objects UP and force us to keep parity with the heaven interface; instead PASS A DICT DOWN and unpack into claude-code — then never maintain parity. Then: make skills for programming with SDNA, give them to SOPHIA -> SOPHIA becomes important again, SOPHIA becomes like PromptWorld. OPEN questions he wants TESTED by subagents (not assumed): (1) does the SDNA claude -p path accurately handle TRANSCRIPTS with the utils, or not; (2) is there a cron in SDNA, or is cron in CAVE — and if CAVE, is it importable WITHOUT booting the full CAVE HTTP server; (3) what are slinky v2 + the ContextEngineering lib in SDNA, and how do they bear on maximizing Chain + Compiler (and a Compoctopus that runs only on the Claude Code SDK); (4) does promptworld work with a TEAM / can teams be called via claude -p / do the previously-built CAVE teams work. Launching a 4-agent testing team to answer these; promptworld+nomicon execution waits on their reports.  tags:[Promptworld, Nomicon, Sdna, Sophia, Legibility]
- [v2]  2026-06-06T22:41:00  FINDING: PROMPTWORLD STATUS MAP (subagent /tmp/promptworld_status_map.md; load-bearing claims re-verified by me). RUNNABLE TODAY (3): (1) CEO chat over claude -p — POST /api/chat (server:94) -> chat() (promptworld_agent.py:141) -> ClaudePMainAgent; subscription-billed (ANTHROPIC_API_KEY scrub p_main_agent.py:182), --session-id/--resume, NO tmux = most-proven piece; (2) convo registry alias<->session_id (ConvoRegistry, .promptworld/convos.json has ceo+prompt-engineer); (3) ONE department 'prompt' (IMPLEMENTED_DEPARTMENTS={'prompt'} server/promptworld_agent.py:61) compiling real prompt .md artifacts to disk. On a CAVE-fork server (PromptWorldAgent(CAVEAgent)+PromptWorldHTTPServer(CAVEHTTPServer)). docs/mirror=0, docs/vision=17. DESIGNED-BUT-UNBUILT: 6 of 7 departments declared-only (skill/mcp/harness/operating_system/team/workflow, DEPARTMENT_TYPES :52-60, implemented:false, no route); persona-engineer = orphan 8th; UI index.html = 29-line single chat box NOT the designed dashboard (no convo-switch, no dept UI, /ws exists but UI never connects); CEO persona NOT injected (bare claude); paia hooks designed+transferable but settings_dir never passed; skills/start-promptworld = EMPTY dir (verified); no Dockerfile; product flywheel (assemble->project->publish->install, GEAR, *World compiler) unbuilt (Isaac: don't force promptworld to BE the *World compiler yet). NOMICON = NAME ONLY within promptworld: grep returns ONLY the 2026-06-06T22:23:33 decision line I wrote today from Isaac's sentence (+vision projections). NO design, NO code, NO scope. NUANCE: stale 2026-06-04 cursor referenced 'AC v3 in sancrev nomicon' -> nomicon likely lives in the SANCREV repo not promptworld; search sancrev to ground it before building. NET: promptworld has a clear finish-list (inject CEO persona / real dashboard UI / wire more departments / paia settings / start-skill+Dockerfile); nomicon is the one BLOCKED item — needs Isaac's definition or a sancrev search.  tags:[Promptworld, Nomicon, Departments, Status, Sancrev]
- [v3]  2026-06-07T14:54:01  FINDING: NOMICON GROUNDED (the status-map BLOCKER): the 2026-06-04 cursor guess 'AC v3 in sancrev nomicon' is WRONG — grep finds ZERO nomicon in treeshell/sancrev. The real nomicon lives in DOC-MIRROR-SYSTEM as the skill2framework 'nomicon' pipeline: doc-mirror-system/plugin/bin/nomicon-atomize, plugin/skills/doc-mirror-prompts/resources/prompts/skill2framework/nomicon/fold-into-nomicon/SKILL.md, + vision _framework-to-nomicon.md / _skill2framework.md / _framework.md. So 'nomicon' = a doc-mirror framework-authoring concept (fold skills/frameworks into a nomicon), NOT a promptworld-native module. Its relationship to 'finish promptworld' is GENUINELY UNDEFINED — Isaac's sentence 'promptworld INCLUDES the nomicon system' does not say HOW they connect. This remains BLOCKED on Isaac's scope: (a) is the nomicon an 8th promptworld department / output type the engineer-CEO compiles? (b) or a separate doc-mirror-system build that promptworld merely surfaces? (c) or something else? Recommend: get Isaac's one-line scope before building anything nomicon-side; meanwhile the unblocked promptworld finish-list proceeds.  tags:[Promptworld, Nomicon, Skill2Framework, Doc_Mirror]
- [v4]  2026-06-07T14:57:02  FINDING: CEO PERSONA verified-done by me (commit 93c3dd7, --append-system-prompt, my E2E re-run: WITH persona CEO names its 7 component kinds, WITHOUT=generic). NOMICON GROUNDED (CORRECTS my earlier 'likely sancrev/AC v3' guess — that was WRONG, zero nomicon in sancrev): the REAL nomicon is in DOC-MIRROR-SYSTEM (plugin/bin/nomicon-atomize + 10 _nomicon* vision docs + skill2framework/nomicon/fold-into-nomicon skill). WHAT IT IS (read the vision docs myself): nomicon = the AUTHOR'S FRAMEWORK-OF-FRAMEWORKS — {author}-nomicon (sancovp-nomicon) = a top-level HOLDER/SEARCH/ROUTER skill that holds {aios}-nomicon skills (one per AIOS), each routing into its AIOS project; FRACTAL (each aios-nomicon is itself a local nomicon). It is the UP-TERMINUS of the compiler ladder info->prompt->skill->rules->AIOS->framework->fold-into-nomicon, and the framework-to-nomicon pipeline ALREADY WORKS E2E (dogfooded on doc-mirror: Blog1->Blog2->chapter->framework-skill->plugin->nomicon-fold, 6 prompt-skills scored 1.00). A FRAMEWORK = a capability packaged as a HUMAN<->LLM PROTOCOL (the human-enactment/IRL side is what makes it a framework vs a bare LLM-side skill); has dream/pitch + human-IRL setup + AI-digital skills + a route to an AIOS. nomicon-atomize projects held frameworks out into a flat discoverable skills/ dir (canon nested + symlink projection). PROPOSED CONNECTION to promptworld (MY interpretation, for Isaac to confirm/correct — NOT asserted): promptworld = the BOTTOM of the SAME ladder whose TOP is the nomicon. PromptGym specialist agents make the 7 COMPONENT TYPES (the bottom rungs) -> those compose UP (prompt->skill->AIOS->framework) -> FOLD into the author's nomicon. So nomicon is NOT an 8th promptworld department (builder option a = NO); it is the UP-TERMINUS promptworld FEEDS (builder option b, sharpened): 'introduce nomicon into promptworld' = CLOSE the ladder so promptworld's components fold all the way up into the nomicon, AND the CEO CARRIES the nomicon as its world-model (per _nomicon.md v2 'the agent has the -nomicon which lets them mesh with the user way of thinking'). 'cascade into per-component agents' = the component types ARE the bottom rungs the PromptGym agents own; 'nomicon self-proving' = the skill2framework pipeline proves each framework E2E as it folds. NEEDS ISAAC: confirm/correct this ladder reading (resolves the builder's blocked fork).  tags:[Nomicon, Promptworld, Skill2framework, Aios, Framework, Ceo, Correction]
- [v5]  2026-06-07T17:56:55  FINDING: AIOS-DIRECTORY MODEL VERIFIED BY ME (re-ran /tmp/test_promptgym.py + read scoping). REVERT ba00fca (undoes wrong 3d1aea5) + REBUILD ceaacf6. p_main_agent.py: ClaudePMainAgent.config_dir -> CLAUDE_CONFIG_DIR in the -p subprocess env (per-agent AIOS scoping); _ensure_config_creds symlinks ~/.claude/.credentials.json into the scoped dir; additive (no config_dir = old behavior). promptgym/{skill,mcp,harness,operating_system,prompt,team,workflow}/ each = an AIOS DIR with its own CLAUDE.md. promptgym.py registry runs a type scoped to its dir (cwd=config_dir=the dir, on_event tagged by type = gallery-ready). MY re-run: skill/mcp specialized+isolated+distinct; ISOLATION held (no gnosys/doc-mirror/isaac leak despite running inside the repo); **DIRECTORY-CODES-IT: edited promptgym/workflow/CLAUDE.md sentinel 'ZEBRA-AIOS' -> agent emitted it -> True** (then restored). So an agent = its directory's AIOS, isolated, editable-by-dir. builder's call: the pre-existing prompt/compile_prompt IS the same wrong abstraction, superseded by promptgym/prompt/; left intact non-regressively, fold/remove in the CEO-control slice. ISAAC INSIGHT (his next msg): this means PromptWorld can ALSO INGEST an existing claude-code-system DIR and convert it -> a {name}-nomicon -> app. CAPABILITY = ASSEMBLABLE FROM EXISTING PIECES: (1) ingest = copy a dir into the bind-mounted workspace volume (trivial); (2) convert = the skill2framework/nomicon pipeline (AIOS->framework->fold-into-nomicon, VERIFIED E2E on doc-mirror) run by an agent equipped with those skills; (3) ->app = the packaging (app-rung, being built); and the AIOS-DIR model just verified is what makes 'an ingested dir IS a runnable agent' true. STATUS: pieces EXIST (IS); the END-TO-END 'drop dir -> get app' single flow is NOT wired (VISION = the composition / app-rung / self-compiling loop). This is literally the nomicon LADDER (ingest AIOS -> {name}-nomicon -> app-rung = the top) = implicitly confirms the ladder reading. NEXT slices: CEO-controls-via-CAVE-teams, gallery frontend, then packaging.  tags:[Promptgym, Aios, Directory, Claude_Config_Dir, Verified, Ingest, Nomicon]
- [v6]  2026-06-07T19:32:20  DECISION: REHYDRATION ANCHOR (corrected current state — supersedes the wrong-MCP detour; a fresh builder should act on THIS). === ARCHITECTURE (locked) === promptworld = CAVE + claude -p, NO SDNA (CAVE Agent.run duck-types its runtime agent.py:374; SDNA is goal-level-only). Each PromptGym agent = an AIOS DIRECTORY (the directory codes the agent: its own CLAUDE.md + .claude/skills + .claude/rules), run as claude -p SCOPED to its dir via CLAUDE_CONFIG_DIR + cwd=the dir (VERIFIED isolates from user/global config; creds symlinked in). === OPTION B LOCKED (this turn) === each specialist agent has its OWN specialized prompt (its dir's CLAUDE.md), does NOT inherit the CEO prompt (A rejected — would make specialists omniscient, breaking D2/D3: CEO knows ALL components, specialists know ONLY their one). Each agent has its OWN doc-mirror (provisioned INTO its scoped dir) + access to a shared UNIVERSAL WORKER subagent (a CEO-mirror, callable by every agent, the uniform capability without prompt-inheritance). CEO = its own omniscient AIOS (doc-mirror scoped to be ABOUT all components — not a 'global' doc-mirror, just the CEO's). Agents don't know each other except via the CAVE hub when teamed. === MVP SEQUENCE (Isaac, verbatim intent) === MVP1 = (a) ALL 7 PromptGym agents WORKING INDIVIDUALLY = fill out the 7 AIOS dirs for real (real CLAUDE.md + skills/rules per component type + doc-mirror provisioned + the universal-worker subagent), each chattable standalone; (b) the NOMICON SYSTEM done; (c) the CEO dispatches them via CLAUDE-CODE-NATIVE teams (Task/TeamCreate — NO custom mechanism, NO MCP, NO event visibility — invisible is ACCEPTED in MVP1). MVP2 = a promptworld-team (or skill that starts the team) -> CAVE AUTO-INJECTS to all agents as they have messages (cave_teams: CEO=TeamLeader, agents=Conversations whose .send runs OUR promptgym.run for AIOS-scoping+gallery streaming; dispatch = leader writes JSONL to agent msg file, Harness delivers — NOT an MCP) + the interface (gallery) shows them all receiving/responding. Isaac: 'I dont know if MVP2 even works right' = uncertain. === WHAT IS BUILT (verified by me) === branch promptworld-event-stream: ClaudePMainAgent streaming+compact (278811f); CEO persona (93c3dd7); /ws broadcast (167daf46); assistant-ui chat (adc7586); Monaco file-explorer path-jailed (32c7b088); PromptGym 7 AIOS-dir STUBS (basic CLAUDE.md) + config_dir scoping (ceaacf6); CAVE Heart/automation/cron inherited (PromptWorldAgent(CAVEAgent) super().__init__). === WRONG / TO REVERT === the GYM MCP (d6451ee: server/gym_mcp.py + /api/gym route + CEO --mcp-config) = WRONG mechanism (CEO must control via CAVE injection / native teams, NOT an MCP) AND wrong phase (MVP2-premature). REVERT it. === PREMATURE MVP2 (leave on branch, do NOT build on yet) === the GALLERY (ece99b2) — it's MVP2 visibility, built early; fine but depends on the CAVE-auto-inject which is MVP2. === NEXT (MVP1) === FIRST fill out the 7 agent AIOS dirs for real (B: own specialized prompt + doc-mirror + universal worker) so each WORKS INDIVIDUALLY; AND build the NOMICON system; CEO uses claude-code-native teams (invisible). === DEPLOYMENT (later, #1 packaging) === container with one doc-mirror provisioned per agent dir + the CEO's; neo4j (CartON) via docker-compose if absent; --strict-mcp-config to make any MCP deterministic; clean container ~/.claude (fixes the CEO-MCP startup-race = 'problem 2'). === MINIMAX === claude -p + ANTHROPIC_BASE_URL env reaches minimax (no SDNA); IS-by-doc, not tested this session.  tags:[Promptworld, Promptgym, Mvp1, Mvp2, Aios, Cave, Claude_Code_Sdk, Nomicon, Doc_Mirror, Universal_Worker, Rehydration, Revert_Mcp]
- [v7]  2026-06-07T19:35:12  OPEN: REHYDRATION VERIFIED: fresh builder pw-builder reconstructed the FULL corrected model (CAVE+claude-p no SDNA, AIOS-dir option-B agents, MVP1-3parts vs MVP2, revert-d6451ee-gym-MCP, all 6 built commits, next step) from the promptworld vision docs ALONE — zero drift vs the anchor. The durable layer is sufficient = green light to compact. BUT it honestly surfaced 3 DESIGN GAPS (spec does not exist yet — NOT rehydration failures) that BLOCK full MVP1 and must be resolved before/early in the build: (1) NOMICON SCOPE INSIDE PROMPTWORLD = UNDEFINED — needs Isaac. WHAT nomicon is = grounded (doc-mirror-system framework-of-frameworks: nomicon-atomize + skill2framework/fold-into-nomicon, dogfooded E2E); the LADDER reading (promptworld=bottom rungs/components -> fold up -> author nomicon=top) is my proposed interpretation Isaac has NOT formally confirmed; and 'the nomicon system done' as MVP1 part (b) has NO concrete promptworld-internal spec. Building it = guessing. (2) UNIVERSAL-WORKER subagent mechanism = named+locked but HOW it's built is unspecified (a claude-code subagent definition in each scoped dir? a shared callable? a CEO-mirror dir?). Needs a spec. (3) DOC-MIRROR-PROVISIONING per agent dir = mechanism unspecified (cp the doc-mirror skill into each scoped .claude/skills? run the doc-mirror install-wizard per dir? symlink?). Needs a spec. ALSO: minimax-via-ANTHROPIC_BASE_URL = IS-by-doc, untested. Minor: vision topic-headers _aios.md='Claude_Code_Sdk' / _cave.md='Jobworld' (journal auto-naming artifact; content correct). POST-COMPACT FIRST MOVE on the build thread = resolve these 3 gaps (nomicon first = Isaac's call) before filling the 7 AIOS dirs.  tags:[Promptworld, Mvp1, Nomicon, Universal_Worker, Doc_Mirror, Open, Rehydration_Verified]
- [v8]  2026-06-07T19:43:09  DECISION: COMPACT ANCHOR (2026-06-07 PM). READ FIRST POST-COMPACT: the MVP1 REHYDRATION ANCHOR (journal 2026-06-07T19:32:20, in vision _mvp1.md) = the authoritative model (CAVE+claude-p no SDNA; PromptGym = AIOS dirs, option B; MVP1 3-parts vs MVP2; revert d6451ee gym-MCP; 6 built commits on branch promptworld-event-stream). REHYDRATION PROVEN: fresh builder pw-builder reconstructed it ALL from the vision docs alone, zero drift. === NOMICON RESOLVED (Isaac confirmed 'yes') — NOT undefined, my mislabel: nomicon = doc-mirror-system's framework-of-frameworks (nomicon-atomize + skill2framework/fold-into-nomicon, ALREADY EXISTS, dogfooded E2E). promptworld's agents FOLD their made components UP into the author nomicon via that pipeline (comes WITH doc-mirror-provisioning). The APP RUNG (Isaac's geometry): ingest a claude-code dir -> fold into a {name}-nomicon -> agentify into a SIDECAR APP (the hot/self-forking/GOD-FROM-HEAVEN pattern) = a plugin skill (port-over/ingest) + packaging (#1). MVP1 nomicon slice = agents-can-fold-up (rides on doc-mirror-provisioning); the full ingest->app rung = plugin skill + packaging (MVP1/deployment boundary). So NO Isaac-blocker remains — it's a DECOMPOSITION job. === IN-FLIGHT: pw-builder (teammate, survives this compact) is doing the UNBLOCKED slice: surgically revert the gym-MCP mechanism (server/gym_mcp.py + CEO --mcp-config + the dispatch instruction; KEEP /api/gym route + run_gym if the gallery/standalone-chat needs them) + write the 7 REAL specialized CLAUDE.mds (option B) + the directory-codes-it E2E. It reports to my inbox; its artifacts must stand alone. === POST-COMPACT FIRST MOVES (build thread): (1) VERIFY pw-builder's slice myself (re-run directory-codes-it E2E + read the diff + confirm the revert didn't regress chat/files/ws/gallery/SPA); (2) SPEC the 2 mechanisms: universal-worker subagent (likely a claude-code subagent def dropped into each scoped .claude/agents/ = a CEO-mirror callable by every agent) + doc-mirror-provisioning per dir (cp/symlink the doc-mirror skill+CLIs into each scoped .claude, or run the install-wizard per dir); (3) DECOMPOSE the nomicon geometry into MVP1 tasks; (4) finish MVP1(a) = provision doc-mirror + universal-worker into the 7 dirs; then MVP1(b) nomicon-fold + (c) CEO-via-native-teams. === DEFERRED: MVP2 (cave_teams CAVE-injection: CEO=TeamLeader, agents=Conversations whose .send=promptgym.run, dispatch=leader writes JSONL->Harness delivers, NOT an MCP; + the gallery viz ece99b2 already built-but-premature); packaging #1 (Dockerfile/compose/install-wizard + plugin skills, clean container ~/.claude + --strict-mcp-config fixes the CEO-MCP race=problem2, neo4j via docker-compose); minimax-via-env test. === ME: commander; pw-builder = the promptworld builder (owns task #27). All verified-by-me. Continuable.  tags:[Promptworld, Compact, Nomicon, Mvp1, Rehydration, Pw_Builder]
- [v9]  2026-06-07T20:13:59  INTENT: INTENT (my #1 commander task while pw-builder completes the promptworld system core): build the NOMICON APP RUNG = the missing top rung above fold-into-nomicon. The UP ladder already EXISTS+dogfooded (skill2framework/{blog-organ,deep-dive,chapter,framework-skill,plugin,nomicon/fold-into-nomicon} prompt-skills @1.00 + nomicon-atomize bin): AIOS -> blog -> chapter -> framework-skill -> plugin -> fold-into-{author}-nomicon (holder/search/router). The GAP (Isaac verbatim): PromptWorld ingests a claude-code dir -> converts it into its own {name}-nomicon -> agentifies into a SIDECAR APP = a skill on the promptworld plugin. So the app rung = an orchestration skill: discover the AIOS(es) in a target claude-code dir -> run the existing UP pipeline per AIOS -> fold all into a fresh {name}-nomicon -> hand that {name}-nomicon to promptworld as the new app/CEO world-model (agentify). INTERPRETATION MARK: the deliverable = the ingest->{name}-nomicon orchestration + the agentify-into-promptworld handoff skill; the container/packaging half is the deployment boundary (#1). NOT asserting Isaac confirmed this exact decomposition; it follows from his APP-RUNG geometry in the compact anchor + his verbatim 'skill on the promptworld plugin'.  tags:[Nomicon, Promptworld, App_Rung, Skill2Framework, Ingest]
- [v10]  2026-06-07T20:17:12  DECISION: AUTHORED + committed the app-rung skill application/promptworld/skills/ingest-into-nomicon-app/SKILL.md (commit 071349e, branch promptworld-event-stream), marked UNVERIFIED. It orchestrates the 3 stages: INGEST (copy dir to workspace) + CONVERT (the dogfooded skill2framework UP pipeline 6 prompt-skills + nomicon-atomize -> fresh {name}-nomicon) + AGENTIFY (CEO carries the {name}-nomicon, ingested AIOS dirs = scoped agents). Container/packaging = deployment boundary #1 (handoff, not inlined). DISPATCHED background agent apprung-e2e@background-streams to PROVE it E2E on a minimal synthetic single-AIOS dir (acme): build src -> run the 3 stages -> verify {name}-nomicon valid + Frameworks table + atomized symlinks resolve + Stage-3 wiring. Its report is a CLAIM; I re-verify the artifacts myself before scoring. PARALLEL: pw-builder completing the system core (doc-mirror provisioning per dir + universal-worker subagent). Division of labor per Isaac: builder completes the promptworld system; nomicon (app-rung) is mine.  tags:[Nomicon, Promptworld, App_Rung, Dispatch]
- [v11]  2026-06-07T20:29:00  FINDING: VERIFIED BY ME (read apprung-e2e artifacts under /tmp/apprung_app/acme + the nomicon-atomize code): the app-rung skill ingest-into-nomicon-app WORKS E2E on a minimal synthetic AIOS. All 3 stages produced real artifacts: STAGE1 ingest (copied AIOS = valid scoped dir); STAGE2 convert (all 6 skill2framework hops -> blog1 via real metastack renderer, blog2 deep-dive, chapter, framework skill, plugin w/ valid plugin.json, fold into holder w/ ## Frameworks table 1 row, links resolve); STAGE3 agentify wiring (CEO scoped config gets the nomicon, ingested AIOS = valid specialist dir). Holder skills/acme-nomicon/SKILL.md (1514B) present; skills_flat symlinks resolve. UNKNOWN (scope-deferred): actual server boot + live CEO routing + PromptGym registration + packaging. The run found 2 PROMPT DEFECTS, now fixed in commit 21a7fa9: (1) name collision framework-skill {aios}-nomicon vs holder {NAME}-nomicon when NAME==aios -> pinned framework skill = {AUTHOR}-framework-{aios}; (2) atomize footgun -> moved holder to <app>/nomicon/, atomize into distinct <app>/skills/. Flow proven; fixed prompt follows the agent's proven workaround (UNVERIFIED on a clean re-run but low-risk deterministic path/name change).  tags:[Nomicon, Promptworld, App_Rung, Verified, Skill2Framework]
- [v12]  2026-06-07T22:32:34  VISION: GOD-FROM-HEAVEN = the autonomous self-fork + hot-reload loop on top of PromptWorld V1 (Isaac, reflective — capture as VISION, not asserted). THE PATTERN (Isaac verbatim): an agent that dispatches to a remote container where there is a COPY OF ITSELF that can develop itself; all capabilities become prompts to that agent; it evolves itself by working on ANOTHER sandbox, then the main loads the updates and restarts it. Originally the pre-Claude-Code influencer product (the CICD self-improving-agent, 'this is how I use agents'); now Claude Code makes the subagent/dispatch substrate NATIVE, so it collapses to wiring the self-fork + reload around Claude Code. ISAAC'S MECHANISM FOR CLOSING IT (verbatim intent): 'to make this happen, we just fold the entire promptworld and gym system into the nomicon system.' = run the app-rung (ingest a claude-code dir -> {name}-nomicon -> agentify into a sidecar app) on PROMPTWORLD ITSELF: promptworld+gym become foldable frameworks held in a promptworld-nomicon (homoiconic self-representation), the app-rung re-agentifies that nomicon into a fresh promptworld app = the SELF-FORK; the main loads the new image + restarts = the RELOAD. PIECES THAT ALREADY MAP (IS, verified this session): CEO + 7 specialists + universal-worker = verbatim copies of the doc-mirror system prompt + role block = literal 'copies of itself'; container + AGENT create->cp creds->start deploy = the sandbox + reload (currently ME-driven, not autonomous); app-rung skill = the self-forking half (E2E-proven on a MINIMAL synthetic single-AIOS only, NOT on promptworld's own multi-AIOS self). PROOF OBLIGATION to call it real (VISION until then): (1) run the app-rung on promptworld's OWN dir and get a working promptworld-nomicon that re-agentifies a fresh, runnable promptworld app (untested at promptworld's scale); (2) close the loop AUTONOMOUSLY — a running promptworld forks the sandbox, develops the next version, the main reloads+restarts ITSELF, with NO human dispatching+redeploying. (2) is the actual product. Today the loop runs only because I (the commander) am the 'main that loads the updates and restarts it'. STATUS: the pieces exist; the closed autonomous fold-self-into-nomicon -> regenerate -> reload loop is UNPROVEN VISION.  tags:[Promptworld, Nomicon, God_From_Heaven, Self_Fork, App_Rung, Cicd, Influencer_Product]
- [v13]  2026-06-07T22:37:05  VISION: THE NOMICON = THE ONE SKILL THAT IS THE WHOLE BUILD-AND-DISTRIBUTE LADDER (Isaac, reflective VISION — capture, not asserted). THE LADDER (Isaac verbatim): make xyz component -> make AIOS (eventually) -> make a nomicon -> make an app -> make an app a MAIN AGENT can install and hit -> make that but with LOTS of AIOSes in it -> even more complex chaining and agent topology patterns... ALL INSIDE the nomicon, which is just a SKILL that explains how to do all that stuff and store it hierarchically and shard it off and publish it. ORDERING ANSWER (Isaac asked 'is the nomicon before or after AIOS'): AFTER the AIOS (and before the app). Full order: component -> AIOS -> framework -> nomicon -> app -> installable-app -> multi-AIOS-app -> topology. You cannot fold an AIOS into a nomicon until the AIOS exists; the app-rung agentifies the nomicon INTO the app. FRACTAL NUANCE (why nomicon felt ambiguous): the {aios}-nomicon = the framework-skill = the index/router sitting DIRECTLY ON one AIOS (right after that AIOS); the {author}-nomicon = the holder CONTAINING all the {aios}-nomicons (after all AIOSes). Both are 'after AIOS' (one per-AIOS, one over-all). THE REFRAME: the nomicon is NOT a data structure, it is the SKILL that (a) explains how to climb each rung (component->AIOS->framework->app->installable->multi-AIOS->topology), (b) STORES it hierarchically (the fractal {author}->{aios} nesting), (c) SHARDS it (nomicon-atomize flat projection), (d) PUBLISHES it (ship-a-plugin). IS today (verified this session + grounded): the rung-skills exist SEPARATELY — compile-claude-code-component, make-ai-operating-system, skill2framework (blog/chapter/framework-skill/plugin/fold-into-nomicon, dogfooded 1.00), the app-rung (E2E on a minimal AIOS), nomicon-atomize (shard), ship-a-plugin (publish: a marketplace catalog references plugins, NOT a monorepo). VISION (not built): the UNIFIED nomicon-skill that IS the whole ladder end-to-end + the app/installable/multi-AIOS/topology rungs + publishing all wired THROUGH the one nomicon skill = the homoiconic top that closes GOD-from-HEAVEN (fold promptworld+gym into the nomicon -> self-regenerate). PROOF OBLIGATION: the unified nomicon skill drives an end-to-end run component->...->published-installable-app, AND folding promptworld's own gym/CEO into it regenerates a runnable promptworld.  tags:[Nomicon, Complexity_Ladder, Aios, App_Rung, Ship_A_Plugin, God_From_Heaven]
- [v14]  2026-06-07T22:42:01  VISION: THE NOMICON IS A SELF-SIMILAR ROLL-UP OPERATOR; nomicon^n = compose it n times (Isaac, reflective VISION — capture, not asserted). THE STRUCTURE (Isaac verbatim intent): originally the nomicon ended at AIOS; now there is also app + etc, but these all NEST THE SAME WAY AIOS did, only the units involve AIOSes. So they are all inside nomicon^n on top of nomicon_base: nomicon_base -> nomicon' -> nomicon'' ... each one an ENTIRE CYCLE of rolling up all these things. THE SAME CYCLE AT EVERY LEVEL = roll up the units one level down -> index -> shard -> publish; each level's UNITS are the previous level's OUTPUTS, and the higher units CONTAIN AIOSes all the way down. THE TOWER: nomicon_base (units=components -> AIOS -> framework -> author-nomicon-over-AIOSes; 'originally ended here'); nomicon' (units=AIOSes -> an APP = the app-rung, multi-AIOS into one app); nomicon'' (units=apps -> a DESKTOP APP = SANCREV OPERA, multi-app shell); nomicon''' (units=boxes/desktops -> a DISTRIBUTED multi-box system, apps on remote boxes talking to each other); ...onward, deferred ('huger and huger shit we arent getting into yet, but eventually will as we deploy'). LOAD-BEARING PROPERTY: every rung nests the same way -> one skill recursively, no new mechanism per level -> scales forever. This is WHY GOD-from-HEAVEN closes: folding promptworld into its own nomicon = applying the operator to itself. IS (verified/grounded): only nomicon_base (compile-component/make-aios/skill2framework/fold/atomize/ship) + the first half of nomicon' (the app-rung, E2E on a TOY single-AIOS) have code. VISION (not built): nomicon'' (opera) / nomicon''' (distributed) are the elegant hypothesis. PROOF OBLIGATION for 'they all nest the same way': show the SAME operator actually COMPOSES at the next level up — e.g. the app-rung generalizes to roll N apps into one opera desktop shell with NO new mechanism. Until one higher rung runs, the self-similarity is VISION.  tags:[Nomicon, Self_Similar, Complexity_Ladder, Sancrev_Opera, Distributed, God_From_Heaven]
