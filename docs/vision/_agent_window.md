# vision: Promptworld (topic — auto-created by `journal`)

<!-- ===VISION DELTA: id-tagged appends below = the gap (`vision diff <m>`); `doc-mirror-commit --realizes <ids>` drops them on build === -->
- [v1]  2026-06-07T18:58:47  FINDING: BUILT + E2E-VERIFIED the GALLERY frontend: one live window per agent, demuxed from a single /ws by alias. agentBus.ts (pub/sub), AgentWindow.tsx (per-alias own useExternalStoreRuntime; subscribes to bus; folds live stream via reduceEvent into per-turn assistant bubble; composer onNew → /api/chat for ceo else /api/gym/{type} = standalone chat per specialist), Gallery.tsx (grid). App.tsx: ONE /ws demuxed → publish to bus + alias list; pre-seeds ceo+7 gym (no first-event race). Thread.tsx parameterized placeholder/emptyText. eventMapping.routeByAlias = pure demux. runtime.tsx removed (superseded). E2E: (a) vitest 6/6 incl routeByAlias (interleaved ceo+skill → separate threads, skill tool-call does NOT leak into ceo). (b) vite build dist index-Dj9tZQbu.js; curl / serves SPA. (c) headless chromium: 8 windows pre-seeded [ceo,skill,mcp,harness,operating_system,prompt,team,workflow]; a real /api/gym/skill stream + a CEO chat each render a live assistant bubble in their OWN window (skill leaves 'idle') = per-agent demux proven in browser. Screenshot /tmp/promptworld_gallery.png. Commit ece99b2. CAVEAT: corrected a FALSE-POSITIVE test (first version matched window chrome/header/placeholder/user-echo; fixed to structural .msg.assistant count). HONEST: CEO-AUTO-dispatch of gym tool is FLAKY under the CEO's heavy inherited user-MCP env (gym MCP sometimes not connected → CEO says tool unavailable); proven reliable via direct slice-C /api/chat E2E. Reliability follow-up: scope the CEO config (config_dir or --strict-mcp-config) so the gym tool connects deterministically — design decision for Isaac. NEXT (per team-lead): cave_teams adapter (B); CEO-MCP reliability.  tags:[Promptworld, Frontend, Gallery, Demux, Agent_Window]
