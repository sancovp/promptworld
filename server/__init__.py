"""PromptWorld v1 server package.

A standalone CAVE-*World fork: an engineer-CEO agent you chat with through a web UI,
backed by the `claude -p` (subscription) main_agent (ClaudePMainAgent) — NO tmux.
Departments are DEFERRED in v1; this is just the engineer-CEO + chat.
"""
